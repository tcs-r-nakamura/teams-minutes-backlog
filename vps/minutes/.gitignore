# runtime data (internal info - never commit)
inbox/
processed/
work/
out/
state/
# concurrency lock (shared aiocr account)
.minutes.lock
# secrets (tokens) - keep in ~/.config/minutes.env, never in the repo
*.env
minutes.env
# Nix
result
result-*
# python
__pycache__/
*.pyc
