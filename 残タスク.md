# 議事録自動化 残タスク

最終更新：2026-07-16　／　凡例：[あなた]=中村さん操作 ・ [私]=Claude実施

> **いま解いている課題：VPSがSSH鍵＋固定グローバルIPを要求するため、実質 中村さんしか運用できない。**
> 処理そのものは2026-07-14に本番稼働済み（第2回まで登録済み）。**残っているのは「他の人が使えるようにする」ことだけ。**
> 方針・根拠・実測は `議事録自動化_フロー整理.md` が一次情報。設計は `議事録自動化_サーバー化設計書.md` v0.8。

## ✅ 完了済み（主なもの）
- Teams字幕(.vtt)×whisper 併用（実装・検証・Codex3回）
- 話者名の半角スペース正規化＋`speaker_aliases.txt`
- `draft.ps1`（③のAI草案生成API化）実装＋Codex2回＋UTF-8応答バグ修正＋実データ検証＋トークンUser環境変数フォールバック
- さくらAI Engine 連携：トークン設定・**Chat疎通**・**Whisper疎通(実データ・高品質)**・草案LLM＝`gpt-oss-120b`確定
- PC手順書：方法①(`-To draft`自動)／方法②(ChatGPT手動)の両対応、A-3bトークン設定
- 実機E2E：`-To draft`で 字幕照合取込→ローカル文字起こし→さくらAI下書き→`minutes.md`成功
- ~~サーバー化設計書 **v0.7**（入口=B2 / …）~~ → **v0.8 で入口を全面改訂**（下記 2026-07-16）
- **VPS Step2 雛形**を `vps/minutes/` に作成＋ローカル/実機検証
- **運用のVPS版CLI一本化**（PC版は当面残すが主運用から外す）
- **`~/.config/minutes.env` をVPSに設定**（`config.py`が自動読込。`SAKURA/BACKLOG set: True`確認済み）
- **手順書を運用手順に再編**（クイックリファレンス/初回A/配置B/コマンドC/トラブルシュート）
- **登壇者を発言最多の1人に絞る**（`vtt.py`/`main.py`/テンプレ。他の話者は本文に残す。2026-07-14）
- **入口を「手動CLI＋薄いラッパー`run.sh`」に確定**（`inbox/`の録画+字幕を自動検出しdraft実行。2026-07-14）
- **不正トークン起動時ガード**（`config.token_problem`。プレースホルダ`＜…＞`/非ASCII/空白を明確なエラーで弾く。2026-07-14）
- **最新版をVPSへ配置＋③実データ検証成功**（2026-07-14）：run.sh自動検出→Whisper→登壇者1人化(6人中 渡邉 麗太)→下書き生成まで完走。会議番号自動採番も復活（`minutes.env`のプレースホルダ修正後）
- **★register 本POST初通し(④)完了**（2026-07-14）：第2回議事録をBacklogに新規登録成功（docId 019f5e8d…）。録画→Whisper→用語集→下書き→整形→人チェック→登録が実運用として一周。
- **議事録の体裁を既存(第1回)に統一**（2026-07-14）：テンプレを第1回型に（`#`タイトル/`---`無し/太字禁止/粒度強化）。加えてコード側で確定整形 `prompt.normalize_md`（行末空白除去・見出し後の空行・太字`**`除去、フェンス内は保護）。
- **用語集で文字起こし誤変換を汎用訂正**（2026-07-14）：`glossary.txt`＋`prompt.load_glossary`。正しい表記へLLMが訂正（例 MPフォー→MP4、Cloud Code→Claude Code）。会議で出た用語を追記して運用。
- **入力の自動退避**（2026-07-14）：draft成功後 `inbox/`→`processed/<日時>_<名>/` へ移動（毎回の手動削除が不要）。

### 2026-07-15〜16（他の人も使えるようにする＝入口の作り直し）
- **フロー整理**（`議事録自動化_フロー整理.md`）：上長助言「認証認可より先にアプリ全体のフローを整理する」に沿って全体を整理。
- **調査7本**（Graph公式ドキュメント根拠）。判明した決定的な事実：
  - **OneDrive の `delta` は root 単位。フォルダ単位の監視は公式REST仕様で確認できない**（→ 全自動案を捨てる決め手）
  - **transcript は Graph で取れるが、Teams管理センターに既定OFFのトグルが2つ**（片方OFFで403・回避策なし・**7月末から有効**）
  - **`Lists.SelectedOperations.Selected` は付与APIが beta のみ＝本番非サポート** → **`Sites.Selected` ＋ 専用サイト**へ
  - **delegated は「アプリ権限 ∩ 本人権限」**（公式明記）＝**アプリが本人の権限を超えることは構造的にない**
- **Codexレビュー5回**（実装前に潰したバグ：delta自己反応ループ・祖先判定漏れ・停止中の承認見落とし・
  検証時のスコープ混入・`scp` の `offline_access` 誤判定）。
- **✅ 小見山さんの暫定承認**（2026-07-15）「一旦その形で進めていただいて結構です」＋検証用アプリ登録の許可。
- **✅ 実機検証（今井さんの手を借りずに完遂）**：
  - アプリ登録／device code flow／`refresh_token` 発行 → **管理者同意は不要だった**
  - VPSから Graph：一覧200・アップロード201・ダウンロード200(内容一致)
  - **`grant_type=refresh_token` で人の介在なしに更新成功**
  - **条件付きアクセスのIP制限は今回の条件ではブロックされず**。**送信元IP `133.125.62.159` を確定**
- **✅ 専用SharePointサイト新設**（`/sites/minutes`・**一般ユーザーで作成できた**）＋4フォルダ＋`siteId` 確定。
- **✅ OneDrive同期の挙動を実測**（公式に文書化されていない領域）：
  編集・`out/`→`processed/` のドラッグ移動で **item-id 不変** → **`approved/` 方式が成立**・**公開前チェックの手段が確定**。
- **✅ サインイン頻度の短い設定が無いことを実測**（2026-07-16・**15時間11分後の refresh が成功**）。
- **✅ 設計書 v0.8**：入口を SharePoint＋Graph delegated に全面改訂。**B2/Caddy/443/証明書/利用者トークンが不採用**。

---

## 方針（確定）
- **運用主体はVPS版CLIに一本化**。PC版は当面残すが主運用ではない（二重運用のズレ回避）。
- **入口（トリガ）＝専用SharePointサイト＋VPSからの15分ポーリング**（**2026-07-16 改訂・上長暫定承認済み**）。
  担当者がブラウザ or OneDrive同期で mp4/vtt を置く → VPSが Graph（delegated・`Sites.Selected`）で検知
  → 下書きを同じ場所へ書き戻す → 人がチェック → `approved/` へ移動＝登録。
  - **認証は利用者のM365アカウントそのもの**（独自トークンは発行しない）。**app-only は会社方針で不可。**
  - **VPSは外部から接続を受けない**（outbound のみ）→ **`configuration.nix` を触らずに済む。**
  - ~~入口＝手動CLI＋`run.sh`（2026-07-14決定）~~ → **Phase 1 のつなぎ運用に格下げ**。
    **中村さんしか実行できないため、本番入口にはできない**（これが解いている課題そのもの）。
- **完全自動化は目標にしない**（上長談「**指定の場所にファイルを置いてAPI実行したら記事が生成されるといっただけでも十分**」）。
  会議は週1回。全自動の対価（他人のドライブ全体への可視性・管理者トグル依存・実装量）が見合わない。
- **Backlog Webhook＝出口 の認識**：VPSがBacklog APIで議事録を「登録」する(=実装済み)。
  Backlog側のWebhook通知(イベント→外部)は設定次第の別機能で、VPS側実装は不要。→認識合わせ済み。

## ⬜ 残タスク

### 0. ★本命（他の人も議事録を作れるようにする）― いまここ

**ブロッカーは1つ：今井さん（M365管理者）による `Sites.Selected` の付与。** これが無いと本番構成の検証に進めない。

**依頼の前にやること（✅ 2026-07-16 完了）**
- [x] [あなた] ~~アプリのリネーム＋`Files.ReadWrite` の同意削除~~ → **新アプリを登録し直した方が確実だったので変更。**
      **`minutes-automation`（`3c741d2b-6ee5-4ef1-adf5-9f096de64d79`）＝ `Sites.Selected` ＋ `offline_access` のみ。**
      → **同意を後から外すのではなく最初から入れない**ので、混入が**構造的に起きない**
      → 実測：`scope` は `profile openid email https://graph.microsoft.com/Sites.Selected`（**`Files.*` 皆無**）
      → 旧 `81214b2e-…` は**後始末で削除する＝アプリの数は1つのまま**（承認の条件内）
- [x] [あなた] **★付与前ベースラインを取得（今しか取れない値）**：**専用サイトを叩いて `403 accessDenied`**
      → **付与後に200になれば「②の付与が効いた」と断言できる**（変わるのは②だけ）
- [ ] [あなた] **サインイン頻度の実測を継続**（`~/.minutes-verify/rt` を消さず毎日 refresh を試す）
      → 計測起点は**最後の対話的サインイン（7/15 16:44）**。refreshではカウンタが戻らないので**待つだけで証拠が積み上がる**
      → **7/22 まで通れば「7日設定も無い」と言える**（今井さんへの確認は続行するが、待ち時間がタダで検証に変わる）
      → ⚠️ **条件付きアクセスはアプリ単位で絞れる**ので、旧アプリでの結果は**参考値**。本番アプリ（`rt-selected`）でも回す

**今井さんへの依頼（実質1件。※アプリ登録・同意・サイト新設・メンバー追加は自力で完了済み）**
- [ ] [あなた] **① 専用サイトへの `Sites.Selected` 付与（roles: `write`）** ← **唯一のブロッカー**
      対象アプリ＝**`3c741d2b-6ee5-4ef1-adf5-9f096de64d79`（`minutes-automation`）**
      → ~~中村さんをサイトのメンバーに追加~~ **不要**（**中村さんがサイトの作成者＝所有者**。他の担当者の追加も自分でできる）
- [ ] [あなた] **② 条件付きアクセスの確認**：**長いサインイン頻度設定**（1日/7日/30日）／IP制限
      → **設定されているとアプリ側で回避不可＝方針ごと組み直し。** 送信元IPは `133.125.62.159` と確定済み
- [ ] [あなた] ③ **保持ポリシー（Purview）がこのサイトに適用されるか**（容量解放と削除に直結）

**付与後の検証（ここが最大の未知。公式にも現場にも情報が無い）**
- [ ] [あなた+私] **`Sites.Selected` で 一覧/DL/書き戻し が通るか**（**drive経路で。lists経路は403報告あり**）
- [ ] [あなた+私] **⚠️ ネガティブテスト：権限を付けていない別サイトが403になること** ← **セキュリティ主張の本体。省略不可**
      → 根拠：`Files.SelectedOperations.Selected` で**権限の無いファイルまでDLできた**報告があり、
        **MSスタッフとMVPの双方が「想定外」と認めたまま解決未確認**（2025-01）
- [ ] [あなた+私] **拒否が 403 か「200＋空」か**を記録 → **空を成功扱いしない実装にする**（変更検知が静かに死ぬ）
- [ ] [あなた+私] **実録画（250MB超）の `createUploadSession`**（**小ファイルで通ってもスキップしない**。過去に境界で落ちた前例）
- [ ] [あなた+私] **Graph の item-id でも移動が保持されるか**（同期側は `UniqueId` で確認済み。Graph側は未確認）
- [ ] [あなた] **`approved/` のフォルダ単位の権限分離** ← **`approved/` 方式の採用条件**
      → **設定できるだけでは不十分。権限の無い人が移動・コピー・上書きできないことを実際に試す**（同期経由で崩れやすい）
      → **不可なら承認方式を再検討**（誤登録はAPIで取り消せない）

**実装（検証が通ってから）**
- [ ] [私] `src/graph.py`（device code / refresh / delta / DL / UL）＋ `src/ingest.py`（ポーリング→ジョブ→書き戻し）
- [ ] [私] `config.py` に `SP_SITE_ID` / `SP_DRIVE_ID`（**`SP_LIST_ID` ではない**）／`flake.nix` に `msal`
- [ ] [私] **refresh_token をバッチ専用OSユーザーへ分離**（全員が同じ `aiocr` では `0600` が無意味・フロー整理 §8.4）
- [ ] [私] **停止検知の通知**（refresh_token 失効で無人ポーリングは止まりうる。**気づけないと詰む**）
- [ ] [あなた] **検証用アプリの後始末**：トークン消去（`/tmp/token*.json`・`~/.minutes-verify/`）／同意削除／
      **RTの明示的失効**／アプリ削除／OneDriveの `minutes-test` 削除 ← **1つも漏らさない**
      → **古いRTは自動失効しない（実測確認済み）。漏れれば本人としてGraphを操作できる。**

### 1. 現行運用（Phase 1・回っている）
- [x] [あなた] 最新の `vps/minutes/`（run.sh＋トークンガード）をVPSへ配置（2026-07-14完了）
- [x] [あなた+私] **register 本POST初通し(④)完了**（2026-07-14）。第2回をBacklog新規登録成功。
      → 実運用フローが一周。以降は毎回このフロー（`./run.sh`→人チェック→`register --dry-run`→`register`）で回す。
- [ ] [運用] 毎会議：新しい`glossary.txt`用語の追記／登録後のBacklog目視確認（APIでは削除できず、取り消しはブラウザでの手作業になるため`--dry-run`必須）

### 2. VPS版（Step2✅ → 完成度上げ / Step3は将来）
- [x] [あなた] Step2雛形をVPSへ転送＋`nix develop`＋実走（2026-07-13）
- [x] [あなた+私] VPSで `draft` を実走：46分mp3→Whisper→字幕→gpt-oss-120bで下書き成功
- [x] [私] 会議番号のBacklog自動採番を移植（`backlog.meeting_no`。draftで`--no`省略時に自動）
- [x] [私] `register` コマンド＋重複チェック＋番号衝突検出＋APIキー秘匿＋fail-closed
- [x] [あなた+私] VPS実機で `register --dry-run` 検証（認証OK・ツリー取得OK・`[DUPLICATE]`停止）
- [x] [私] トークン自動読込（`~/.config/minutes.env`・所有者/権限チェック・キー名検証）
- [x] [私] 登壇者を発言最多の1人に絞る（2026-07-14）
- [x] [私] 入口ラッパー `run.sh` 追加＋`.gitattributes`でLF固定（2026-07-14）
- [x] [私] 不正トークン起動時ガード（`config.token_problem`＋`main()`で検査。2026-07-14）
- [x] [あなた+私] 実データE2E検証（③）：run.sh→Whisper→登壇者1人化→下書き 成功（2026-07-14）
- [ ] [私] チャンク境界のオーバーラップ＆重複除去、LLMトークン上限対策（任意・品質向上）
- [ ] [私] ジョブ/状態管理(`jobstore.py`)・クォータ台帳(`quota.py`)… **§0の実装で必要になる**（HTTPの有無と無関係）
- [x] ~~[私] 入口 `server.py`（FastAPI）~~ → **✗ 廃止（2026-07-16）。** 入口がSharePointになり受口が不要（設計書 §6.3）
- [x] ~~[あなた] Caddy(443)＋証明書＋利用者トークン発行~~ → **✗ 消滅（2026-07-16）。**
      **VPSは外部から接続を受けなくなった** → 443の待受・証明書・ドメイン・独自トークン運用が**まとめて不要**。
      **v0.7 が「グローバル非依存制約の唯一の例外」として管理者OKを取ろうとしていた変更が発生しない。**

### 3. 合意・ガバナンス
- [x] Backlog Webhook＝出口 の認識合わせ（2026-07-14整理済み）
- [x] ~~[あなた] 利用場所の確定（IP制限の要否）※外部公開する場合のみ~~ → **✗ 消滅（外部公開しないため）**
- [x] **[あなた] 方針の上長承認**（2026-07-15・小見山さん・暫定）
- [ ] [あなた] 未チェック草案を共有領域に出さない方針の周知（Backlog登録＝唯一の出口）
      ※専用SharePointサイトは全社共有領域ではない（議事録担当者のみ）ので、`out/` に草案を置くのは この原則に反しない
- [ ] [あなた] **`approved/` ＝ 登録**という運用の周知（**うっかり移動＝登録。APIで取り消せない**）

### 4. 任意・将来
- [x] ~~Microsoft Graph API での録画/字幕 自動取り込み（別プロジェクト）~~
      → **§0 の本命に格上げ（2026-07-16）。** ただし**「人が置く」は残す**（全自動＝主催者のドライブ全体が視界に入る・§7.2）
- [x] ~~B1（Webアップロード画面）~~ → **✗ 消滅**（443バックエンドが無くなったため。そもそも全員エンジニアで不要）
- [ ] PC版 *.ps1 の整理（縮小方向・優先度低）
- [ ] **パスワードレスでのサインインを試す**（`amr` が `pwd` のため、**中村さんのパスワード変更でRTが失効する**。
      パスワードレスなら失効しない＝穴を塞げる可能性。**要確認**・フロー整理 §8.4）

---

## 次の一手（推奨）

**処理は2026-07-14に本番稼働済み。いま解いているのは「中村さんしか運用できない」こと。**

**順番**：
1. **サインイン頻度の実測を継続**（毎日 refresh。**`Files.ReadWrite` の同意を外す前に**。外すと失敗の原因が切り分けられなくなる）
2. **アプリの本番化**（リネーム → `Files.ReadWrite` 削除 → `Sites.Selected` 追加）
3. **今井さんへ依頼**（付与＋メンバー追加／条件付きアクセス確認）
4. **付与後に再検証**（特に**ネガティブテスト**）→ 通れば `graph.py` / `ingest.py` 実装

**その間も Phase 1 で運用は回る**（中村さんの手動オペ）：
`inbox/へ録画+字幕をscp` →`./run.sh`→`out/minutes.md`を人チェック→`register --dry-run`→`register`→Backlog目視。

**⚠️ 最大の未知**：**delegated × `Sites.Selected` × 各操作は、公式にも現場にも情報が無い**（delta の報告はゼロ）。
**通らなければ方針の作り直し。** 付与後の検証までは「たぶん動く」以上のことは言えない。
