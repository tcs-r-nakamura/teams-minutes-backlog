# 議事録自動化 フロー整理（2026-07-14）

## 0. この文書の位置づけ

上長助言「**認証認可より先に、アプリ全体のフローを整理する**」に沿って、議事録自動化の
**全体フロー**と、それに従属する**トリガ（起点）・認証認可**を整理する。
サーバー化設計書 v0.7 の **§6 入口 / §7 取り込み** を、今日判明した事実で見直す位置づけ。

**今日判明した重要な事実（設計前提の更新）**
- **録画(mp4)** は、Teams会議時に **OneDrive/SharePoint へ自動保存**される（Microsoft既定＋実運用で確認）。
- **字幕(vtt)** は、mp4と**同じフォルダには存在しない**（実機で確認済み）。Teamsの**トランスクリプト**として
  会議に紐づき、現状は **Teamsから手動ダウンロード**、Phase 2 では **Graph のトランスクリプトAPI**
  （`onlineMeetings/transcripts` 等）で取得する想定。→ mp4と字幕は**取得元が別**。
- その後、録画した人が**手動でCybozuにアップロード**している（全社閲覧用）。
- v0.7 は「録画はCybozu・**Cybozu APIなし**＝自動取得は将来課題」としていたが、
  **録画の原本はSharePoint側にもあり、SharePoint/OneDriveはGraph APIで取得可能**。
  → 「自動取得できない」という前提は（少なくとも録画については）誤り。**OneDrive/SharePoint（Teams録画の保存先）を起点にすれば自動化の道がある。**
- **本件の実態（2026-07-14 確認）**：**主催者は固定**で、録画は**その1人のOneDrive（Recordings）に mp4 のみ**保存される。
  → 監視対象が**1箇所に確定**する（会議ごとに散らばらない）。字幕は別途トランスクリプトから取得。
- 実装は v0.7 の B2(HTTP/443) ではなく、**手動SSH＋CLI（`run.sh`）で稼働中**（第2回登録済み）。

---

## 1. 現状フロー（実装済み・稼働中）

```
1. Teams会議
2. →【自動】録画mp4 が OneDrive/SharePoint に保存（字幕vttは同フォルダに無い）
3. →【手動】録画者が Cybozu にアップロード（全社閲覧用の保管＋議事録の録画リンク元）
4. →【手動】録画(mp4)を入手（Cybozu/OneDrive）＋字幕(vtt)を Teams のトランスクリプトからDL
5. →【手動】scp で VPS の inbox/ へ
6. →【手動起動】./run.sh（音声抽出→さくらWhisper→用語訂正→gpt-oss-120bで下書き→整形）
7. →【人】out/minutes.md を公開前チェック
8. →【手動】register（Backlog Document APIで登録）
```

- **3〜5 は「同じファイルの移し替え」**で重複手作業。原本は最初からSharePointにある。
- 処理(6)・チェック(7)・登録(8)は完成・実運用済み（設計書 §3〜§4 のとおり）。

---

## 2. 現状の課題

- **重複手作業**：SharePoint→Cybozu→DL→scp と、同じ録画を何度も移動している。
- **アクセス配布が重い**：他メンバーが手動運用するには、VPSへのSSH（**公開鍵＋固定グローバルIP許可**）が要る。固定IPが無い人は運用に乗れない。
- **起動が手動**：会議のたびに人が scp→run.sh を実行。

---

## 3. トリガ（起点）方式 × 認証認可 ― フローが認証を決める

| トリガ方式 | 認証・認可の主体 | 録画の入手 | MS依存 | 実装規模 |
|---|---|---|---|---|
| **手動SSH＋CLI（現状）** | **SSH鍵＋許可IP**（自前） | 手動DL(Cybozu) | なし | 実装済み |
| 直接HTTPエンドポイント(443)＝旧B2 | **自前の発行トークン＋TLS**（＋任意IP） | PCからPOST | なし | 中〜大（認証設計） |
| Teamsチャット/コマンド起点(Graph/Bot) | **M365アカウント**（Teams認証がそのまま） | Graph API | 強 | 大・要権限/承認 |
| **SharePoint/OneDrive配置→hook（本命候補）** | **M365アプリ登録**（VPSアプリの資格情報） | **Graph APIで自動取得** | 強 | 大・要権限/承認 |

**要点**：Microsoft系（Teams/SharePoint）に乗せると、**利用者の操作はTeamsで録画するだけ**になり、
**利用者ごとのSSH鍵/IP配布は不要**。ただし**VPS側はEntra IDアプリ（app-only）としてGraphにアクセス**するため、
実際のアクセス範囲は「会議参加者かどうか」だけでは決まらず、**M365管理者が付与するアプリ権限・対象サイト/ユーザー・
Teamsアプリケーションアクセスポリシーで制御**する。逆に手動SSH/直接HTTPを続けるなら自前の認証（SSH+IP or トークン）が要る。

---

## 4. 目標フロー（段階計画）

### Phase 1（現在）：手動 SSH＋CLI ― つなぎ運用
- いま稼働中。当面はこれで回す。
- 他メンバーに配るなら、**公開鍵＋固定IPの許可**が必要（固定IPが無い人は「運用者に録画を渡す」でカバー）。

### Phase 2（本命）：固定主催者のOneDrive を起点に Graph API で自動取得
本件は**主催者が固定**なので、監視対象は**その1人のOneDrive（Recordings フォルダ）1箇所**に確定する。
```
1. Teams会議 →【自動】録画mp4 が 固定主催者のOneDrive/Recordings に保存（mp4のみ）
2. →【自動】VPSが そのフォルダを delta で定期ポーリング（インバウンド公開不要）
3. →【自動】新規mp4を取得（Drive API）＋対応する字幕をトランスクリプトAPIで取得（主催者の会議から照合）
4. →【自動】処理コア（既存src/）→ out/minutes.md
5. →【人】公開前チェック → 【手動】register（人手は残す：設計書 §8）
```
**取得経路・権限の候補（承認しやすい＝権限が狭い順に検証）**
1. **(C) Teams会議API ― 推奨・最有力**：Graphの**録画API `OnlineMeetingRecording.Read.All`** ＋ **字幕API `OnlineMeetingTranscript.Read.All`** を、**Teamsのアプリケーションアクセスポリシーで固定主催者1人に限定**できる。録画・字幕とも「1人分だけ」に絞れ、返る metadata の `contentCorrelationId`/`meetingId` で **mp4↔字幕の対応付けも堅い**（OneDriveのファイル名推定より確実）。※meetingId解決・**未期限切れの会議のみ**・録画content取得は**従量課金**、を要検証。
2. **(D) 選択ファイル権限 `Files.SelectedOperations.Selected`**：主催者OneDriveの Recordings フォルダ「だけ」に絞れるか検証（driveItem delta が通るか要確認）。
3. **(B) SharePoint専用サイト ＋ `Sites.Selected`**：録画を専用サイトへ **Power Automateで自動コピー**（手動でも可）＋そのサイトだけ最小権限。
4. **(A) 主催者OneDriveをそのまま監視 ― 暫定/検証用**：`Files.Read.All`（**全社のOneDriveを読める広い権限**・読み取りのみでよい）。実装は最簡だが権限が広いので**本番の主案にはしない**。
- **承認の出し方**：管理者・上長には **C → D → B → A の順**（権限が狭い＝通りやすい順）で提示する。開発の実機検証も **C を最優先**に。

- **scp・DL が消え、起動も自動化**。
- **利用者側の認証（SSH/IP・自前トークン）は不要**になり、アクセス範囲は**M365管理者が付与するアプリ権限で制御**される。
- インバウンドを晒したくなければ **Webhookでなく定期ポーリング**（VPSからの外向き通信のみ）で可。月5回ならポーリングで十分。
- ※旧B2（自前トークンHTTP）は、Graphが使えるなら不要になりうる。
- **要検証（Phase 2着手前）**：mp4と字幕は**取得元が別**（OneDrive/SharePoint の Drive と `onlineMeetings/transcripts`）。
  **会議と録画ファイルの対応付け**（どの録画がどの会議か）、**トランスクリプトの取得可否・必要権限**を実機で確認する。

---

## 5. 認証・認可（フロー確定後に決まる）

| | Phase 1（手動SSH） | Phase 2（SharePoint/Graph） |
|---|---|---|
| 認証の主体 | SSH鍵＋固定IP許可 | M365アプリ登録（VPSの資格情報） |
| 利用者の操作 | 各自SSHで実行 | 何もしない（録画するだけ） |
| 誰が設定 | 管理者（configuration.nix） | **M365管理者**（アプリ登録・権限付与） |
| 承認 | 許可メンバー/IPの合意 | **管理者作業＋上長承認**（外部連携・組織データアクセス） |
| アクセス制御 | 鍵とIPを持つ人 | 管理者が付与したアプリ権限・対象サイト/ユーザー・Teamsアクセスポリシー |

---

## 6. Cybozuの扱い（録画リンク）

議事録に貼る「録画リンク」は、全社が閲覧できる **Cybozu** のURLを使っている（設計書 §3）。
SharePointの録画は権限的に全社閲覧できないことが多い。

- **(あ) Cybozuは残す**：全社閲覧リンクはCybozu、**処理の入力だけSharePointから自動取得**。
  → 手動は「Cybozuへ1回アップロード」だけに減る。
- **(い) SharePointに寄せる**：全社が閲覧できるなら録画リンクもSharePointにしてCybozu廃止。
  → 全社閲覧の可否確認が必要。

---

## 7. 未決事項（上長・管理者に確認）

1. **Graph API連携を進めてよいか**（＝M365管理者によるアプリ登録・権限付与＋上長承認が必要）。
   これがPhase 2の前提。理由：組織データ（会議録画）へアプリが恒久アクセスする仕組みの新設。
   - あわせて**取得経路・権限の方針**：推奨は **(C) Teams会議API**（録画/字幕を**固定主催者1人に限定**）。
     次点で (D) 選択ファイル権限 / (B) SharePoint専用サイト。(A) 広い `Files.Read.All` は暫定・検証用。**C→D→B→A の順**で諮る（詳細は §4・§8.2）。
2. **録画リンクはCybozuのままか、SharePointに寄せるか**（全社閲覧の可否）。
3. **Phase 1のSSHアクセス許可（つなぎ）**：許可するメンバーと各自の固定グローバルIP。
   ※固定IPが無い人は「運用者に録画を渡す」でカバー。

---

## 8. Phase 2 技術検証・実装項目（詳細）

> Graph API の仕様・権限は変わりうるため、着手前に**最新のMicrosoft公式ドキュメントと実機**で確認する（本節は現時点の一般知識に基づく計画）。

### 8.1 認証（Entra ID アプリ登録）
- **Entra ID（Azure AD）にアプリを登録**し、**アプリケーション権限（app-only／無人実行）**で使う。
- 資格情報は**クライアントシークレット or 証明書**（証明書推奨）。VPSに保管（環境変数/鍵管理）。
- **管理者による「管理者の同意（admin consent）」が必須**。＝一般ユーザーだけでは完結しない。

### 8.2 必要な権限（狭い順に検証）
| 経路 | 権限（application） | 絞り込み | 備考 |
|---|---|---|---|
| **(C) 録画API** | `OnlineMeetingRecording.Read.All` | **アプリケーションアクセスポリシーで主催者1人に限定** | metadataに `contentCorrelationId`。録画content取得は従量課金・未期限会議のみ |
| **(C) 字幕API** | `OnlineMeetingTranscript.Read.All` | **同上（主催者1人）** | `text/vtt` 取得。話者属性オフだと403→話者なし形式へ |
| (D) 選択ファイル | `Files.SelectedOperations.Selected` | 選択したファイル/OneDriveのみ | Recordings列挙・delta・content取得に使えるか要検証 |
| (B) SharePointサイト | `Sites.Selected` | 指定サイトのみ | 録画を専用サイトへ移す運用が前提 |
| (A) OneDrive全体（暫定） | `Files.Read.All`（読取のみ） | 絞り込み不可（全社読める） | 実装最簡だが権限広い。本番主案にしない |

→ **`OnlineMeetingRecording/Transcript` はアプリケーションアクセスポリシーで固定主催者1人に絞れる**ため、権限の広さ・対応付けの両面で有利。まず **(C)** を検証する。個人OneDriveを狭く読む `Files.SelectedOperations.Selected` も候補（delta可否は要検証）。

### 8.3 取得の2系統と「対応付け」課題（最大の検証点）
- **録画(mp4)**：Drive（組織者のOneDrive or チームのSharePointサイト）の `Recordings` フォルダから。**どのDrive/サイトか特定**が要る。
- **字幕(vtt)**：`/users/{組織者}/onlineMeetings/{meetingId}/transcripts/{id}/content`（vtt形式）で取得。
- **課題**：**録画 ↔ 会議(meetingId) ↔ トランスクリプト の対応付け**。**録画API/字幕API(C)なら metadata の `contentCorrelationId`/`meetingId` で堅く対応付けできる**。OneDriveファイル(A/D)から入ると `meetingId` が直接取れず、ファイル名・時刻・主催者でのフォールバック照合になり曖昧。→ **対応付けの観点でも (C) 経路が有利**。
- 固定主催者でも**残る課題**：同時刻/定例の複数録画の識別、録画完了前の未完成ファイル検知、字幕が未生成/遅延/期限切れ、共同主催者やチャネル会議など保存先の例外。**ここがPhase 2最大の検証ポイント**。

### 8.4 トリガ（起点）
- **ポーリング（推奨）**：新規録画を定期検知。**インバウンド公開不要**（VPSからの外向き通信のみ）。月5回ならこれで十分。
  - (C)経路：録画API/字幕APIを定期的に一覧して差分を取る。
  - (A/B/D)経路：Drive の **delta クエリ**（`/drive/root/delta` 等）。※delta は「変更履歴」でなく「最新状態」を返し同じitemが複数回出るので、**パスでなく item-id で追跡**する。Recordingsフォルダ配下の delta・選択ファイル権限での delta 可否は要検証。
- Webhook（Graph変更通知）：即時だが **443の受け口＋検証応答＋購読の更新**が要る。今回は不要。※OneDrive for Business の driveItem 通知は購読対象に制限があるため、採用時は要検証。

### 8.5 実機で確認すべきこと（チェックリスト）
- □ Teams録画の**実際の保存先**（組織者のOneDrive か チームのSharePoint か）
- □ **トランスクリプトがGraphで取得できるか**（会議の種類・保持設定次第で存在しないことがある）
- □ `OnlineMeetingTranscript.Read.All` ＋ **アプリケーションアクセスポリシー**の要否
- □ **録画↔会議の対応付け**が機械的に可能か（メタデータ／命名規則）
- □ 録画フォルダだけに権限を絞る **`Sites.Selected`** が使えるか
- □ **アプリ登録・権限付与・管理者同意ができる管理者は誰か**

### 8.6 リスク・折衷案・設定メモ
- **アプリケーションアクセスポリシー**は `New-CsApplicationAccessPolicy`（作成）＋`Grant-CsApplicationAccessPolicy`（対象ユーザーに割当）で設定。**反映に最大30分**かかる。固定主催者1人にだけ割り当てれば、アプリはその1人の会議のみアクセス可。
- 会議トランスクリプトAPIは**権限・前提が厳しめ**で、取得できないケースがある。依存要因の例：
  - Teams管理センター側で **Graph transcript access / 話者属性（speaker attribution）** を無効化していると **403 や属性欠落**になる。
  - **会議の有効期限切れ・ライブイベント非対応**など、会議種別・保持設定に依存する。
  → その場合は **「録画の取得＋起動の自動化だけ先行し、字幕は当面手動DLのまま（または話者属性なし形式）」** という折衷が可能（段階導入）。
- つまりPhase 2は「**録画自動化（比較的容易）**」と「**字幕自動化（要件が重い）**」に分けて考えられる。

---

## 9. 次アクション

- 上長へ「**起動方式（Phase 1手動 → Phase 2 SharePoint/Graph）と、それに伴う認証方針**」を相談。
  SSH許可の可否だけでなく、**将来はM365に認証を寄せる方向でよいか**をセットで確認する。
- Graph連携がOKなら、§8のチェックリストで**実機検証**。**まず (C) Teams会議API**（録画API＋字幕APIを**アプリケーションアクセスポリシーで固定主催者1人に限定**、`contentCorrelationId` で対応付け）を検証し、通れば権限も対応付けも最小・最良。
- **段階案**：まず「録画自動取得＋起動自動化」→ 次に「字幕自動取得」（要件が重いため後）。
- 当面はPhase 1（手動SSH＋CLI）で運用継続。設計書 §6/§7 は本整理に合わせて改訂予定。

---

## 10. 実装方針（Phase 2・承認後に着手）

### 10.1 基本方針
既存の**処理コア（src/ 一式）は無改修**。「取り込み係」を1つ足し、録画・字幕を `inbox/` に置いて
既存の draft を呼ぶだけ。ツール依存は**フォルダ内で自己完結**（`msal` も flake に追加・グローバル非依存）。

### 10.2 追加モジュール
| ファイル | 役割 |
|---|---|
| `src/graph.py`（新規） | MSALでアプリ認証(app-only)＋録画/字幕の一覧・取得・ダウンロード |
| `src/ingest.py`（新規） | 新録画検知→取得→`inbox/`配置→draft起動→処理済み記録（冪等） |
| `src/config.py`（追記） | `GRAPH_TENANT_ID`/`GRAPH_CLIENT_ID`/`GRAPH_CLIENT_SECRET`(or 証明書)/`ORGANIZER_ID` を `minutes.env` から |
| `flake.nix`（追記） | 認証ライブラリ `msal` を devshell に追加 |

### 10.3 認証（app-only／無人）
MSAL の `ConfidentialClientApplication` → `acquire_token_for_client(scopes=["https://graph.microsoft.com/.default"])`。人のログイン不要。
- **本番は証明書認証を推奨**（クライアントシークレットはローテーション/漏えい耐性が劣る＝検証用に留める）。
- トークンはMSALがキャッシュ。プロセス都度終了ならメモリキャッシュは消えるが、15分間隔なら都度取得で問題なし。必要なら `SerializableTokenCache` を `state/msal_cache.bin`（600）に保存。

### 10.4 取得（主催者単位・delta同期・要実機検証）
- **主催者単位の `getAllRecordings` / `getAllTranscripts`** を使う（個別 `/onlineMeetings/{id}/recordings` は meetingId が先に必要で不向き）。
- **初回は日付範囲/full sync、以後は deltaLink を保存して差分同期**（15分ごとに全走査しない）。
- **対応付け**：主キーは **`recording.id`**。照合に `contentCorrelationId` を使い、`meetingId`/`transcript.id`/`createdDateTime` も保存。`contentCorrelationId` 欠落時は `meetingId + recording.id` にフォールバック。
- 字幕は `?$format=text/vtt`（→ 既存 `vtt.py` が解析）。**話者名の有無はアプリ登録後に実機で確定**。

### 10.5 起動連携（出力を会議ごとに分ける）
- ingest は **`python -m src.main draft <mp4> --vtt <vtt> --out out/minutes_<date>_<recid>.md` を直接 subprocess で呼ぶ**（`run.sh` は対話・inbox単一ファイル前提なので自動取込には使わない）。
- **`--out` を会議ごとに一意化**：既定の `out/minutes.md` のままだと、タイマーで拾った次の下書きが**人のレビュー前に上書き**してしまう（flockは同時実行しか防げない）。
- **録画リンク**：`--fid` は **Cybozuの数字fFID専用**なので **Graphの録画IDは渡さない**。録画リンクは SharePoint/Teams の録画URLを `--link` に渡すか、既定フォルダのまま（方針は別途決定）。
- **処理コアは無改修**。register は人のまま。

### 10.6 定期実行
- `systemd --user` タイマーで 15分ごとに ingest（インバウンド公開不要）。ログインしていない時間も動かすため **`loginctl enable-linger <user>`** が必要。
- service に `WorkingDirectory=%h/projects/minutes`・`EnvironmentFile=%h/.config/minutes.env`・`ExecStart=nix develop -c python -m src.ingest` 相当、timer に `Persistent=true`。多重起動は systemd ＋ アプリ側 flock で抑止。

### 10.7 状態管理・冪等性（段階状態）
- **段階状態**：`discovered → downloading → downloaded → transcript_pending → drafting → drafted / failed`。各録画に `attempts`・`last_error_code`・`next_retry_at`・保存ファイルパス・`output_md` を持たせる。書き込みは**一時ファイル→atomic rename**。
- **主キーは `recording.id`**（成功記録だけの `processed.json` にしない＝部分失敗を区別できるように）。
- **同時実行**：既存の flock。
- **秘密**：アプリの鍵は `minutes.env`（600・ホーム内・証明書推奨）。既存の読込/権限検査を流用。

### 10.8 失敗・遅延の扱い（SLO）
- **一時的（404 / 字幕未生成 / 処理中）**：一定期間リトライ。
- **恒久的（例 `403 GraphAccessToTranscriptsDisabled`）**：即・恒久失敗として通知（無駄なリトライをしない）。
- **字幕が来ない場合のSLO例**：録画作成から24時間 字幕なし → **録画のみで draft**（登壇者は「要確認」）。
- **スロットリング**：429 は **`Retry-After` を最優先**（指数バックオフはその補助）。
- **ログに出さない**：Authorization ヘッダ・署名付きURL・`recordingContentUrl`・トークン応答。ログは request-id・HTTP status・Graph `innerError.code`・録画ID短縮 のみ。

### 10.9 前提・制約（実機で必ず確認）
- **会議種別の制約**：録画/字幕APIは **live events 非対応**、`getAllRecordings/getAllTranscripts` は **channel meetings 非対応**。固定主催者でも**チャネル会議に変わる運用があるなら別経路が必要**。
- **アプリケーションアクセスポリシーは必須**：録画・字幕の application permission は、管理者がポリシー作成→対象主催者へ付与（反映〜30分）。
- 字幕は話者属性オフだと 403 / 話者名なし（§8.6）。

### 10.10 着手順（作りながら検証）
1. `graph.py`：認証OK → `getAllRecordings` で主催者の録画が一覧できる（最小疎通）
2. 字幕取得：**話者名つき `text/vtt`** が取れるか（最大の確認点）。ダメなら折衷（録画だけ自動／話者なし）
3. `ingest.py`：`--out` 一意化で draft起動 → 段階状態を1会議で通す
4. タイマー＋linger で自動化 → 手動運用と並行で安定確認 → 主運用へ
