# 議事録自動化 残タスク

最終更新：2026-07-13　／　凡例：[あなた]=中村さん操作 ・ [私]=Claude実施

## ✅ 完了済み（主なもの）
- Teams字幕(.vtt)×whisper 併用（実装・検証・Codex3回）
- 話者名の半角スペース正規化＋`speaker_aliases.txt`
- `draft.ps1`（③のAI草案生成API化）実装＋Codex2回＋UTF-8応答バグ修正＋実データ検証＋トークンUser環境変数フォールバック
- さくらAI Engine 連携：トークン設定・**Chat疎通**・**Whisper疎通(実データ・高品質)**・草案LLM＝`gpt-oss-120b`確定
- PC手順書：方法①(`-To draft`自動)／方法②(ChatGPT手動)の両対応、A-3bトークン設定
- 配布ZIP最新化：`議事録自動化_v2_20260713.zip`（12ファイル・draft.ps1/speaker_aliases含む）
- 実機E2E：`-To draft`で 字幕照合取込→ローカル文字起こし→さくらAI下書き→`minutes.md`（文字化けなし・登壇者自動）成功
- サーバー化設計書 **v0.7**（入口=B2 / 文字起こし=さくらWhisper / 出口=承認後Backlog / 実装=Python、Codex複数ラウンド反映）
- **VPS Step2 雛形**を `vps/minutes/` に作成＋ローカル検証（Whisper API・vtt.py移植・py_compile）

---

## 方針（2026-07-14 決定）
**運用主体はVPS版CLIに一本化**。PC版は当面残すが主運用ではない（二重運用のズレ回避）。

## ⬜ 残タスク

### 1. PC版（縮小方向・主運用から外す）
- [ ] [あなた] （任意）配布ZIPは現状維持で可。VPS一本化に伴い更新優先度は下げる
- [判断済] PC版の`-To register`は主運用にしない（VPS版へ集約）

### 2. VPSサーバー化（Step2✅ → Step3）
- [x] [あなた] Step2雛形をVPSへ転送＋`nix develop`＋実走（2026-07-13）
- [x] [あなた+私] VPSで `draft` を実走：46分mp3を無音2分割→Whisper→字幕6名→gpt-oss-120bで下書き→out/minutes.md（文字化けなし・登壇者/日付正しい）成功
- [x] [私] 会議番号のBacklog自動採番を移植（`Get-BacklogMeetingNo` → `backlog.meeting_no`。draftで`--no`省略時に自動）
- [x] [私] `register` コマンド（承認後のBacklog Document API登録）＋重複チェック＋番号衝突検出＋APIキー秘匿＋失敗時fail-closed（Codexレビュー反映）
- [x] [あなた+私] VPS実機で `register --dry-run` 検証（2026-07-13）：Backlog認証OK・ツリー取得OK・既存議事録を`[DUPLICATE]`で正しく停止。※実POST（新規作成）は削除不可アカウントのため**次の本物の新規会議で初回登録**する
- [ ] [あなた] 次回会議で `register`（本POST）を初通し。Backlog APIキーは**コロン無し**（さくらトークンと混同しない）
- [x] [私] トークン毎回入力の解消：`~/.config/minutes.env`（chmod 600・リポジトリ外）から自動読込（`config.py _load_env_file`。既存env優先・所有者/権限チェック・キー名検証、Codexレビュー反映）。手順書0-1に反映
- [ ] [あなた] VPSに `~/.config/minutes.env` を作成（SAKURA_AI_TOKEN／BACKLOG_API_KEY）。以降トークン入力不要
- [ ] [私] ジョブ/状態管理（`jobstore.py`）・クォータ台帳（`quota.py`）… 主にStep3向け
- [ ] [私] チャンク境界のオーバーラップ＆重複除去、LLMトークン上限対策
- [ ] [私] 入口 `server.py`（FastAPI・トークン認証・非同期ジョブ）… Step3
- [ ] [あなた] Caddy(443)をシステム側に追加（グローバル唯一の例外）… Step3・要管理者OK
- [ ] [あなた+私] 証明書方式の決定（ドメイン+Let's Encrypt推奨 / 社内CA / 自己署名）… Step3公開前
- [ ] [あなた] 利用者トークン発行運用（発行・失効・ハッシュ保存）… Step3

### 3. 合意・ガバナンス
- [ ] [あなた] Caddy(443)例外の管理者OK（＝Step3の前提）
- [ ] [あなた] 利用場所の確定（オフィス限定か在宅もか → IP制限の要否）
- [ ] [あなた] Backlog Webhook＝出口 の認識合わせ／未チェック草案を共有領域に出さない方針の合意

### 4. 任意・将来
- [ ] Microsoft Graph API での録画/字幕 自動取り込み（Cybozu手動DLを無くす。別プロジェクト・要権限/承認）
- [ ] B1（Webアップロード画面）… 同じ443バックエンドに追加可
- [ ] 未コミット変更のコミット（区切りとして。プッシュは指示があれば）

---

## 依存・おすすめ順
```
① [済] トークン→Chat/Whisper疎通→LLM選定
② PC版の締め：ZIPアップロード・登録判断        （すぐ終わる）
③ VPS Step2 実走：scp→nix develop→draft動作確認 （核は検証済み、実環境で通す）
④ VPS Step3：register/jobstore/quota → FastAPI入口 →(管理者OK)Caddy公開
```

## 次の一手（推奨）
③「**VPS Step2をVPSで実走**」。核（Whisper・vtt・chat）は検証済みなので、実環境で通せば残りの穴（無音分割・nix）を潰せます。
