# 議事録自動化 残タスク

最終更新：2026-07-14　／　凡例：[あなた]=中村さん操作 ・ [私]=Claude実施

## ✅ 完了済み（主なもの）
- Teams字幕(.vtt)×whisper 併用（実装・検証・Codex3回）
- 話者名の半角スペース正規化＋`speaker_aliases.txt`
- `draft.ps1`（③のAI草案生成API化）実装＋Codex2回＋UTF-8応答バグ修正＋実データ検証＋トークンUser環境変数フォールバック
- さくらAI Engine 連携：トークン設定・**Chat疎通**・**Whisper疎通(実データ・高品質)**・草案LLM＝`gpt-oss-120b`確定
- PC手順書：方法①(`-To draft`自動)／方法②(ChatGPT手動)の両対応、A-3bトークン設定
- 実機E2E：`-To draft`で 字幕照合取込→ローカル文字起こし→さくらAI下書き→`minutes.md`成功
- サーバー化設計書 **v0.7**（入口=B2 / 文字起こし=さくらWhisper / 出口=承認後Backlog / 実装=Python）
- **VPS Step2 雛形**を `vps/minutes/` に作成＋ローカル/実機検証
- **運用のVPS版CLI一本化**（PC版は当面残すが主運用から外す）
- **`~/.config/minutes.env` をVPSに設定**（`config.py`が自動読込。`SAKURA/BACKLOG set: True`確認済み）
- **手順書を運用手順に再編**（クイックリファレンス/初回A/配置B/コマンドC/トラブルシュート）
- **登壇者を発言最多の1人に絞る**（`vtt.py`/`main.py`/テンプレ。他の話者は本文に残す。2026-07-14）
- **入口を「手動CLI＋薄いラッパー`run.sh`」に確定**（`inbox/`の録画+字幕を自動検出しdraft実行。2026-07-14）
- **不正トークン起動時ガード**（`config.token_problem`。プレースホルダ`＜…＞`/非ASCII/空白を明確なエラーで弾く。2026-07-14）
- **最新版をVPSへ配置＋③実データ検証成功**（2026-07-14）：run.sh自動検出→Whisper→登壇者1人化(6人中 渡邉 麗太)→下書き生成まで完走。会議番号自動採番も復活（`minutes.env`のプレースホルダ修正後）
- **★register 本POST初通し(④)完了**（2026-07-14）：第2回議事録をBacklogに新規登録成功（docId 019f5e8d…）。録画→Whisper→用語集→下書き→整形→人チェック→登録が実運用として一周。
- **議事録の体裁を既存(第1回)に統一**（2026-07-14）：テンプレを第1回型に（`#`タイトル/`---`無し/太字禁止/粒度強化）。加えてコード側で確定整形 `prompt.normalize_md`（行末空白除去・見出し後の空行・太字`**`除去、フェンス内は保護）。
- **用語集で文字起こし誤変換を汎用訂正**（2026-07-14）：`glossary.txt`＋`prompt.load_glossary`。正しい表記へLLMが訂正（例 MPフォー→MP4、Cloud Code→Claude Code）。会議で出た用語を追記して運用。
- **入力の自動退避**（2026-07-14）：draft成功後 `inbox/`→`processed/<日時>_<名>/` へ移動（毎回の手動削除が不要）。

---

## 方針（確定）
- **運用主体はVPS版CLIに一本化**。PC版は当面残すが主運用ではない（二重運用のズレ回避）。
- **入口（トリガ）＝手動CLI＋`run.sh`ラッパー**（2026-07-14決定）。録画到着を起点とし、
  DL→scp後に人が `./run.sh`→人チェック→`register`。Backlog Webhook入口(A)は当面保留、
  登録後通知(B)は任意の将来課題。※月5回・全員エンジニア・グローバル依存追加NG に最適。
- **Backlog Webhook＝出口 の認識**：VPSがBacklog APIで議事録を「登録」する(=実装済み)。
  Backlog側のWebhook通知(イベント→外部)は設定次第の別機能で、VPS側実装は不要。→認識合わせ済み。

## ⬜ 残タスク

### 1. 本命（これで運用が回る）
- [x] [あなた] 最新の `vps/minutes/`（run.sh＋トークンガード）をVPSへ配置（2026-07-14完了）
- [x] [あなた+私] **register 本POST初通し(④)完了**（2026-07-14）。第2回をBacklog新規登録成功。
      → 実運用フローが一周。以降は毎回このフロー（`./run.sh`→人チェック→`register --dry-run`→`register`）で回す。
- [ ] [運用] 毎会議：新しい`glossary.txt`用語の追記／登録後のBacklog目視確認（削除不可のため`--dry-run`必須）

### 2. VPS版（Step2✅ → 完成度上げ / Step3は将来）
- [x] [あなた] Step2雛形をVPSへ転送＋`nix develop`＋実走（2026-07-13）
- [x] [あなた+私] VPSで `draft` を実走：46分mp3→Whisper→字幕→gpt-oss-120bで下書き成功
- [x] [私] 会議番号のBacklog自動採番を移植（`backlog.meeting_no`。draftで`--no`省略時に自動）
- [x] [私] `register` コマンド＋重複チェック＋番号衝突検出＋APIキー秘匿＋fail-closed
- [x] [あなた+私] VPS実機で `register --dry-run` 検証（認証OK・ツリー取得OK・`[DUPLICATE]`停止）
- [x] [私] トークン自動読込（`~/.config/minutes.env`・所有者/権限チェック・キー名検証）
- [x] [私] 登壇者を発言最多の1人に絞る（2026-07-14）
- [x] [私] 入口ラッパー `run.sh` 追加＋`.gitattributes`でLF固定（2026-07-14）
- [x] [私] 不正トークン起動時ガード（`config.token_problem`＋`main()`で検査。2026-07-14）
- [x] [あなた+私] 実データE2E検証（③）：run.sh→Whisper→登壇者1人化→下書き 成功（2026-07-14）
- [ ] [私] チャンク境界のオーバーラップ＆重複除去、LLMトークン上限対策（任意・品質向上）
- [ ] [私] ジョブ/状態管理(`jobstore.py`)・クォータ台帳(`quota.py`)… Step3向け（当面不要）
- [ ] [私] 入口 `server.py`（FastAPI）… Step3。※入口はBで確定のため当面不要（完全無人化を狙う段で再検討）
- [ ] [あなた] Caddy(443)＋証明書＋利用者トークン発行 … Step3・外部公開する場合のみ・要管理者OK

### 3. 合意・ガバナンス
- [x] Backlog Webhook＝出口 の認識合わせ（2026-07-14整理済み）
- [ ] [あなた] 利用場所の確定（オフィス限定か在宅も → IP制限の要否）※外部公開(Step3)する場合のみ
- [ ] [あなた] 未チェック草案を共有領域に出さない方針の周知（Backlog登録＝唯一の出口）

### 4. 任意・将来
- [ ] Microsoft Graph API での録画/字幕 自動取り込み（Cybozu手動DLを無くす。別プロジェクト・要権限/承認）
- [ ] B1（Webアップロード画面）… Step3で443バックエンドに追加可
- [ ] PC版 *.ps1 の整理（縮小方向・優先度低）

---

## 次の一手（推奨）
**本命(④)は2026-07-14に完了。VPS版は本番稼働に到達した。**
以降は毎会議、次のフローで回す（初回セットアップ済みの人は毎回これだけ）：
`inbox/へ録画+字幕をscp` →`./run.sh`（用語集訂正・整形込み）→`out/minutes.md`を人チェック
→`register --dry-run`→`register`→Backlog目視。
残るのは任意・将来課題（Step3の外部公開、Graph API自動取込、PC版整理）のみ。急ぎではない。
