# 議事録自動化 事実記録（2026-07-16 時点）

> **この文書には事実のみを記載する。** 判断の理由・推奨・見通しは書かない。
> それらは `議事録自動化_フロー整理.md`（一次情報）と `議事録自動化_サーバー化設計書.md` v0.8 にある。
> **「実測」と書いた項目は、実際に操作して結果を確認したもの。** それ以外は出典を併記する。

---

## 1. 前提となる事実

- 勉強会「システム設計 × AI駆動開発カリキュラム」は **渡邉麗太さんが主催**し、**Microsoft Teams で実施**している。
- **録画をもとに議事録を作成し、Backlog のドキュメントへ保存する方針**である。
- 議事録作成の処理（音声抽出 → 文字起こし → 話者判定 → 用語訂正 → 下書き生成 → 整形 → Backlog登録）は
  **実装済みで稼働中**。**2026-07-14 に第2回を Backlog へ登録し、実運用フローが一周した。**
- 処理は **VPS（NixOS・AI-OCR用）上**で動いている。実体は `vps/minutes/`。
- **VPS への接続には SSH鍵と固定グローバルIPの両方が必要**（port 2201・`aiocr`・鍵のみ）。
  → **現状、運用できるのは中村のみ。**
- **録画(mp4)** は Teams 会議時に **OneDrive/SharePoint へ自動保存**される。
- **字幕(vtt)** は mp4 と同じフォルダには存在しない。Teams のトランスクリプトとして会議に紐づく。
- 録画した人が**手動で Cybozu にアップロード**している（全社閲覧用）。**Cybozu に公式のダウンロードAPIは無い。**
- **会議は週1回。**
- 文字起こしと下書き生成は **さくらのAI Engine**（Whisper / `gpt-oss-120b`）。**小見山さんの承認済み。国内完結。**

---

## 2. 上長・管理者に関する事実

| 日付 | 発言者 | 内容 |
|---|---|---|
| — | 小見山さん | **「Application permissions を想定している場合、導入は難しいと考えてください」**。理由：範囲が個人ではなく**会社テナント**となるため |
| — | 小見山さん | **「まずはアプリ全体でフローを整理し、その上で認証認可は考えるとよいです」** |
| — | 小見山さん | **「M365の管理者は今井さんです」** |
| — | 小見山さん | 「麗太さんのOneDrive for Business を使うとなると、当然麗太さんにアプリを導入してもらう必要があります。**その前に自身のアカウントを用いて十分検証は行うようにしてください**」 |
| — | 小見山さん | **「指定の場所にファイルを置いてAPI実行したら記事が生成されるといっただけでも十分です」**（完全自動化は必須でない） |
| 2026-07-15 | 小見山さん | **「一旦その形で進めていただいて結構です」** ＝ 方針の暫定承認。**検証用アプリを中村のアカウントで1つ登録すること**も許可 |

**中村が小見山さんへ説明した内容（承認時の文面）**

- 「認証は **Delegated permissions のみ**で行います。Application permissions は使いません」
- 「アクセス範囲は **`Sites.Selected`** で、**この専用サイト1つに限定**する想定です」
- 「検証用のアプリを私のアカウントで1つ登録してよいでしょうか。**単一テナント・管理者同意が不要な委任権限のみ・
  私がアクセスできる範囲の動作確認のみ・検証後は削除**、という条件で考えています」
- **「Backlog登録は当面、従来どおり私が実行します」**
  → **⚠️ 2026-07-16 に「担当者が登録まで完結する」へ方針変更。この点は未報告。**

---

## 3. 確定値（すべて実測で取得）

| 項目 | 値 | 取得元・日付 |
|---|---|---|
| テナントID | `85c9340e-a7a2-48cb-97cf-ca56a6e44875` | アクセストークンの `iss`（2026-07-16） |
| テナントドメイン | `tcsbunkyou.onmicrosoft.com` | authority として動作確認（2026-07-16） |
| 中村の oid | `63bb0835-d67e-44bf-9d9c-6308af187864` | トークンの `oid`（2026-07-16） |
| **VPSのグローバルIP** | **`133.125.62.159`** | トークンの `ipaddr`＝Entraから見えている実際の値（2026-07-16） |
| 専用サイト | `https://tcsbunkyou.sharepoint.com/sites/minutes`（名称「議事録作成」） | 2026-07-15 中村が新設 |
| siteId | `tcsbunkyou.sharepoint.com,92289f8d-ebbe-4ad3-b6a4-c041b99268e2,8046e19d-e6f7-49d5-ac57-d07e3b80b642` | Graph（2026-07-15） |
| driveId | `b!jZ8okr7r00q2pMBBuZJo4p3hRoD35tVJrFfQfjuAtkKjRmf4Zn5fSrSvXE8QcLCP` | `_api/v2.0/drive`（2026-07-16）※Graphの値との照合は未 |
| **本番アプリ（clientId）** | **`3c741d2b-6ee5-4ef1-adf5-9f096de64d79`（`minutes-automation`）** | 2026-07-16 登録 |
| 旧・検証用アプリ | `81214b2e-d6e6-4afc-a371-06c033b56077`（`minutes-automation-test`） | 2026-07-15 登録・**後始末対象** |

### 専用サイトの属性（2026-07-16 実測・SharePoint REST）

| 項目 | 値 |
|---|---|
| `Created` | **2026-07-15T07:32:36** |
| `Language` | **1041（日本語）**（※作成後に変更不可） |
| `WebTemplate` | **`SITEPAGEPUBLISHING`（コミュニケーションサイト）** |
| `owner` | **中村流星（`63bb0835-…`）** |
| メンバー | **なし**（`associatedmembergroup` が空） |
| `quota.total` | **27,487,790,694,400 バイト（25 TiB）** |
| `quota.used` | 1,447,590 バイト（1.4 MB） |
| フォルダ | `error/` `inbox/` `out/` `processed/` ＋ **`approved/` `done/`（2026-07-16 にエクスプローラーで作成）** ＝ 設計どおり6つ |

> **注**：ライブラリの `createdDateTime` は **2026-07-07**、`createdBy` は **「システム アカウント」**。
> サイトの `Created`（7/15）より古い。SharePoint Online は作り置きしたサイトをプールから払い出すため。

### アプリの設定（2026-07-16 実測）

| 項目 | `minutes-automation`（本番） | `minutes-automation-test`（旧） |
|---|---|---|
| clientId | `3c741d2b-6ee5-4ef1-adf5-9f096de64d79` | `81214b2e-d6e6-4afc-a371-06c033b56077` |
| 権限 | **`Sites.Selected` ＋ `offline_access`（委任）** | `Files.ReadWrite` ＋ `offline_access`（委任） |
| アカウントの種類 | 単一テナント | 単一テナント |
| リダイレクトURI | なし | なし |
| パブリッククライアントフロー | 許可 | 許可 |
| 管理者同意 | **不要だった** | **不要だった** |

---

## 4. 実測結果（日付・条件つき）

### 4.1 認証（2026-07-15〜16・VPS上）

| 項目 | 結果 | 日付 |
|---|---|---|
| 一般ユーザーによるアプリ登録 | **可能** | 07-15 |
| `Files.ReadWrite` / `offline_access` の管理者同意 | **不要** | 07-15 |
| **`Sites.Selected`（委任）の管理者同意** | **不要**（Entraの「管理者の同意が必要」列＝いいえ） | 07-16 |
| device code flow でのサインイン | **成功** | 07-15, 07-16 |
| `refresh_token` の発行 | **成功** | 07-15, 07-16 |
| `grant_type=refresh_token` での更新（人の介在なし） | **成功** | 07-15, 07-16 |
| **15時間11分の無操作後の refresh** | **成功**（保存 07-15 16:44 JST → 実行 07-16 07:55 JST） | 07-16 |
| **同一 `refresh_token` の3回連続使用** | **3回とも成功** | 07-16 |
| アクセストークン寿命 | **4320秒 / 4006秒 / 4528秒**（3回とも異なる） | 07-15, 07-16 |
| トークンの `amr` | **`["pwd","mfa"]`** | 07-16 |
| トークンの `scope`（新アプリ） | **`profile openid email https://graph.microsoft.com/Sites.Selected`**（`Files.*` なし） | 07-16 |
| `offline_access` が `scope` に含まれるか | **含まれない**（3回とも） | 07-15, 07-16 |
| `device_code` の再引き換え | **失敗**（`AADSTS54005: OAuth2 Authorization code was already redeemed`） | 07-16 |

### 4.2 Graph（`Files.ReadWrite` ＋ `/me/drive`・2026-07-15〜16）

| 項目 | 結果 |
|---|---|
| `GET /me/drive/root/children` | **HTTP 200** |
| `PUT /me/drive/root:/…/hello.md:/content` | **HTTP 201** |
| `GET …/content` | **HTTP 200・内容一致** |
| VPSからGraphへの疎通 | **成功**（IP制限に抵触せず） |

### 4.3 `Sites.Selected` の付与前ベースライン（2026-07-16・新アプリ）

**状態：① Entra での同意＝済／② サイトへの付与＝未／③ トークン取得＝済**

| 項目 | 結果 |
|---|---|
| `GET /v1.0/sites/{siteId}` | **HTTP 403 `accessDenied`** |
| `GET /v1.0/sites/{siteId}/drive/root/children` | **HTTP 403 `accessDenied`** |

### 4.4 `delta` の挙動（2026-07-16・`Files.ReadWrite` ＋ `/me/drive`）

**手順**：`?token=latest` → ファイル作成 → delta → 別フォルダへ移動 → delta → 削除 → delta

| 項目 | 結果 |
|---|---|
| `?token=latest` のレスポンス | **`"value":[]` ＋ `@odata.deltaLink`**（既存ファイルは返らない） |
| `deltaLink` は毎回変わるか | **変わる** |
| ファイル作成後の delta | **3件返る**（`root` / `minutes-test` / `delta1.txt`） |
| `PATCH` で親を変更（移動） | **HTTP 200。`id` は `01HJFHMPG3HH57ANKIIVEZHNL7JHZJP5UZ` のまま不変** |
| 移動後の delta | **4件返る**（`root` / `minutes-test2` / `delta1.txt` / `keep.txt`）。**`deleted` は0件** |
| **移動元フォルダ（`minutes-test`）** | **delta に返らなかった** |
| `DELETE` 後の delta | **`id` ＋ `"deleted":{"state":"deleted"}` のみ。`name` は返らない** |
| **削除の親フォルダ（`minutes-test2`）** | **delta に返らなかった** |
| `root` の `size` | **1,993,789,171 バイト（約2GB）＝ドライブ全体** |
| `createdBy` / `lastModifiedBy` | **`application.id` が含まれる**（`81214b2e-…`） |
| その他のレスポンス項目 | `@microsoft.graph.hasMoreData` / `@microsoft.graph.syncStatus` |

### 4.5 OneDrive同期の挙動 ②（2026-07-16・専用サイト・ライブラリ全体を同期）

同期先：`C:\Users\r-nakamura\OneDrive - 株式会社ティー・シー・エス\議事録投入 - ドキュメント\`

| 項目 | 結果 |
|---|---|
| ライブラリ全体の「OneDriveへのショートカットの追加」 | **成功。`inbox` `out` `processed` `error` が全部エクスプローラーに出る** |
| エクスプローラーでフォルダを新規作成（`approved` `done`） | **成功。SharePoint側（ブラウザ）でも見えた** |
| **`.mp4` を `inbox\` へドラッグ** | **成功（同期クライアント経由の拡張子ブロックは無い）** |
| **200MB の mp4 の同期** | **成功** |

> **注**：ショートカットのフォルダ名は **`議事録投入`**（旧サイト名）。**現在のサイト名は `議事録作成`**。
> サイト名を変更してもショートカット名は追随しない。

> **注**：**200MB は実測で通った**。1時間の録画は約400MB（Teams公式）なので倍の余地はあるが、
> 200MB が通って400MBで落ちる理由は考えにくい。**同期クライアント側のサイズは論点にならない。**
> **⚠️ これは同期クライアント（人が置く）の話。Graph API（VPSが書き戻す）の 250MB 超は別で、未検証。**

**会社名（正式表記）：`株式会社ティー・シー・エス`**

### 4.6 OneDrive同期の挙動 ①（2026-07-15・専用サイト・SharePoint REST の `UniqueId` で追跡）

| 項目 | 結果 |
|---|---|
| 「OneDriveへのショートカットの追加」 | エクスプローラーに出現 |
| VS Code で `.md` を編集・保存 | 同期される。**`UniqueId` 不変** |
| エクスプローラーで `out/` → `processed/` へドラッグ移動 | **`UniqueId` 不変** |
| 日本語ファイル名 | 問題なし |

```
終始一貫: 50dd006d-a6b8-4dde-a7de-32939dc0dffa
```

**サンプル1件。**

### 4.7 権限の確認（2026-07-16）

| 項目 | 結果 |
|---|---|
| SharePoint 管理センター（`https://tcsbunkyou-admin.sharepoint.com`） | **中村はアクセス不可** |
| Entra のロール一覧 | **中村は参照可能** |
| **SharePoint 管理者ロールの割り当て** | **該当者なし** |
| **グローバル管理者ロールの割り当て** | **今井さんが該当** |
| Entra のサインイン ログ | **中村は参照不可** |
| Entra の条件付きアクセスのポリシー | **中村は参照不可** |

---

## 5. 決定事項

| 日付 | 決定 |
|---|---|
| 2026-07-15 | **Application permissions（app-only）は使わない**（小見山さんの判断） |
| 2026-07-15 | **入口＝専用SharePointサイト ＋ Graph（delegated・`Sites.Selected`）＋ VPSからの15分ポーリング** |
| 2026-07-15 | **案A（渡邉さんのOneDrive監視＋字幕もGraph）を採らない** |
| 2026-07-15 | **transcript は Graph では取得しない。** 人がTeamsからDLして投入する |
| 2026-07-15 | **`Lists.SelectedOperations.Selected` は採らない**（付与APIが beta のみ＝本番非サポート） |
| 2026-07-15 | **`Sites.ReadWrite.All` は使わない** |
| 2026-07-15 | **承認＝`approved/` フォルダへ移動**（`y` 押下の代わり） |
| 2026-07-16 | **v0.7 の入口 B2（PC→VPSのHTTPS/443・Caddy・独自トークン）は不採用** |
| 2026-07-16 | **リネーム＋同意削除ではなく、新アプリを登録**（`Sites.Selected` ＋ `offline_access` のみ） |
| 2026-07-16 | **専用サイトは作り直さない**（言語が日本語・コミュニケーションサイト・所有者が中村） |
| 2026-07-16 | **`approved/` のフォルダ単位の権限分離は採用しない。** 代わりに**「サイトのメンバー＝Backlogに登録してよい人だけ」**を前提とする |
| 2026-07-16 | **「うっかり移動＝登録」への追加対策は積まない** |
| 2026-07-16 | **担当者が「置く → チェック → 登録」まで完結する**（中村を経由しない）※**小見山さんへ未報告** |
| 2026-07-16 | **下書き完成・登録完了・失敗の通知は、勉強会チームの専用Teamsチャネルへ Graph で投稿する。** 追加権限 `ChannelMessage.Send`（delegated）が必要。※**この権限追加も小見山さんへ未報告** |

---

## 6. 未確認の事項

### 6.1 今井さんの作業が前提

- **`Sites.Selected` で 一覧 / ダウンロード / 書き戻し が通るか**
  - 公式の権限表に `Sites.Selected` の明示なし
  - **delegated × `Sites.Selected` × `delta` の実績報告はゼロ**
  - `/lists/` 配下は 403 か空、という未解決の報告あり（2024-04・MSは未回答）／drive経路では children 列挙が動いた報告あり（2024-04）
- **意図した範囲に限定されるか（ネガティブテスト）**
  - `Files.SelectedOperations.Selected` で権限を付けていないファイルまでDLできた報告あり。
    **MSスタッフとMVPの双方が「想定外」と認めたまま解決未確認**（2025-01）
- **拒否が 403 か「200＋空」か**
- **実録画（250MB超）の `createUploadSession`**
  - 2021年に Selected で「小さいファイルは通るが、当時の単純PUT上限(4MB)を超えると落ちる」バグの実績
- **Graph の item-id / driveId が専用サイトでも同じ挙動か**
- **`lastModifiedBy.application.id` が専用サイトでも入るか**
- **条件付きアクセスの「サインインの頻度」の長い設定（1日/7日/30日）**
- **SharePoint の非管理デバイスからのアクセス制限**
- **Token Protection**
- **保持ポリシー・保持ラベル（Purview）の適用**

### 6.2 その他

- **`ChannelMessage.Send`（Teams通知用）に管理者同意が要るか**（§10.8）
- **専用チャネルの `teamId` / `channelId`**
- **delegated ＋ このアプリで `POST /teams/{teamId}/channels/{channelId}/messages` が通るか**
- `pendingOperations` が立つか
- `quickXorHash` が返るか（公式に "if available"）
- `.md` をブラウザで直接編集できるか
- **パスワードレス方式でサインインできるか**（現在 `amr: ["pwd"]`）

---

## 7. 公式ドキュメントに基づく事実（実測ではない）

- **`driveItem: delta` の公式HTTP request一覧は全て `/root/delta` 止まり。** フォルダ単位の形式の記載なし
- ODfB では **`$filter` は公式に "not available"**、`$orderby` は `name` と `url` のみ
- 変更通知は「On OneDrive for Business, you can subscribe to **only the root folder**」と現行明記
- **children 列挙は「列挙中に書き込みがあると全件を返す保証がない。delta を使うことが唯一の方法」**と公式明記
- **「all Selected scopes now support delegated and application modes」**
- **「In the delegated scenario, both the application and user permissions are calculated and then intersected,
  which means that the application can never exceed the user's permissions.」**
- `Sites.Selected` の付与API `POST /v1.0/sites/{siteId}/permissions` は **v1.0 GA**。delegated は **2024-02-09 対応**
- 付与には **`Sites.FullControl.All`** が必要。**delegated では実行者が SharePoint 管理者以上のロールを持つ必要がある**
- `Lists.SelectedOperations.Selected` の付与API `POST /sites/{siteId}/lists/{listId}/permissions` は **beta のみ**。
  公式警告：「APIs under the `/beta` version ... **Use of these APIs in production applications is not supported.**」
- `OnlineMeetingTranscript.Read.All` は **delegated をサポート**。招待されていれば主催者以外も取得可
- **Teams管理センターに既定OFFのトグルが2つ**（Microsoft Graph access / Include speaker attribution）。
  公式に「the related tenant administrator controls **take effect at the end of July 2026**」と記載
- **`getAllTranscripts` / `getAllRecordings` は delegated 非サポート**（"Not supported." と明記）
- refresh token の既定「90日」は **`MaxInactiveTime`**。`MaxAgeSingleFactor` / `MaxAgeMultiFactor` は既定 **`Until-revoked`**
- **「Refresh tokens are bound to a combination of user and client, but aren't tied to a resource or tenant.」**
- 公式の保護要件は「access control **and encryption**, in relation to the **user-owner, or process identity**」
- **device code flow は public client 限定**。public client ＋ パスワード認証だと**本人のパスワード変更/SSPRで失効**する
- 単純 PUT の上限は **250MB**。超える場合は `createUploadSession`（各チャンク60MiB未満・320KiBの倍数・順次）
- ハッシュは ODfB では **`quickXorHash` のみ保証**。**`sha256Hash` は「This property isn't supported. Don't use.」**
- `id` は**移動・リネームで不変**（公式明記）
- 重複判定は **`(drive-id, item-id)` ＋ `cTag`** が公式のscan-guidanceの推奨
- **SharePoint Online にブロック拡張子は存在しない**（「There are currently no known types of files that cannot be added」）
- ライブラリのバージョン履歴は**既定500世代**
- テナント容量は **1TB ＋ ライセンスごと10GB**（F1/F3 は 1TB固定）
- **`DELETE` では容量は解放されない**（93日ごみ箱行き・ごみ箱も容量を消費）。即時解放は **`permanentDelete`（v1.0 GA）**
- 1時間の録画は**約400MB**（Teams公式）
- **Backlog は API で削除できない**（`register.ps1` 冒頭：「The API cannot delete (DELETE is rejected; removal is
  only possible by hand in the Backlog web UI), so this script cannot undo a post - hence the confirm gate.」）

---

## 8. 訂正した誤り（2026-07-15〜16）

| 誤り | 訂正 |
|---|---|
| `Lists.SelectedOperations.Selected` を第一候補にした | 付与APIが beta のみ＝本番非サポート → `Sites.Selected` へ |
| 「ストレージが破綻する」 | 年20GB に対し 1TB＝50年分。**実測では25TB** |
| 「4MB超で `createUploadSession`」 | 単純PUTの上限は **250MB** |
| 「delta をやめて children 列挙に」と提案 | 公式が「children列挙は取りこぼす」と明記 → 撤回 |
| `Sites.ReadWrite.All` を足す提案 | 承認の説明と食い違う → 不採用 |
| 「URLは後から変えにくい」 | 変更手順は公式にある。**変更不可なのは言語** |
| device code は `/common` 非対応と断定 | MSAL.NET の記述で、本件は Python。断定を撤回 |
| `scp` に `offline_access` があることを確認、と記載 | **`offline_access` は `scope` に載らない**（実測3回）。正常なトークンを異常と誤判定する記述だった |
| IP制限を「実質クリア」と記載 | 「今回の検証条件ではブロックされなかった」まで |
| `amr` から「パスワードレスではない」と一般化 | `amr` は**今回のサインインの認証方式**を示すもの |
| 「漏れれば何度でも」 | 「失効するまでは再利用できる」 |
| 「変更した item の祖先チェーンが全部返る」 | 1回の観測からの一般化。**子の増減では親フォルダは返らない**（実測） |
| **`approved/` の権限分離を「採用条件」とした** | **権限分離は「権限のない人の登録」には効くが「権限のある人のうっかり移動」には無力。** 心配していたのは後者だった（中村さんの指摘） |
| What If ツールで `minutes-automation` を「クラウドアプリ」に指定する手順 | **CAの対象リソースはアクセス先サービス。** クライアントアプリは選択肢に出ない |

---

## 9. 現在の状態

```
① Entra でアプリに Sites.Selected を同意   ✅ 完了（2026-07-16・中村・管理者同意不要）
② 対象サイトへ permission を付与           ❌ 未   ← 今井さん待ち
③ Sites.Selected を含むトークン取得        ✅ 完了（2026-07-16）

→ 専用サイトへのアクセスは 403 accessDenied
```

**今井さんへの依頼文は作成済み・未送信。**
