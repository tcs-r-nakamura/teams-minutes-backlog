# runtime data (internal info - never commit)
inbox/
work/
out/
state/
# secrets (tokens) - keep in ~/.config/minutes.env, never in the repo
*.env
minutes.env
# Nix
result
result-*
# python
__pycache__/
*.pyc
