# 議事録自動化 フロー整理（2026-07-14 初版 / 2026-07-15 改訂 / 2026-07-16 追記）

## 0. この文書の位置づけ

上長助言「**認証認可より先に、アプリ全体のフローを整理する**」に沿って、議事録自動化の
**全体フロー**と、それに従属する**トリガ（起点）・認証認可**を整理する。
サーバー化設計書 v0.7 の **§6 入口 / §7 取り込み** を見直す位置づけ。

**確定値（実測で取得済み・以後はこれを使う）**

| 項目 | 値 | 取得元 |
|---|---|---|
| テナントID | `85c9340e-a7a2-48cb-97cf-ca56a6e44875` | アクセストークンの `iss`（2026-07-16） |
| テナントドメイン | `tcsbunkyou.onmicrosoft.com` | 同上（authority として動作確認済み） |
| **VPSのグローバルIP** | **`133.125.62.159`** | トークンの `ipaddr`＝**Entraから見えている実際のIP**（2026-07-16） |
| 専用サイト | `https://tcsbunkyou.sharepoint.com/sites/minutes`（名称「議事録作成」） | 2026-07-15 新設 |
| siteId | `tcsbunkyou.sharepoint.com,92289f8d-ebbe-4ad3-b6a4-c041b99268e2,8046e19d-e6f7-49d5-ac57-d07e3b80b642` | Graph（2026-07-15） |
| **driveId** | **`b!jZ8okr7r00q2pMBBuZJo4p3hRoD35tVJrFfQfjuAtkKjRmf4Zn5fSrSvXE8QcLCP`** | `_api/v2.0/drive`（2026-07-16）※**要Graph照合** |
| **検証用アプリ（clientId）** | **`3c741d2b-6ee5-4ef1-adf5-9f096de64d79`（`minutes-automation`）** ← **今井さんへの依頼はこれ** | 2026-07-16 登録 |
| ~~旧・検証用アプリ~~ | ~~`81214b2e-…`（`minutes-automation-test`）~~ **後始末対象**。サインイン頻度の実測にのみ使用中 | 2026-07-15 登録 |
| 中村の oid | `63bb0835-d67e-44bf-9d9c-6308af187864` | トークンの `oid` |

**2026-07-16 の進捗②（アプリの作り直しと★付与前ベースラインの取得）**

**リネーム＋同意削除ではなく、`Sites.Selected` ＋ `offline_access` だけの新アプリを登録し直した**（§8.6 の
「検証の前提条件」を、あとから外すのではなく**最初から満たす**形にした）。**旧アプリは後始末で削除する＝数は1つのまま。**

- **★`Sites.Selected`（delegated）は管理者同意が不要だった**（Entraの「管理者の同意が必要」列＝**いいえ**）。**新事実。**
  → **今井さんへの依頼は「②サイトへの付与」だけで済む**（同意の依頼は不要）。
  → 小見山さんへの説明条件「**管理者同意が不要な委任権限のみ**」も、そのまま満たしたまま進められる。
- **★付与前のベースラインを取得（今しか取れない値）**：
  **`Sites.Selected` の同意済みトークンで専用サイトを叩いて `403 accessDenied`。**
  → **付与後に200になれば「②の付与が効いた」と断言できる**（変わるのは②だけ）。
  → **§8.2 の「3ステップ全て必須」が実測で確認された**（①③が揃っても②が無ければ403）。
- **拒否は `403 accessDenied`。「200＋空」ではなかった**（§8.6 の確認事項の一部）。
  **⚠️ ただし「未付与」のケースの話。本命のネガティブテスト（サイトAに付与してサイトBを叩く）とは別物。**
- **トークンの `scope` が完全にクリーン**：`profile openid email https://graph.microsoft.com/Sites.Selected`
  → **`Files.*` が一切混ざっていない** ＝ 「Selectedで通ったのか区別できない」問題が構造的に起きない。
  → **`offline_access` が `scope` に載らないことを3度目の確認**（§8.6 の訂正が正しかった）。
- **アクセストークン寿命の実測が3回目**：**4528秒(75分)**。4320 / 4006 / 4528 と**3回とも違う → 固定値でないことが確定**。
- **⚠️ 実装メモ：`device_code` は使い捨て。** 2回引き換えると **`AADSTS54005: OAuth2 Authorization code was
  already redeemed`**。→ **`graph.py` では取得と保存を1回のリクエストで完結させる**（表示してから取り直す、はできない）。
- refresh_token は `~/.minutes-verify/rt-selected`（600）に保存。**旧アプリの `rt` は消さない**（サインイン頻度の実測継続中）。
  - ※**条件付きアクセスはアプリ単位で対象を絞れる**ため、**本来は本番アプリでの実測の方が正しい**。
    旧アプリの15時間の結果は**参考値**として扱う。

**2026-07-16 の進捗①（サインイン頻度の実測）**
- **約15時間の無操作を挟んだ `refresh_token` 更新に成功**（詳細は §8.6）。
  → **数時間〜15時間レベルのサインイン頻度は設定されていない。** ただし**「1日」以上は未確認**（§7-2 は残る）。
- **`amr: ["pwd","mfa"]` を確認 ＝ 今回のサインインは パスワード＋MFA で行われた。**
  → **今回発行された refresh token は「パスワードベース」に該当する ＝ 中村のパスワード変更／SSPR で失効する**（§8.4）。
  → **ただし「アカウントがパスワードレスに対応していない」とまでは言えない。**
    **パスワードレス方式でサインインし直せばこの穴を塞げる可能性がある**（§8.4・**要確認**）。
- **同一 refresh_token を3回連続で使用でき、3回とも成功**
  → 公式記述「**新しいRTが出ても、古いRTは自動失効しない**」を**実測で確認**（§8.4）。
  → **失効するまでは古いRTも再利用できる ＝ ローテーションは漏洩対策にならない。後始末は必須**（§8.6）。

**2026-07-15 の進捗（承認と実機検証）**
- **小見山さんの承認取得**：「一旦その形で進めていただいて結構です」＝**方針の暫定承認**。
  あわせて**検証用アプリを中村のアカウントで登録すること**も許可された。
- **実機検証を実施**（詳細は §8.6）。device code flow・`refresh_token` の発行と更新・Graphでの一覧/DL/書き戻し が
  **VPS上で成功**。**今井さんの手を借りずにここまで到達**（＝上長の「まず自身のアカウントで検証」の指示を満たした）。
  - **⚠️ ただし検証したのは `Files.ReadWrite` ＋ 自分のOneDrive（`/me/drive`）まで。**
    **本番の `Sites.Selected` ＋ 専用サイト（`/sites/{siteId}/drive`）は未検証。**
    確認できたのは「**Graph delegated の作法とトークン運用が成立すること**」であり、
    **「Selected に絞っても通ること」「意図した範囲に限定されること」はまだ何も確認していない。**
- **⚠️ 条件付きアクセス（サインイン頻度・IP制限）の確認は未了**。
  上長への文面から質問が漏れたため、**今井さんへの依頼時に必ず確認する**（§7-2）。

**2026-07-15 改訂の要点（前提が変わった）**
- **Application permissions（app-only）は使えない**（上長判断）。範囲が個人ではなく**会社テナント**となり、
  「外部（VPS）からテナント内にアクセスする」話になるため、セキュリティポリシー上ハードルが高い。
  → **初版の Phase 2（app-only ＋ Teamsアプリケーションアクセスポリシー前提）は全面的に成立しない。**
- **Delegated permissions なら可能性がある**（それでも小見山さんの許可は必要）。
- **M365管理者は今井さん。**
- **会議は週1回**。上長談「**指定の場所にファイルを置いてAPI実行したら記事が生成されるといっただけでも十分**」。
  → 放置で完全自動化まで持っていく必要は薄い。
- Graph公式ドキュメントを調査した結果、**OneDrive for Business のフォルダ単位 delta は公式REST仕様では確認できない**
  （§8.1）。→ **設計上は「ドライブ全体（root）を監視する前提」に倒す。**
  → 渡邉さんのOneDrive起点は「ドライブ全体を監視する」ことを意味する。**初版 Phase 2 を捨てる決め手。**
- **transcript（字幕）は話者名の特定に使用中**のため捨てられない。ただし**Graph API では取得しない**方針に変更（§8.3）。

**今日判明した重要な事実（初版 2026-07-14 時点・現在も有効）**
- **録画(mp4)** は、Teams会議時に **OneDrive/SharePoint へ自動保存**される（Microsoft既定＋実運用で確認）。
- **字幕(vtt)** は、mp4と**同じフォルダには存在しない**（実機で確認済み）。Teamsの**トランスクリプト**として会議に紐づく。
- その後、録画した人が**手動でCybozuにアップロード**している（全社閲覧用）。
- **本件の実態**：**主催者は固定（渡邉さん）**で、録画は**その1人のOneDrive（Recordings）に mp4 のみ**保存される。
- 実装は **手動SSH＋CLI（`run.sh`）で稼働中**（第2回登録済み）。

---

## 1. 現状フロー（実装済み・稼働中）

```
1. Teams会議
2. →【自動】録画mp4 が OneDrive/SharePoint に保存（字幕vttは同フォルダに無い）
3. →【手動】録画者が Cybozu にアップロード（全社閲覧用の保管＋議事録の録画リンク元）
4. →【手動】録画(mp4)を入手（Cybozu/OneDrive）＋字幕(vtt)を Teams のトランスクリプトからDL
5. →【手動】scp で VPS の inbox/ へ
6. →【手動起動】./run.sh（音声抽出→さくらWhisper→用語訂正→gpt-oss-120bで下書き→整形）
7. →【人】out/minutes.md を公開前チェック
8. →【手動】register（Backlog Document APIで登録）
```

- **3〜5 は「同じファイルの移し替え」**で重複手作業。原本は最初からSharePointにある。
- 処理(6)・チェック(7)・登録(8)は完成・実運用済み（設計書 §3〜§4 のとおり）。

---

## 2. 現状の課題

- **アクセス配布が重い**：他メンバーが運用するには、VPSへのSSH（**公開鍵＋固定グローバルIP許可**）が要る。
  固定IPが無い人は運用に乗れない。**これが本件の主課題。**
- **重複手作業**：SharePoint→Cybozu→DL→scp と、同じ録画を何度も移動している。
- **起動が手動**：会議のたびに人が scp→run.sh を実行。
- **VPSは全員が同じ `aiocr` OSアカウント**。トークンは `~/.config/minutes.env` に共有保存。

---

## 3. トリガ（起点）方式 × 認証認可 ― フローが認証を決める

| トリガ方式 | 認証・認可の主体 | 録画の入手 | MS依存 | 実装規模 | 可否 |
|---|---|---|---|---|---|
| **手動SSH＋CLI（現状）** | **SSH鍵＋許可IP**（自前） | 手動DL(Cybozu) | なし | 実装済み | 稼働中 |
| 直接HTTPエンドポイント(443) | **自前の発行トークン＋TLS** | PCからPOST | なし | 中〜大（認証設計＋443公開） | 可・不採用 |
| Teamsチャット/コマンド起点(Bot) | M365アカウント | Graph API | 強 | 大 | 未検討 |
| **専用SharePointライブラリに人が投入（案B・本命）** | **M365アカウント**（置く人）＋**delegatedトークン**（VPS） | 人がDLして投入 | 中 | 小〜中 | **本命** |
| ~~固定主催者OneDrive監視＋字幕もGraph（案A）~~ | ~~app-only~~ | ~~Graph自動取得~~ | 強 | 大 | **不採用**（§4.3） |

**要点**：Microsoft系に乗せると、**利用者側のSSH鍵/固定IP配布が不要**になり、
「誰がアクセスしてよいか」の判定は**会社が既に持っているM365アカウントの仕組み**がそのまま担う。
ただし**VPS側は何らかの資格情報でGraphにアクセスする**必要があり、そこが app-only か delegated かで
テナント全体か個人かという**範囲の差**が出る。→ **delegated（個人の範囲）を採る。**

---

## 4. 目標フロー（段階計画）

### 4.0 これで何が解けて、何が解けないか（2026-07-16 追記・正直な整理）

**★解ける：他の担当者が、SSH鍵も固定グローバルIPもVPNも無しで、議事録を最後まで作れる。**
担当者はVPSに一切触らない。ブラウザ／エクスプローラーで完結する。

```
1. Teamsから録画(mp4)と字幕(vtt)を手元にDL       ← 手動（現状と同じ）
2. SharePointの inbox/ に会議フォルダを作って置く  ← ブラウザ or OneDrive同期
3. 15分待つ                                       ← VPSが自動で処理
4. out/ の下書きをエディタでチェック・修正         ← OneDrive同期（実測確認済み・§8.6）
5. approved/ へドラッグ → Backlog登録              ← ★担当者が完結する（中村を経由しない・§7-4）
6. Cybozuに録画をアップロード                      ← 手動（スコープ外・現状と同じ）
```

**★解けない：中村への依存。**
- **VPSは「中村の refresh_token」で動く。** delegated なので、**全てのGraph操作は中村として実行される。**
- **次のいずれかで、全員の議事録作成が止まる**（中村が再サインインするまで復旧しない）：
  - **中村のパスワード変更／SSPR**（**`amr: ["pwd"]` を実測。確実に起きる**・§8.4）
  - 中村のアカウント無効化・退職
  - 条件付きアクセスによる再認証要求
- **→「中村しか操作できない」は解けるが、「中村に依存する」は残る。** SSH運用より明確に良くなるが、依存は消えない。
- **app-only ならこれが解けるが、会社方針で使えない**（技術ではなく方針の制約。受け入れる）。
- **→ だから「止まったことに気づける通知」が必須要件**（§10.8）。**気づけないまま止まるのが最悪。**

**★まだ確定していない前提**（これが崩れると上は成立しない）：
- **`Sites.Selected` で各操作が通ること** … 付与待ち。**公式にも現場にも情報が無い**（§8.2・§8.6）
- **条件付きアクセスの長いサインイン頻度設定が無いこと** … **アプリ側で回避不可**（§7-2）

### Phase 1（現在）：手動 SSH＋CLI ― つなぎ運用
- いま稼働中。当面はこれで回す。
- 他メンバーに配るなら、**公開鍵＋固定IPの許可**が必要（固定IPが無い人は「運用者に録画を渡す」でカバー）。

### Phase 2（本命・案B）：専用SharePointライブラリに人が投入 → VPSがポーリングして下書き生成

```
1. Teams会議 →【自動】録画mp4 が渡邉さんのOneDrive/Recordings に保存（変更なし）
2. →【手動】渡邉さんが Cybozu にアップロード（変更なし・全社閲覧用＋録画リンク元）
3. →【手動】担当者が mp4 と vtt を入手（Cybozu / Teams）  ※ここは減らない
4. →【手動】担当者が **専用SharePointライブラリの投入フォルダにブラウザで置く**（1会議1フォルダ）
5. →【自動】VPSが15分ごとにポーリング → 検知 → ダウンロード → 既存の処理コアで下書き生成
6. →【自動】同ライブラリの out/ に minutes_<一意名>.md ＋ **事前確認レポート**（＝dry-run相当）を書き戻す
7. →【人】ブラウザで公開前チェック・修正 → **out/ から approved/ へ移動**（＝承認行為。`y` 押下の代わり）
8. →【自動】VPSが approved/ を検知 → register 実行（重複チェック・番号衝突検出はコード内で従来どおり）
9. →【自動】結果を done/ に記録
```

**この案の要点**
- **認証は delegated のみ**。VPSは**中村（本人）のアカウントの代理**として動く。app-only は使わない。
- **アクセス範囲は専用サイト1つ**に限定を狙う（`Sites.Selected`・§8.2）。そのサイトには議事録ライブラリしか置かない。
- **字幕はGraphで取らない**（人が置く）→ Teams管理センターの2トグルも transcript API も不要（§8.3）。
- **渡邉さんへのアプリ導入は不要**。検証も本番も中村のアカウントで同一経路。
- **④〜⑨のすべてがブラウザで完結する。議事録作成の定常運用からSSHと固定IPが消える。**
  ※ただし**保守用のSSHは残る**（トークン失効時の再サインイン・ログ確認・障害対応）。
  「VPSにSSHで入れる人が不要になる」わけではない。
- 処理コア（src/ 一式）は**無改修**。register も既存のものを呼ぶだけ。

### Phase 3（上積み・任意）：録画の自動取得
Phase 2 が安定してから、必要なら③④の一部を自動化する。**必須ではない**（週1回・上長談）。
初版 Phase 2（案A）の全自動化は、代償（§4.3）が見合わないため現時点では追わない。

### 4.3 案A（固定主催者OneDrive監視＋字幕もGraph）を採らない理由
1. **フォルダ単位の絞り込みが公式REST仕様では確認できず、root監視前提になる**（§8.1）。
   → 渡邉さんのOneDrive**全体**がアプリの視界に入る。
2. **app-only が使えない**。delegated だと渡邉さん本人のトークンが必要になり、
   本人のサインイン＋失効時の再サインインを本人に依頼し続けることになる。
3. **字幕APIに期限つきの管理者依頼が乗る**。Teams管理センターの2トグル（既定OFF・**2026年7月末から有効**）。
4. `getAllRecordings` / `getAllTranscripts` が **delegated 非サポート**（§8.3）。
→ 消える手間は「mp4を1回置く」だけ。**週1回の頻度に対して代償が重すぎる。**

---

## 5. 認証・認可（フロー確定後に決まる）

| | Phase 1（手動SSH） | **Phase 2（案B・SharePoint＋delegated）** |
|---|---|---|
| 認証の主体 | SSH鍵＋固定IP許可 | **M365アカウント**（置く人）／**delegatedトークン**（VPS） |
| 権限の種類 | ― | **Delegated のみ**（app-only は使わない） |
| アクセス範囲 | VPS全体（`aiocr` で何でもできる） | **専用サイト1つ**（`Sites.Selected`・要実機確認） |
| 権限の性質 | 鍵とIPを持てば全部できる | **アプリの権限 ∩ 利用者の権限**（アプリは中村の権限を超えられない） |
| 利用者の操作 | 各自SSHで実行 | **ブラウザで置く → チェック → `approved/` へ移動**（SSH不要） |
| 保守（トークン再サインイン等） | ― | **SSHが残る**（運用者のみ・低頻度） |
| 誰が設定 | 管理者（configuration.nix） | **今井さん**（アプリ登録・Selected permission 付与） |
| 承認 | 許可メンバー/IPの合意 | **小見山さん**（方針）＋**今井さん**（作業） |
| 前提確認 | ― | **条件付きアクセス**（サインイン頻度・IP制限）※不成立要因（§8.4） |

**⚠️ 認可主体を混同しないこと。** 上表の「M365アカウント（置く人）」は、**その人がライブラリにファイルを置けるか**を
決めるだけ。**VPSがGraphで読み書きできるかは、置いた人とは無関係**で、
**「中村のdelegatedトークン ∩ アプリのSelected付与」**で決まる。
→ 置く人がライブラリに書けても、**中村がその専用サイトを読み書きできなければ自動処理は動かない**
（**書き戻しと退避をするので「読める」だけでは足りない。書き込みも要る**）。
→ **中村を専用サイトの読み書き可能なメンバーにすることが必須**（§7-3）。サイトの権限を見直すときも、
中村を外すとVPSが黙って止まる。**メンバー管理時の前提として維持すること。**

---

## 6. Cybozuの扱い（録画リンク）

議事録に貼る「録画リンク」は、全社が閲覧できる **Cybozu** のURLを使っている（設計書 §3）。
SharePointの録画は権限的に全社閲覧できないことが多い。

- **案Bでは (あ) を採る**：**Cybozuは残す**。全社閲覧リンクはCybozu、処理の入力は投入ライブラリから。
  録画リンク（`fFID`）は、投入時に **`metadata.txt` に書いて一緒に置く**（§10.5）。
- (い) SharePointに寄せる案は、全社閲覧の可否確認が必要なため当面採らない。

---

## 7. 未決事項（上長・管理者に確認）

1. ~~この方針（案B・delegated）で進めてよいか~~ → **✅ 承認済み（2026-07-15・小見山さん）**
   「一旦その形で進めていただいて結構です」。**検証用アプリの登録も許可**された（実施済み・§8.6）。
2. **🔺 条件付きアクセスの確認 ― 半分解決。残りは今井さんへ。**
   - 🔺 **IPベースの制限 → 今回の検証条件ではブロックされなかった**（2026-07-15）。VPSからサインインしてGraphが200。
     **Entraから見えているVPSのIPは `133.125.62.159`**（トークンの `ipaddr`・2026-07-16 実測）。**この値は確定。**
     **⚠️ ただし「クリア」ではない。** 条件付きアクセスは**ユーザー／アプリ／リソース／場所／リスク**の組で効くため、
     **`Sites.Selected` 付与後・本番サイト（SharePoint リソース）への操作でも同じとは限らない。**
     → **最終確認は今井さん（Sign-in logs / What If ツール）で行う。** 上記IPを添えて依頼する。
   - ✅ **サインイン頻度の「短い設定」→ 無い**（2026-07-16）。**15時間11分の無操作後に refresh が成功**（§8.6）。
   - ⚠️ **残：サインイン頻度の「1日／7日／30日」等の長い設定は未確認。**
     設定されていると**無人での定期実行が成立しない**（**アプリ側で回避不可＝方針ごと組み直し**）。
     **時間経過でしか現れないため、実測で潰すには日数がかかる。→ 今井さんへの依頼時に必ず一緒に聞く。**
   - ※**上長への文面から、この質問が漏れたまま承認が出てしまった**経緯がある。依頼書で必ず拾うこと。
3. **今井さんへの依頼内容**：
   - **議事録用の専用SharePointサイトを新設**（※中村自身で作れるなら不要。要確認・§8.6）
   - **そのサイトへの `Sites.Selected` 付与**（`POST /v1.0/sites/{siteId}/permissions`・**roles は `write`**）
     対象アプリ＝検証用に登録済みのクライアントID。**SharePoint 管理者以上の権限が必要**（§8.2）。
   - **⚠️ 中村（および議事録の担当者）を、その専用サイトの読み書きできるメンバーに追加**
     → **これを忘れると動かない。** delegated は「**アプリのSelected付与 ∩ 本人のSharePoint権限**」なので、
       アプリに `write` を付けても、**中村自身がそのサイトに書けなければVPSは書き戻せない**（§5）。
       アプリの権限だけ整えて満足しがちな箇所。**依頼書に必ず含めること。**
   - **条件付きアクセス**（サインイン頻度・IP制限）の確認（上記2）
   - ※**アプリ登録は不要**（中村が登録済み・管理者同意も不要だった）
   - ※Teamsのアプリケーションアクセスポリシーは **application permissions のときだけ必要 → 今回は不要**
4. ~~register をSSHのまま残すか~~ → **決定：`approved/` フォルダ方式を採る**（2026-07-15）。
   - **理由**：公開前チェックをした人がそのまま登録できる方が自然。チェックする人と登録する人が分かれると
     「チェック済みだが未登録」の受け渡しが発生し、かえって手間が増える。
   - **緩くなる点（重要・当初の評価は誤りだった）**：「うっかり移動＝登録」は `y` 押下より明確に緩い。
     **⚠️ そして誤登録は簡単には取り消せない。** `register.ps1` の冒頭に
     「**The API cannot delete (DELETE is rejected; removal is only possible by hand in the Backlog web UI),
     so this script cannot undo a post - hence the confirm gate.**」とあるとおり、
     **`y` の確認ゲートは「APIで取り消せないから」存在している**。
     復旧には**権限を持つ人がBacklogのWeb UIで手作業削除**する必要がある。
   - **⚠️ 2026-07-16 訂正：「権限分離で補う」という理由付けは筋が通っていなかった。**
     ~~**→ 採用の条件**：緩さを**SharePointの権限分離で補う**（`approved/` は登録してよい人だけ書き込み可）。
     分離ができないなら再検討。~~
     - **中村さんの指摘**：「そもそも**書き込める人しかメンバーに入れない**ので、分離は要るのか」
     - **そのとおり。しかも私の論理は飛んでいた**：
       **権限分離は「権限のない人が登録してしまう」リスクには効くが、
       「権限のある人がうっかり移動する」リスクには無力。** 心配していたのは後者なのに、前者の対策を挙げていた。
     - **さらに同じ節の中で矛盾していた**：上の「理由」は「**チェックした人がそのまま登録できる方が自然**」
       ＝ **チェックできる人＝登録できる人**。**なら分ける対象がいない。**

   - **→ 決定（2026-07-16）：`approved/` のフォルダ単位の権限分離は採用しない。**
     **代わりに「サイトのメンバー＝Backlogに登録してよい人だけ」を運用の前提とする。**
     - **⚠️ これは運用の制約になる。** **サイトにメンバーを追加するときは、必ず
       「この人はBacklogに登録してよいか」を確認すること。** 「置くだけの人」を入れた瞬間に前提が崩れる。
       → **必要になったらそのとき権限分離を検討する**（設計を先回りしない）。
   - **→ 決定（2026-07-16）：担当者が「置く → チェック → 登録」まで完結する。**
     **中村を経由しない。**（＝上の「チェックした人がそのまま登録できる方が自然」を素直に実装する）
     - **⚠️ 小見山さんへの文面との食い違い**：承認をもらった文面には
       「**Backlog登録は当面、従来どおり私が実行します**」と書いてある。**今の想定はこれより一歩進んでいる。**
     - **セキュリティの範囲（テナント／権限スコープ）は変わらないので再承認は不要**と判断するが、
       **次の報告で「担当者がチェックからそのまま登録まで行う形にした」と一言触れる。**
       **黙って変えると、後から見て説明が食い違う。**
   - **→ 「うっかり移動」も、対策を積まずに受け入れる（2026-07-16 決定）。**
     - **理由**：**週1回・関係者は実質2名**。誤登録しても**BacklogのWeb UIで手作業削除すれば復旧できる**
       （APIでは消せないが、不可能ではない）。**対策を積むコストの方が高い。**
     - **残る防御（権限分離とは無関係に効く）**：
       ① 台帳で `registered` の item-id は再実行しない ② **本文ハッシュ＋タイトル＋第N回**での突合（コピー対策）
       ③ `find_duplicate` ④ `number_conflict`（いずれも `vps/minutes/src/backlog.py`）⑤ `.md` 以外はトリガしない
     - **これらで捕まらないのは「チェック途中の下書きをうっかり `approved/` に入れる」ケースのみ。**
       → **登録後の通知（§10.8）で気づける**（通知は refresh_token 失効の検知のために元々必要。副産物として効く）。
   - **`--dry-run` 相当**：下書き生成時に**事前確認レポート**を `out/` へ書き出す（重複・番号衝突・登録先）。
     ただし**人がmdを編集するとレポートは陳腐化する**ため、`approved/` 到着時に `cTag` が変わっていれば
     **dry-run を再実行**し、その結果を残す（§10.5）。→ 「dry-run で確認 → y」と**近い**2段構えにはなるが、
     **同一ではない**。**その差は許容する**（上記）。
5. **SharePoint側の制約**：専用ライブラリに **mp4 を置けるか**（ファイル種別制限・容量制限・DLP・
   ウイルススキャン遅延・外部共有ポリシー）。→ 実機で確認。

---

## 8. 技術調査の結果（2026-07-15 調査・2026-07-16 実測追記／公式ドキュメント根拠）

> Graph API の仕様・権限は変わりうるため、着手前に**最新の公式ドキュメントと実機**で再確認する。

### 8.1 OneDrive for Business の「特定フォルダだけの監視」は公式仕様で確認できない ★案A却下の根拠
- `driveItem: delta` の公式HTTP request一覧は**すべて `/root/delta` 止まり**。フォルダ単位の形式は記載がない。
  https://learn.microsoft.com/en-us/graph/api/driveitem-delta?view=graph-rest-1.0
- 過去に「In OneDrive for Business and SharePoint, **delta is only supported on the root folder**」と明記されていたが、
  2018年のドキュメント整理で削除。**動作変更のアナウンスではない＝撤回の証拠なし。**
- 非rootで叩くと `notSupported: view.delta can only be called on the root.` の報告あり（GitHub issue #1060）。
- ODfB では **`$filter` は公式に "not available"**、`$orderby` は `name` と `url` のみ。→ 日時での絞り込みも不可。
  https://learn.microsoft.com/en-us/onedrive/developer/rest-api/concepts/optional-query-parameters?view=odsp-graph-online
- 変更通知でも「On OneDrive for Business, you can subscribe to **only the root folder**」と現行明記。
- **注**：SDKスニペットは item スコープの delta を公開しており、REST仕様と矛盾している（未解決）。
  → 「不可」と断定せず、**「公式REST仕様上は確認できず、root監視前提になる」**と扱う。

### 8.2 Selected permissions（案Bの土台）
- 「**all Selected scopes now support delegated and application modes**」＝ delegated でも使える。
- 「In the delegated scenario, both the application and user permissions are calculated and then **intersected**,
  which means that **the application can never exceed the user's permissions**.」
  https://learn.microsoft.com/en-us/graph/permissions-selected-overview

| スコープ | 粒度 | 付与APIの状態 | 本件での位置づけ |
|---|---|---|---|
| **`Sites.Selected`** | サイトコレクション単位 | **`POST /v1.0/sites/{siteId}/permissions`＝v1.0 GA** | **採用（第一候補）** |
| ~~`Lists.SelectedOperations.Selected`~~ | リスト（＝ドキュメントライブラリ）単位 | **beta のみ（本番非サポート）** | **不採用**（下記） |
| `ListItems.SelectedOperations.Selected` | リストアイテム/フォルダ単位 | 同上 | ― |
| `Files.SelectedOperations.Selected` | ファイル/ライブラリフォルダ単位 | driveItem経路は v1.0 GA | 代替候補 |

**⚠️ `Lists.SelectedOperations.Selected` を採らない理由（2026-07-15 調査で判明）**
- リスト単位の付与API `POST /sites/{siteId}/lists/{listId}/permissions` は **beta にしか存在しない**。
  公式警告：「APIs under the `/beta` version ... **Use of these APIs in production applications is not supported.**」
  v1.0 の list リソースには `permissions` リレーション自体が無い。
  https://learn.microsoft.com/en-us/graph/api/list-post-permissions?view=graph-rest-beta
  https://learn.microsoft.com/en-us/graph/api/resources/list?view=graph-rest-1.0
- → **スコープは存在するが、本番サポートされた付与手段が無い。**
- **代わりに `Sites.Selected`（v1.0 GA・delegated は2024-02-09 対応）＋ 専用サイト新設**を採る。
  粒度はサイト単位になるが、**そのサイトには議事録ライブラリしか置かない**ので実質の範囲は同じ。
  加えて**サイトコレクションレベルの付与は継承を壊さない**（list/item/file への付与は継承を破棄すると公式が警告）。
  https://devblogs.microsoft.com/microsoft365dev/sharepoint-now-supports-delegated-sites-selected-authentication/

- **付与は3ステップ全て必須**（1つでも欠けるとアクセス不可）：
  ①Entra IDでのアプリ同意 → ②対象サイトへ permission を付与 → ③そのスコープを含むトークン取得。
  - **✅ 2026-07-16 実測で確認**：①と③を済ませ、②が無い状態で専用サイトを叩くと **`403 accessDenied`**。
    **紙の上の話ではなく、実際にそのとおりだった。** これが**付与前ベースライン**になる（§8.6）。
  - **✅ ①（Entraでのアプリ同意）は管理者同意が不要**（2026-07-16 実測。Entraの「管理者の同意が必要」列＝**いいえ**）。
    → **中村が自分で①を完了できる。今井さんに頼むのは②だけ。**
  - **付与API**：`POST /v1.0/sites/{siteId}/permissions`（**v1.0 GA**）
    ボディ：`{"roles":["write"], "grantedToIdentities":[{"application":{"id":"<clientId>","displayName":"..."}}]}`
    ※ **書き戻しをするので `roles` は `write`**（`read` では不可）。
    ※ ボディ形状は **site単位＝`grantedToIdentities`（配列）／driveItem単位＝`grantedToV2`** と異なる。要注意。
  - **付与に必要な権限**：`Sites.FullControl.All`。
    **delegated の場合、実行者は SharePoint 管理者以上のロールが必要**（公式明記）→ **今井さんの作業**。
  - **鶏と卵**：`Sites.Selected` だけのトークンでは自分に付与できない。付与は必ず管理者側から。
  https://learn.microsoft.com/en-us/graph/permissions-selected-overview
  https://learn.microsoft.com/en-us/graph/api/site-post-permissions?view=graph-rest-1.0
- **⚠️ 要実機確認（最重要）**：`driveItem: delta` / children列挙 / ダウンロード / 書き戻し の各APIの権限表には
  **`Files.*` / `Sites.*` の広い権限しか列挙されておらず、Selected スコープは明示されていない**。
  Selected permissions の設計思想（アクセス先リソースで制御）とは整合するが、**公式表だけでは「通る」と断定できない**。
  → **本実装前に、当社テナントで小さく実機確認する。**
- 既知の穴：`driveItem: search` は Selected 非対応と明記。webhook subscription の権限表にも Selected は載っていない
  （→ webhookは使わずポーリングとする本方針とは整合）。

### 8.3 transcript（字幕）― 使うが、Graphでは取らない
- `OnlineMeetingTranscript.Read.All` は **delegated をサポート**。「This API is also available to users who are
  **part of the meeting calendar invite**」＝主催者以外でも招待されていれば取得可。
  https://learn.microsoft.com/en-us/graph/api/calltranscript-get?view=graph-rest-1.0
- **アプリケーションアクセスポリシーは application permission のときだけ必要**（delegated では不要）。
- 話者名は **`text/vtt` の `<v Speaker>` voice tag** に入る（→ 既存 `vtt.py` がそのまま使える）。
- **⚠️ ただしTeams管理センターに2つの独立したトグルがあり、どちらも既定OFF**：
  - 「Microsoft Graph access」OFF → 全transcript要求が `403 GraphAccessToTranscriptsDisabled`（**回避策なし**）
  - 「Include speaker attribution」OFF → `text/vtt` 要求が `403 SpeakerAttributionNotAllowed`（話者名なし形式なら可）
  - 公式に「the related tenant administrator controls **take effect at the end of July 2026**」と記載。**期限つき。**
  https://learn.microsoft.com/en-us/microsoftteams/meeting-transcript-api-access
- **`getAllTranscripts` / `getAllRecordings` は delegated 非サポート**（"Not supported." と明記）。
  → 初版 §10.4「主催者単位で横断列挙して delta 同期」は**成立しない**。
- **→ 結論：字幕は人がTeamsからDLして投入する。** 上記の管理者依頼・API制約をすべて回避できる。

### 8.4 トークンの保持・更新（delegated の無人実行）
- refresh token の既定「90日」は **`MaxInactiveTime`（無操作期間の上限）**。
  `MaxAgeSingleFactor` / `MaxAgeMultiFactor` は既定 **`Until-revoked`**（絶対寿命の上限なし）。
  → 15分ごとに使えば**非アクティブ期限には到達しない**。
  https://learn.microsoft.com/en-us/entra/identity-platform/configurable-token-lifetimes
- **ただし「実質切れない」とは言えない。失効事由**：条件付きアクセス、管理者による失効、本人のパスワード変更/SSPR、
  リスクベース制御、CAEのcritical event。**失効時の自動復旧手段は公式に存在しない**（interactive sign-in のみ）。
  https://learn.microsoft.com/en-us/entra/identity-platform/refresh-tokens
- **⚠️ 不成立要因**：**条件付きアクセスの Sign-in frequency（定期的な再認証）**が設定されていると無人実行は破綻。
  **アプリ側で回避不可。** IPベースの条件付きアクセスがあればVPSのIPが許可レンジ内である必要（→ §7-2）。
  https://learn.microsoft.com/en-us/entra/identity/conditional-access/howto-conditional-access-session-lifetime
- 2021-01-30以降、refresh/session token の寿命は token lifetime policy で**設定不可**（既定値のみ・管理者も変更不可）。
- **device code flow は public client 限定**。public client ＋ パスワード認証だと**本人のパスワード変更/SSPRで失効**する
  （confidential client なら失効しない）。**パスワードレス認証なら public client でも失効しない。**
  - **⚠️ 2026-07-16 実測：`amr: ["pwd","mfa"]`** ＝ **今回のサインインは パスワード＋MFA で行われた。**
    → **今回発行された refresh token は「パスワードベース」側に該当する。**
      **中村がパスワードを変更／SSPRした時点で無人ポーリングが止まり、再サインインが必要になる。**
    → **confidential client には逃げられない**（device code flow は public client 限定）。
    → **⚠️ ただし「回避不可」と断定してはいけない（2026-07-16 訂正）。**
      `amr` が示すのは**今回使われた認証方式**であって、**アカウントがパスワードレス非対応という意味ではない。**
      上記のとおり**パスワードレス認証なら public client でも失効しない**ため、
      **パスワードレス方式（Windows Hello for Business / FIDO2 / Authenticator のパスワードレスサインイン等）で
      device code flow のサインインを通せれば、この穴は塞げる可能性がある。** → **□ 要確認**
      （device code flow のブラウザ側でこれらの方式が選べるか、テナントで有効かの両方が前提）。
    → 塞げない場合は**運用上の既知の穴として受け入れる。** いずれにせよ、
      **停止に気づける仕組み（§10.8 の失敗通知）は前提条件**（パスワード変更以外の失効事由も残るため）。
- **⚠️ 2026-07-16 実測：同一 refresh_token を3回連続で使用でき、3回とも成功した。**
  → 公式記述「**ローリング更新で新しいものが返っても、古いものは自動失効しない**」を**実測で確認**。
  → **失効するまでは、古いRTも再利用できる。ローテーションだけでは漏洩対策にならない。** 下記セキュリティ項の裏付け。
  → ※「漏れれば**何度でも**」は過大（2026-07-16 訂正）。**寿命・管理者失効・パスワード変更・条件付きアクセスでは止まる。**
- **セキュリティ**：「Refresh tokens are bound to a combination of **user and client**, but
  **aren't tied to a resource or tenant**.」→ **読めた人は中村本人として同意済みスコープ全部でトークンを取得できる。**
  公式の保護要件は「access control **and encryption**, in relation to the **user-owner, or process identity**」。
  https://learn.microsoft.com/en-us/entra/architecture/resilience-client-app
  → **全員が同じ `aiocr` でログインする現構成では、この要件を構造的に満たせない**（同一 user-owner のため `0600` が無意味）。
  → **バッチ専用OSユーザーへの分離が必須**（§10.7）。ただし**OSユーザー分離＋`0600` は「アクセス制御」のみで、
  「暗号化」は別途**。「公式要件を満たす」とは言わず「共有 `aiocr` より改善する」と表現する。

### 8.5 mp4のダウンロードと重複判定
- `$select` で **`@microsoft.graph.downloadUrl`** を取得 → 直接GET（**Authorization不要**）。`/content` は302で同じURLへ。
- **`Range` は `/content` ではなく downloadUrl 側に付ける。** レンジが無視されて `200` で全体が返ることがあるため
  **`206`/`200` の判定が必須**（しないと再開処理でファイルが壊れる）。
- downloadUrl の有効期限は**公式内で矛盾**（driveItem resource:「1 hour」／Download content:「might expire within minutes」）。
  → **取得直後に使い、失効したら driveItem を再取得**する。
- **重複判定：`(drive-id, item-id)` をキーに、`cTag`（内容が変わったときだけ変わる）で再処理要否を判定。**
  これは公式ガイダンスが明示的に推奨する方法。
  https://learn.microsoft.com/en-us/onedrive/developer/rest-api/concepts/scan-guidance?view=odsp-graph-online
- **`id` は移動・リネームで不変**（公式明記）。一意性スコープは "within the Drive" → 台帳キーは **`(drive-id, item-id)`**。
- ハッシュは ODfB では **`quickXorHash` のみ保証**。**`sha256Hash` は「This property isn't supported. Don't use.」と明記**。
- アップロード途中は **`pendingOperations` / `pendingContentUpdate`** で判別できる可能性があるが、
  **Teams録画/SharePoint投入での挙動は公式に記載なし＝要実機検証**。→ `size`・`cTag` の安定化待ちを併用。
- **delta の注意**：最新状態のみ返る／同じitemが複数回出る（**最後の出現を採用**）／**パスでなくitem-idで追跡**／
  **`410 Gone` + resync**（`resyncChangesApplyDifferences` 等）のハンドリングが必須／`?token=latest` で初期全列挙を回避可能。
- **スロットリング**：429は **`Retry-After` を最優先**（指数バックオフは補助）。`RateLimit-Reset` と食い違えば**大きい方**。
  SharePointのユーザー単位制限は Requests **3,000/5min** 等。UA装飾 `NONISV|株式会社ティー・シー・エス|minutes-automation/1.0` を付けると優先される。
- 1時間の録画は**約400MB**（Teams公式）。

### 8.6 実機検証の結果と、残る確認事項

#### ✅ 検証済み（2026-07-15・VPS上・中村のアカウント・検証用アプリ `81214b2e-…`）
上長の承認を得て検証用アプリを登録し、`Files.ReadWrite` ＋ `offline_access`（**どちらも管理者同意不要**）で実施。
**今井さんの手を借りずに、以下がすべて通った。**

| 検証項目 | 結果 |
|---|---|
| 一般ユーザーによる**アプリ登録** | ✅ 可能（テナントで許可されている） |
| `Files.ReadWrite` / `offline_access` の**管理者同意** | ✅ **不要**（本人同意で済んだ） |
| **device code flow** でサインイン | ✅ 成功（`allowPublicClient=true` が有効） |
| **`refresh_token` の発行** | ✅ 返ってきた（`offline_access` が有効） |
| **VPSからGraphへの疎通** | ✅ HTTP 200（ログイン経路はIP制限に抵触せず） |
| `GET /me/drive/root/children`（**一覧**） | ✅ HTTP 200 |
| `PUT /me/drive/root:/…/hello.md:/content`（**書き戻し**） | ✅ HTTP 201 |
| `GET …/content`（**ダウンロード**） | ✅ HTTP 200・**内容一致** |
| **`grant_type=refresh_token` でトークン更新** | ✅ **人の介在なしで成功** |
| 更新時に**新しい `refresh_token`** が返るか | ✅ 返る（ローリング更新が働いている） |
| アクセストークンの寿命 | 実測 **4320秒(72分)**（公式どおり60〜90分でランダム） |

#### ✅ 検証済み ― OneDrive同期の挙動（2026-07-15・専用サイト `/sites/minutes` で実測）
**公式に一切文書化されていない挙動を、実測で確定させた。** `Sites.Selected` も Graph も使わず、
SharePoint自身のREST API（ブラウザのログインセッション）で `UniqueId` を追跡して確認。

| 検証項目 | 結果 |
|---|---|
| 一般ユーザーによる**サイト新設** | ✅ 可能（コミュニケーションサイト `/sites/minutes` を作成） |
| **OneDriveへのショートカットの追加** | ✅ エクスプローラーに `OneDrive - <会社名>\議事録投入 - ドキュメント\` として出現 |
| エクスプローラーから **VS Codeで `.md` を開いて編集・保存** | ✅ 同期される。**`UniqueId` 不変** |
| エクスプローラーで **`out/` → `processed/` へドラッグ移動** | ✅ **`UniqueId` 不変**（＝SharePoint側で **move** として扱われた） |
| **日本語ファイル名** | ✅ 問題なし |

```
終始一貫: 50dd006d-a6b8-4dde-a7de-32939dc0dffa
  out/ に配置 → VS Codeで編集・保存 → processed/ へドラッグ移動  すべて同一
```

**→ 設計上の結論：`approved/` 方式の二重登録防止は `item-id` ベースのままで成立する。**
**→ 公開前チェックの手段が確定：OneDriveショートカット ＋ 手元のエディタ**（§10.5）。

**⚠️ 限界（正直に）**
- **サンプル1件**。1回の観測であり、断定はできない。
- ~~**`UniqueId` は Graph の `item-id` そのものではない**。Graph の id はこれを元にしているので**強い代理指標**だが、
  厳密には別の値。→ `Sites.Selected` 付与後に Graph 側でも同じことを確認する。~~
  → **✅ 解消（2026-07-16）**：**Graph の item-id でも移動後に不変であることを実測**（下記「`delta` の実挙動」）。
    `PATCH /me/drive/items/{id}` で親を変更 → **id は `01HJFHMPG3HH57ANKIIVEZHNL7JHZJP5UZ` のまま**。
    **⚠️ ただし `/me/drive` での確認。専用サイトでの再確認は残る。**
- **「コピー」なら当然 id は変わる**（自明・検証不要）。→ 多層の防止策（本文ハッシュ等）は維持する（§10.5）。
- **他のPC・他の人が同時に同期している場合**は未検証。→ 競合コピーの検出は引き続き必要（§10.5）。

**⚠️ この表の読み方を誤らないこと**
- **検証対象は `Files.ReadWrite` ＋ 自分のOneDrive（`/me/drive/...`）**。
  **本番の `Sites.Selected` ＋ 専用サイト（`/sites/{siteId}/drive/...`）ではない。**
  → 言えるのは「**Graph delegated の作法とトークン運用が成立する**」まで。
  → **「Selected でも通る」「範囲が限定される」はまだ何も確認していない。**
- **「新しい refresh_token が返る」＝「無期限に回せる」ではない。** ローリング更新が働いていることを示すだけで、
  **条件付きアクセス・管理者による失効・パスワード変更などでいつでも切れる**（§8.4）。失効時は人の再サインインが必要。

#### ⚠️ `Sites.ReadWrite.All` は使わない（2026-07-15 判断）
`Sites.Selected` の付与（今井さん待ち）を待たずに検証を進めるため、検証用アプリに
**`Sites.ReadWrite.All`（delegated・管理者同意不要）**を足す案を検討したが、**採らない**。
- 実態は「**中村がアクセスできる全SharePointサイトの読み書き**」。delegated なので本人の権限は超えないが、
  **「専用サイト1つに絞る」と説明して承認をもらった直後に、説明なしで全サイトへの同意を入れる**ことになる。
- **この会社は Application permissions を「範囲がテナント全体になるから」という理由で止めている。**
  技術的に承認文言（「私がアクセスできる範囲の動作確認のみ」）の内側でも、**後から見て筋が悪い。**
- → **今井さんの `Sites.Selected` 付与を待つ。** 検証が数日遅れるだけで失うものはない。
- どうしても先に検証する必要が出た場合は、**「中村がアクセス可能な全サイトへの一時的な読み書き同意になる」と
  明記して上長に再確認する。** 曖昧にしたまま進めない。

#### ⚠️ 検証用アプリの後始末（検証完了後に必ず実施）
**refresh token は、使うたびに新しいものが出ても、古いものは自動失効しない**（公式明記・§8.4）。
VPS上に残ったトークンは、**漏れれば中村本人としてGraphを操作できる鍵**。
**アプリを削除すれば既発行トークンが即時無効になるかは、公式からは確認できなかった＝要確認。**
**⚠️ 対象は2アプリある**（2026-07-16 に新アプリを登録したため）。**旧アプリ `81214b2e-…` は、
サインイン頻度の実測が済み次第、先に削除してよい**（それ以外の役目は終わっている）。

→ 検証後は次を**すべて**実施する（1つでも漏らさない）。
- □ VPS上のトークンファイルを消去（`/tmp/token*.json`・**`~/.minutes-verify/rt`**・**`~/.minutes-verify/rt-selected`**）
- □ **OneDrive の検証用フォルダを削除**：**`minutes-test/`**（`hello.md`・`delta1.txt`）・**`minutes-test2/`**（2026-07-16 の delta 検証で作成）
  → **`DELETE` はゴミ箱行き**（容量を消費し続ける）。**`permanentDelete`（v1.0 GA）で完全削除する**か、ゴミ箱も空にする
- □ **専用サイトの `inbox/test/` を削除**（2026-07-16 の同期検証で作成。**200MBのmp4が入っている**）
  → **残したまま実装すると `ingest` が処理しようとする。** 容量も食う
- □ **ユーザー同意の削除**（Entra → エンタープライズ アプリケーション → 該当アプリ → 権限 → 取り消し）**×2アプリ**
- □ **refresh token の明示的失効**（本人のサインアウト／管理者によるトークン失効）
  → **⚠️ 2026-07-16：新アプリの `refresh_token` を Claude Code の会話に貼ってしまった**（値が対話ログに残っている）。
    **この失効は必須**。「古いトークンは自動失効しない」ことは実測済みのため、**放置すれば使える鍵が残る。**
- □ **アプリ登録の削除**（Entra → アプリの登録 → 削除）
  （※上の「OneDrive の検証用フォルダを削除」と同じ。**`minutes-test/` と `minutes-test2/` の2つある**）

#### ✅ 検証の前提条件 → **解決済み（2026-07-16・新アプリを登録して対応）**

**採った手段：`Sites.Selected` ＋ `offline_access` **だけ**の新アプリ `minutes-automation`（`3c741d2b-…`）を新規登録。**
→ **トークンの `scope` が `profile openid email https://graph.microsoft.com/Sites.Selected` となり、`Files.*` は皆無**（実測）。
→ **同意を後から外すのではなく、最初から入れない**形にしたので、**混入の可能性が構造的に無い。**
→ 旧アプリ `81214b2e-…`（`minutes-automation-test`）は**後始末で削除する＝アプリの数は1つのまま**
  （小見山さんへの説明「検証用のアプリを1つ・検証後は削除」の範囲内）。
→ ただし**サインイン頻度の実測に使用中**のため、**それが済むまでは残す**（下記の後始末チェックリストに含める）。

<details><summary>（参考）当時の記述 ― なぜこれを問題視したか</summary>

**検証用アプリには既に `Files.ReadWrite` が同意済み。このまま `Sites.Selected` を足して検証してはいけない。**
トークンに両方のスコープが乗り、**「Selected で通った」のか「`Files.ReadWrite` で通っただけ」なのか区別できなくなる。**
ネガティブテスト（範囲外が403になること）も**素通りしてしまい、絞れていないのに絞れたと誤判定する。**
→ **`Sites.Selected` の検証時は、次のいずれかを必ず行う。**
- **`Files.ReadWrite` の同意を削除する**（エンタープライズ アプリケーション → 権限 → 取り消し）、または
- **検証専用の別アプリを新規に登録する**（`Sites.Selected` ＋ `offline_access` のみ）

→ **加えて、取得したアクセストークンの `scp` クレームを必ずデコードして確認する。**
確認するのは **`Sites.Selected` が入っていること**と、**`Files.*` が混ざっていないこと**の2点**だけ**。

**⚠️ `offline_access` を `scp` で探してはいけない（2026-07-16 訂正）。**
`offline_access` は「refresh_token を返させる」ための指示であって**リソースへの権限ではない**ため、
**アクセストークンには載らない**（2026-07-16 実測：`"scope":"profile openid email .../Files.ReadWrite"`）。
**`offline_access` の有無は「`refresh_token` が返ったか」で確認する。**
→ 旧記述「`Sites.Selected offline_access` だけが入っていること」は誤り。**正常なトークンを異常と誤判定する。**

</details>

#### ✅ 専用サイトの素性確認（2026-07-16・SharePoint REST・ブラウザのログインセッション）

**付与を頼む前に、サイトが想定どおりに作れているかを実測で確定させた。**
**⚠️ Graphは付与前で403なので、SharePointの `_api/` を使った**（付与前でも使える抜け道）。

`https://tcsbunkyou.sharepoint.com/sites/minutes/_api/web?$select=Title,Created,Language,WebTemplate`

| 項目 | 値 | 意味 |
|---|---|---|
| `Created` | **2026-07-15T07:32:36** | **文書の「7/15新設」どおり。素性不明のサイトではない** |
| **`Language`** | **1041** | **日本語。→ 唯一の「作り直す理由」（言語は変更不可）が消滅。このまま使う** |
| `Title` | 議事録作成 | |
| **`WebTemplate`** | **`SITEPAGEPUBLISHING`** | **コミュニケーションサイト。→ M365グループが付かない＝グループの有効期限でサイトごと消える経路が無い** |

`https://tcsbunkyou.sharepoint.com/sites/minutes/_api/v2.0/drive`

| 項目 | 値 | 意味 |
|---|---|---|
| **`id`** | **`b!jZ8okr7r00q2pMBBuZJo4p3hRoD35tVJrFfQfjuAtkKjRmf4Zn5fSrSvXE8QcLCP`** | **driveId を取得。⚠️ Graph の `driveId` と同一かは付与後に照合**（`UniqueId` と同じ注意） |
| **`owner`** | **中村流星（`63bb0835-…`）** | **中村が所有者＝「メンバー追加」を今井さんへの依頼から外した判断が実測で裏づけられた** |
| `name` / `driveType` | ドキュメント / `documentLibrary` | 既定のライブラリ。**名前が日本語＝言語1041と整合** |
| **`quota.total`** | **27,487,790,694,400（＝25 TiB）** | **1TB前提の「50年分」より更に余裕。ストレージは論点にならない**（§10.5 の訂正を再確認） |
| `quota.used` | 1,447,590（1.4MB） | |

**⚠️ 紛らわしい点（調べた結果、問題なし）**
ライブラリの `createdDateTime` は **2026-07-07**・`createdBy` は **「システム アカウント」**で、**サイトの作成日(7/15)より古い。**
→ **SharePoint Online は作り置きしたサイトをプールし、「サイトの作成」時に1つ払い出して名前と設定を付け替える**ため、
  **中身の部品はプールに入った日付を引きずる。** `createdBy` がシステムアカウントであることと完全に整合する。
→ **サイトが7/15より前から別用途で存在していた、ということではない。**

#### ✅ 付与前ベースライン（2026-07-16・**今しか取れない値**・新アプリ `3c741d2b-…`）

**②サイトへの付与が無い状態で、`Sites.Selected` の同意済みトークンで専用サイトを叩いた結果。**

| 検証項目 | 結果 |
|---|---|
| `Sites.Selected`（delegated）に**管理者同意が必要か** | ✅ **不要**（Entraの列が「いいえ」）→ **今井さんへの依頼は②の付与だけ** |
| device code flow（新アプリ・`Sites.Selected` ＋ `offline_access`） | ✅ 成功 |
| トークンの `scope` | ✅ `profile openid email https://graph.microsoft.com/Sites.Selected`（**`Files.*` 皆無**） |
| `refresh_token` | ✅ 返る（`scope` には載らない＝正常・**3度目の確認**） |
| アクセストークン寿命 | 実測 **4528秒(75分)**。4320 / 4006 / 4528 → **3回とも違う＝固定値でないことが確定** |
| **`GET /v1.0/sites/{siteId}`** | **✅ `403 accessDenied`** ← **期待どおり** |
| **`GET /v1.0/sites/{siteId}/drive/root/children`** | **✅ `403 accessDenied`** ← **期待どおり** |

**→ 付与後にここが `200` になれば、変わったのは②の付与だけなので「`Sites.Selected` の付与が効いた」と断言できる。**
**→ §8.2 の「3ステップ全て必須」が実測で確認された**（①③が揃っても②が無ければ通らない）。
**→ 拒否は `403 accessDenied` であって「200＋空」ではなかった。**
  **⚠️ ただしこれは「未付与」のケース。本命のネガティブテスト（サイトAに付与してサイトBを叩く）とは別物。**
  **「未付与なら403」と「付与済みだが範囲外なら403」は別の話。後者はまだ何も分かっていない。**

**⚠️ 実装メモ：`device_code` は使い捨て。**
2回引き換えると **`AADSTS54005: OAuth2 Authorization code was already redeemed`**（実測）。
→ **`graph.py` では取得と保存を1回のリクエストで完結させる。**「表示してから取り直す」はできない。

#### ✅ `delta` の実挙動（2026-07-16・**旧アプリ `81214b2e-…` ＋ 自分のOneDrive `/me/drive`**）

**`Sites.Selected` の付与を待たずにできる「作法の練習」として実施。**
**目的**：付与後に動かなかったとき、**「Selected のせい」か「こちらの使い方が悪い」かを切り分けられるようにする。**
**⚠️ 検証したのは `/me/drive`。専用サイト（`/sites/{siteId}/drive`）で同じかは未確認。**

**手順**：`?token=latest` → ファイル作成 → delta → 別フォルダへ移動 → delta → 削除 → delta

| 実測できたこと | 実装への意味 |
|---|---|
| **`?token=latest` は `"value":[]` ＋ `deltaLink` だけ返す** | **初回に既存ファイルを全部処理する事故を防げる。** 本番でも初期化はこれを使う |
| **`deltaLink` は毎回変わる** | **毎回保存し直す。「引換券の保存」と「処理の完了」の順序が冪等性の要**（保存漏れ＝二重処理） |
| **★移動は「1件の更新」。`deleted` は付かない** | **`approved/` 方式に理想的。** 「削除＋作成」に見えたら二重処理の危険があった |
| **★移動しても `id` 不変（Graph の item-id で確認）** | **昨日の `UniqueId` での確認の穴が埋まった**（下記の「限界」を解消） |
| **★移動元のフォルダは delta に返らない** | **パス／フォルダの中身で追うと破綻する。** 公式の「item-idで追え」の理由が実物で判明 |
| **★削除は `id` ＋ `deleted:{state:deleted}` のみ。`name` すら返らない** | **台帳 `(drive-id, item-id)` が必須。** 名前で管理していたら「何が消えたか」すら分からない |
| **`lastModifiedBy` / `createdBy` に `application.id` が入る** | **★delta の自己反応ループを見分けられる可能性**（自分の `clientId` なら無視）。**要・専用サイトでの確認** |
| **`@microsoft.graph.hasMoreData` / `syncStatus` が返る** | 文書に無かった。`nextLink` のページング制御に使えそう |
| **`root` の `size` がドライブ全体（実測 約2GB）** | **★案A（渡邉さんのOneDrive監視）却下の理由の実物。** root/delta はドライブ全体を見ている |

**⚠️ 訂正（同日中に自分で訂正）**
一度「**変更した item の祖先チェーンが全部返る**」と書いたが、**1回の観測からの general化しすぎだった。**
3回のdeltaを並べると **「子が増減しても親フォルダは返らない」** ことが分かる（移動元も、削除の親も返らなかった）。
**正確に言えるのは**：`root` は毎回返る／変更のあった item は返る／**フォルダが返ることはある**（新規作成時など）／
**子の増減だけでは親は返らない。**
→ **実装上の結論は変わらない**（`folder` を持つものは除外が必要）が、
  **「祖先チェーンが必ず返る」と思い込んで組むと外す。**

#### □ 残る確認事項（今井さんの作業が前提）
- □ **`Sites.Selected` で 一覧 / ダウンロード / 書き戻し が通るか**（§8.2）
  - **公式の権限表には Selected の明示なし**（＝公式からは可否を判断できない）。
  - **現場報告は断片的**：**delegated × Selected × `delta` の報告はゼロ**。
    **`/lists/` 配下は 403 か空**という**未解決の報告**あり（2024-04・公式ブログのコメント欄でMSは未回答）。
    一方 **drive 経路（`/drives/…`）では children 列挙が動いた報告**あり（2024-04）。
  - → **drive 経路を使い、lists 経路を避ける。**
- □ **ネガティブテスト：意図した範囲に本当に限定されるか**（**セキュリティ主張の本体。省略不可**）
  → 権限を付与していない別サイトに同じ操作をして **403 になること**を確認する。
  → 根拠：`Files.SelectedOperations.Selected` で**権限を付けていないファイルまでDLできた**という報告があり、
    **MSスタッフとMVPの双方が「想定外」と認めたまま解決未確認**（2025-01）。
  → **救い**：delegated は「アプリ権限 ∩ 本人権限」なので、**最悪でも中村個人の権限を超えない**（テナント全体には広がらない）。
- □ **拒否が 403 か「200＋空」か**を記録し、**空を成功扱いしない実装**にする
  → 権限不足が**空レスポンス**で返る報告が複数。「空＝変更なし」と実装すると**変更検知が静かに死ぬ**。
- □ **`Sites.Selected` で `createUploadSession` 経路が通るか → 実録画（250MB超）で確認する**
  → **境界は250MB**（＝単純 PUT の上限。これを超えると `createUploadSession` になる）。1時間の録画は**約400MB**なので必ず通る経路。
  → **⚠️ 小さいファイルで通ってもスキップしないこと。**
    2021年に Selected で「**小さいファイルは通るが、単純PUTの上限を超えると落ちる**」バグの実績がある
    （当時の上限は4MB。MSが修正を認め、修正報告あり）。**「小はOK＝大もOK」が成り立たない前例がある**のが要点。
  → 実装要件（公式）：**各チャンクは60MiB未満・320KiBの倍数・順次アップロード**（推奨5〜10MiB）。
  → 中断した場合は **`DELETE {uploadUrl}`** で明示的にキャンセルする（残骸が残る）。
- 🔺 **条件付きアクセスの IP制限 → 今回の検証条件ではブロックされなかった**（2026-07-15）
  → **VPSからサインインし、Graphが200を返した。** VPSのIPを弾く条件付きアクセスがこの条件に効いていれば
    この時点で失敗していたはず。**送信元IP `133.125.62.159` は確定**（`ipaddr`・2026-07-16）。
  → **⚠️ 「クリア」と書いてはいけない（2026-07-16 訂正）。** 条件付きアクセスは
    **ユーザー／アプリ／リソース／場所／リスク／report-only** の組で評価される。
    **今回通ったのは「この user × このアプリ × Graph(`/me/drive`)」の組み合わせにすぎない。**
    **`Sites.Selected` 付与後や SharePoint リソースへの操作で別のポリシーが当たる可能性は消えていない。**
  → **最終確認は今井さん（Sign-in logs / What If ツール）で行う**（§7-2）。
- 🔺 **条件付きアクセスの「サインイン頻度」→ 短い設定は潰せた。長い設定は未確認**（**アプリ側で回避不可**）
  - **2026-07-15 16:44 JST に refresh_token を保存 → 2026-07-16 07:55 JST に更新を実行 → ✅ 成功**
    （経過 **15時間11分**。`iat=1784156115`／新しい `refresh_token` も返った）
  - **→ 結論：数時間〜15時間レベルのサインイン頻度は設定されていない。**
    `AADSTS50076` / `AADSTS50078` / `interaction_required` はいずれも返らなかった。
  - **⚠️ 残る限界**：**「1日」「7日」「30日」等の長い設定は、この15時間の検証では見抜けない。**
    → **今井さんへの確認事項として残す**（§7-2）。**なくなっていない。**
  - **副産物（実測で確定）**：
    - **テナントID `85c9340e-a7a2-48cb-97cf-ca56a6e44875`**（`iss` より）
    - **VPSのグローバルIP `133.125.62.159`**（`ipaddr` より＝**Entraから見えている実際のIP**）
      → **今井さんへのIP制限の確認依頼に、このIPを添えて聞ける。**
    - **`amr: ["pwd","mfa"]`** → **今回のサインインは パスワード＋MFA**。
      → **このRTはパスワードベースに該当＝パスワード変更/SSPRで失効する**（→ §8.4）。
      ※「アカウントがパスワードレス非対応」の意味ではない。**パスワードレスで入り直せば塞げる可能性あり（要確認）**。
    - アクセストークン寿命 **4006秒(67分)**。前日は 4320秒(72分) → **固定値でないことを2回の実測で確認**
    - `scope` に `offline_access` は載らない（**正常**。refresh_token を返させる指示であって権限ではない）
  - ※`~/.minutes-verify/rt` は**中村本人と同等の資格情報**。VPSは全員が同じ `aiocr` アカウントなので、
    **同じアカウントで入れる人からは読める**。**同一トークンの3回連続使用が成功した**ことで、
    「コピーされたら終わり」が実測で裏づけられた（§8.4）。検証用の一時的な妥協であり、**終わったら必ず消す**。
- ✅ ~~mp4 がファイル種別でブロックされないか~~ → **確認不要**。公式に「**There are currently no known types of
  files that cannot be added**」＝**SharePoint Online にブロック拡張子は存在しない**。
  （管理者が追加できるブロック設定は「**同期クライアント経由のアップロード**」のみが対象で、Graph には効かない）
- □ **⚠️ 保持ポリシー（Purview）がこのサイトに適用されるか** ← **今井さんへの確認事項。容量解放と削除に直結**（§10.5）
  → 経験的な検出法：ファイルを削除したあと「サイト コンテンツ」に **`Preservation Hold Library`** が現れれば適用されている
- □ **ライブラリのバージョン履歴の既定設定**（既定500世代。同名上書きしない設計なら実害なしだが、確認しておく）
- ✅ ~~大ファイル（250MB超）は `createUploadSession`~~ → **上の項目に統合**（2026-07-16・重複と閾値の食い違いを解消）。
  ※「4MB超で `createUploadSession` が必要」は**誤り**。**単純 PUT の上限は250MB**。
- ✅ ~~中村自身でSharePointサイトを新設できるか~~ → **できた**（2026-07-15・`/sites/minutes` を作成済み）。
  → **今井さんへの依頼から「サイト新設」が落ちた。**
- ✅ ~~ライブラリの OneDrive 同期が使えるか~~ → **確認済み**（ショートカット追加・VS Code編集・移動すべて動作）
- ✅ ~~同期経由のフォルダ間移動で item-id が保持されるか~~ → **保持された**（`UniqueId` で確認・上記）
  → ただし **Graph の `item-id` でも同じか**は `Sites.Selected` 付与後に再確認する
- □ `.md` を**ブラウザで**編集できるか（公式に「編集可」の記述なし。**同期で解決済みなので優先度は低い**）
- ✅ ~~フォルダ単位の権限分離ができるか~~ → **✗ 検証不要（2026-07-16・不採用にしたため／§7-4・§10.5）**
  → **「サイトのメンバー＝Backlogに登録してよい人だけ」を前提にすれば、分ける対象がいない。**
  → **⚠️ 代わりの運用前提**：メンバー追加時に必ず「この人は登録してよいか」を確認する。
    **「置くだけの人」を入れた時点で前提が崩れる** → そのとき改めてこの検証をする。
- □ アップロード途中の検知（`pendingOperations` が立つか／`size`・`cTag` 安定化待ちで代替できるか）
- □ `quickXorHash` が返るか（"if available" のため保証なし）

---

## 9. 次アクション

1. ~~小見山さんへ相談~~ → **✅ 完了（2026-07-15・暫定承認）**
2. ~~自身のアカウントでの実機検証（Graphの作法・トークン運用）~~ → **✅ 完了（2026-07-15・§8.6）**
   `Files.ReadWrite` ＋ 自分のOneDriveで、device code flow・refresh・一覧/DL/書き戻し が成立。
   **今井さんの手を借りずに到達。** ただし **`Sites.Selected` ＋ 専用サイトは未検証**。
3. **今井さんへ依頼**（次の一手）：§7-3。**依頼は2件に減った**（サイト新設・アプリ登録・IP確認の一部は自力で完了）。
   - ~~専用SharePointサイトの新設~~ → **✅ 中村が作成済み**（`/sites/minutes`）
   - **① そのサイトへの `Sites.Selected` 付与（roles: `write`）** ＋
     **⚠️ 中村を、その専用サイトの読み書きできるメンバーに追加**（忘れると動かない・§5）
   - **② 条件付きアクセスの確認** ← **不可なら方針ごと組み直し**
     - **サインイン頻度の「1日／7日／30日」等の長い設定**（短い設定は実測で否定済み・§8.6）
     - **IP制限**：送信元IPは **`133.125.62.159`** と確定済み。**Sign-in logs / What If** での確認を依頼する
   - ※**アプリのリネームを先に済ませること**（現在 `minutes-automation-test`。依頼書に載る名前になる）
4. **`Sites.Selected` での再検証**：§8.6 の「残る確認事項」。
   **⚠️ 先に §8.6 の「検証の前提条件」を満たすこと**（`Files.ReadWrite` の同意を外すか別アプリを使う。
   でないと**Selectedで通ったのか区別できず、ネガティブテストが素通りする**）。特に
   **① 一覧/DL/書き戻しが Selected で通るか（drive経路で。lists経路は403報告あり）**、
   **② ネガティブテスト（範囲外が403になること）**、**③ 実録画(250MB超)のアップロード**。
5. **実装**：§10。**処理コアは無改修**（`draft` の `--out`/`--vtt`/`--fid`/`--link`、
   `register` の `--md`/`--dry-run`/`--force` は**既に実装済み**。ingest から呼ぶだけで足りる）。
6. 当面は **Phase 1（手動SSH＋CLI）で運用継続**。設計書 §6/§7 は本整理に合わせて改訂予定。

---

## 10. 実装方針（Phase 2・案B／承認＋実機確認後に着手）

### 10.1 基本方針
既存の**処理コア（src/ 一式）は無改修**。「取り込み係」を1つ足し、録画・字幕を `inbox/` に置いて
既存の draft を呼ぶだけ。ツール依存は**フォルダ内で自己完結**（`msal` も flake に追加・グローバル非依存）。

### 10.2 追加モジュール
| ファイル | 役割 |
|---|---|
| `src/graph.py`（新規） | MSALで**delegated**認証（トークンキャッシュ永続化）＋投入ライブラリの列挙・DL・書き戻し |
| `src/ingest.py`（新規） | 新規会議フォルダの検知→DL→draft起動→書き戻し→台帳更新（冪等） |
| `src/config.py`（追記） | `GRAPH_TENANT_ID`/`GRAPH_CLIENT_ID`/`SP_SITE_ID`/**`SP_DRIVE_ID`** を `minutes.env` から。<br>**⚠️ `SP_LIST_ID` は持たない**（`/sites/{siteId}/lists/…` 経路は403報告があるため使わない。§8.6） |
| `flake.nix`（追記） | `msal` を devshell に追加 |
| 初回サインイン用スクリプト（新規） | device code flow で1回だけサインインし、トークンキャッシュを作る |

### 10.3 認証（delegated／無人実行）
- MSAL の **`PublicClientApplication`** ＋ **device code flow** で初回サインイン。
  スコープに **`offline_access` を明示**（v2.0エンドポイントでは明示しないと refresh token が返らない）。
- authority は**自社テナントを指定する**（社内テナント固定の用途なので自然。かつどの記述に従っても安全）。
  ※`/common`・`/consumers` の可否は**公式内で記述が割れている**（プロトコル仕様は「Can be `/common`, `/consumers`,
  or `/organizations`」と許容、MSAL.NETのConstraintsは "authority needs to be **Tenanted**" と記載。
  同ページ内にも「MSAL.NET 4.5 以降は `/common`・`/consumers` でも可」とあり自己矛盾）。
  いずれにせよテナント指定なら問題にならないので、深追いしない。
- アプリ登録で **「Allow public client flows」= Yes** が必要。**リダイレクトURIを `spa` で登録しない**
  （`spa` だと refresh token が24時間で失効する）。
- 2回目以降は `acquire_token_silent()`。**`None` が返ったら＝再サインインが必要**なので**アラートを飛ばす**
  （自動復旧は不可・§8.4）。CAEの claim challenge に備え `claims_challenge` を渡せるようにする。
- **トークンキャッシュの永続化**：`SerializableTokenCache` を使う。
  **⚠️ 公式レシピの `XDG_RUNTIME_DIR` をそのまま使わない**（「SSHセッション終了でキャッシュを消す」ためのパスであり、
  常駐バッチでは毎回サインインを要求されて破綻する）。**ホーム内の永続パス**に置く。
  **ファイルロック必須**（実行が重なる可能性があるため）。

### 10.4 取り込み（投入ライブラリのポーリング）
- 監視対象は**専用ドキュメントライブラリ（＝1つのdrive）のルート**。→ `/root/delta` でよい（§8.1の制限に抵触しない）。
- **⚠️ delta は自分の書き込みも返す（自己反応ループの原因）**：ライブラリrootのdeltaには `out/`・`processed/`・
  `done/`・`error/` への書き戻し・退避が**すべて変更として現れる**。→ **処理対象を
  `inbox/`（＝取り込み）と `approved/`（＝登録）の2つだけに限定**し、
  **`out/`・`processed/`・`done/`・`error/` は明示的に無視**する。
  これがないと、自分の出力が次の起動で入力に見えて回り続ける。
- **⚠️ `children` 列挙で代替してはいけない**（2026-07-15 調査）：公式に
  「Other approaches, such as paging through the `children` collection of a folder, **are not guaranteed to
  return every single item if any writes take place during the enumeration**. Using `delta` is the only way to
  guarantee that you've read all of the data you need to.」と明記。**頻度の問題ではなく方式の保証の問題**。
  → **delta を使う。** ただし **delegated × `Sites.Selected` × delta の実績報告はゼロ**（§8.6）なので、
    **実機で通ることを確認するまでは、children 列挙をフォールバックとして用意しておく**（取りこぼし対策に
    安定化待ち＋再列挙を併用）。
- **⚠️ delta は `cTag` を返さない**（ODfB・公式明記。Create/Modify で `ctag`、Delete で `ctag`/`name` が省略される）。
  → **`cTag` が要るなら個別に GET し直す。**
- **⚠️ `pendingOperations` があるアイテムはスキップする**：アップロード途中で内容が確定していない可能性。
  ただし「アップロード途中は必ず立つ」とは公式に書かれていないので、**`size`/`cTag` の安定化待ちと併用**。
- **⚠️ 判定は「即時の親」ではなく「祖先」で行う**：`inbox/` は**1会議1フォルダ**なので、`recording.mp4` の
  `parentReference` は `inbox/` ではなく **`inbox/2026-07-22_定例/`** を指す。
  即時親だけで判定すると**会議フォルダ内の変更を全部落とす**。
  → **「祖先に `inbox/` を含むか」で判定**する（または `inbox/` 直下フォルダを起点に children を再列挙）。
  `approved/` は直下にmdを置く前提なので即時親でよい。
- **⚠️ 初回は `?token=latest` を使わない**：`token=latest` は「以後の変更だけ見る」意味なので、
  **既に置かれている未処理のものを全部見落とす**。→ **初回は `inbox/` と `approved/` の両方を列挙して台帳を作り**、
  そのとき得た deltaLink 以降を差分同期する。（`token=latest` は「既存を無視してよい」と決めた場合のみ使う）
- **`410 Gone` を受けたら resync**：deltaLinkを捨てて **`inbox/` と `approved/` の両方を再列挙**し、
  **台帳と突合**して未処理だけ拾う（主キーは `(drive-id, item-id)`・再処理判定は `cTag` なので二重処理にならない）。
  - **⚠️ `approved/` を再列挙対象から外さないこと**：VPSが停止している間に人が承認（`out/`→`approved/` へ移動）した
    場合、delta の差分だけを見ていると**その承認を永久に見落とす**。
- **item-id で追跡**（パス・ファイル名に依存しない。Teams録画のファイル名規則は公式に文書化されていない）。
  **同じitemが複数回出たら最後の出現を採用。**
- **スロットリング**：429は `Retry-After` 最優先。UA装飾 `NONISV|株式会社ティー・シー・エス|minutes-automation/1.0` を付ける。

### 10.5 投入の単位とペアリング（1会議1フォルダ）
```
議事録投入/  （＝専用ドキュメントライブラリ。ここだけにアプリの権限を絞る）
  inbox/                        ← 【処理対象】
    2026-07-22_定例/            ← 担当者がフォルダを1つ作って、中に3点を置く
      recording.mp4
      transcript.vtt
      metadata.txt              ← 録画リンク(CybozuのfFID)・会議名。1行1項目
  out/                          ← 【無視】下書き＋事前確認レポートが出てくる（人がチェック）
      minutes_2026-07-22_定例.md
      minutes_2026-07-22_定例.check.txt   ← dry-run相当（重複・番号衝突・登録先）
  approved/                     ← 【処理対象】人がチェック済みmdをここへ移動＝承認
  done/                         ← 【無視】登録済みの結果
  processed/                    ← 【無視】取り込み済みの入力が退避される
  error/                        ← 【無視】失敗したものと理由
```
- **1会議1フォルダにすることで、mp4↔vtt のペアリング・出力名の一意化・録画リンクの受け渡しが同時に解決する**
  （ファイル名パースに依存しない）。

**ストレージの保持と削除（2026-07-15 調査・※当初の記述は誇張だったので訂正）**
- **課金なし**：Graphの通常のCRUD（一覧・DL・アップロード）は**課金対象外**（公式の metered API 一覧に無い）。
  **サイト新設にも課金なし。** 15分ポーリング（月約3000回）でも同じ。
- **⚠️ 訂正：容量そのものは問題にならない。** 当初「`processed/` に残し続ける設計は破綻する」と書いたが**誇張**だった。
  テナント容量は **1TB ＋ ライセンスごと10GB**（公式）。**年20GB は1TBに対して50年分**。
  ※例外：**F1/F3 プランは 1TB 固定**（10GB/ライセンスの加算なし）。それでも50年分。
- **★真のリスク1：バージョン履歴**。ライブラリは**既定で500世代**保持する。
  **同名で上書きすると 400MB × 500世代 ＝ 理論上200GB が1ファイルで積み上がる。**
  → **現設計（1会議1フォルダ・会議日時入りの名前）なら同名上書きが起きないので回避できている。**
    **「上書きしない」を設計の要件として維持すること。**
  → 公式も「バージョン上限を絞る」より安全な回避策として、設計で避けるのが筋。
- **★真のリスク2：保持ポリシー（Preservation Hold library）**。**最大の設計リスク。**
  全社に「SharePointサイトすべて」の保持ポリシーが掛かっていると、**削除してもコピーが
  Preservation Hold library に残り、容量が一切解放されない**（公式：「PHLはサイトの容量枠に含まれる」）。
  解放まで**最短でも30〜37日＋93日**。しかも**保持ポリシー配下ではバージョン履歴の上限設定も無視される**。
  → **新設サイトに保持ポリシーが適用されるかを Purview で確認**（今井さんへの確認事項・§7-3）。
- **削除の実装**：**`DELETE` だけでは容量は解放されない**（93日ごみ箱行き）。**ごみ箱も容量を消費する**（公式明記）。
  即時解放は **`POST /drives/{id}/items/{id}/permanentDelete`**（v1.0 GA・**復元不可**）。
  → **容量に余裕がある以上、通常の `DELETE` で十分。** `permanentDelete` は容量が逼迫した場合の手段。
- **`processed/` の保持期間**：容量のためではなく、**「品質問題が後から発覚したときの再処理窓」**として決める。
  録画の原本はCybozu・渡邉さんのOneDriveにもあるので、**30日程度で十分**（常時1.6GB程度にしかならない）。
- **`507 Insufficient Storage`** の扱いは実装に入れる（ただし `fileSize` の事前指定は OneDrive personal 専用との
  注記があり、**SharePointで事前検知できるかは不明**。最終チャンク投入時のエラーとして現れうる）。
- **揃い待ち**：`metadata.txt` の存在を「投入完了」の合図とする（**最後に置く**運用）。
  **ただしこれだけでは足りない**。次を全部満たして初めて処理を開始する：
  - **mp4が1個・vttが1個・metadata.txtが1個**であること（0個/複数個は `error/` に理由を出す）
  - `metadata.txt` の**必須項目（録画リンクのfFID）が読めること**
  - **`size` と `cTag` が連続2回のポーリングで変化しないこと**（アップロード途中・ブラウザupload遅延を弾く）
  - 想定される事故：**metadata.txt を先に置かれる**／mp4だけ置かれる／後から同名で差し替えられる／
    複数のmp4が入る。→ 揃わないまま一定時間が過ぎたら `error/` に出す。
    差し替え（`cTag` 変化）は**再処理扱い**にする。
- **`--out` は会議ごとに一意化**：既定の `out/minutes.md` のままだと、次の下書きが**人のレビュー前に上書き**される
  （flockは同時実行しか防げない）。→ `out/minutes_<フォルダ名>.md` とする。**これは必須修正。**
- **`--fid` は Cybozuの数字fFID専用**。`metadata.txt` から読んで渡す。Graphのidは渡さない。
- ingest は **`python -m src.main draft <mp4> --vtt <vtt> --out ... --fid ...` を subprocess で直接呼ぶ**
  （`run.sh` は対話・inbox単一ファイル前提なので自動取込には使わない。手動用に存置）。

**承認と登録（`approved/` 方式・§7-4で決定）**
- 下書き生成時に、`out/` へ **md ＋ 事前確認レポート（`.check.txt`）** の2点を書き出す。
  レポートは**既存の register の dry-run 相当を呼んで結果を書き戻す**（重複チェック `find_duplicate` ・
  番号衝突 `number_conflict` ・登録先）。※VPS版の実体は `vps/minutes/src/backlog.py`。
- **人が md を修正 → `out/` から `approved/` へ移動**＝承認行為（`y` 押下の代わり）。
- **⚠️ `.check.txt` は人がmdを編集した時点で陳腐化する**：タイトル・第N回・日付・録画リンクを直せば、
  重複チェックも番号衝突も結果が変わりうる。→ **`.check.txt` に対象mdの `item-id` と `cTag` を記録**し、
  **`approved/` 到着時に `cTag` が変わっていたら dry-run を再実行**する。
  **register 直前の dry-run 結果を `done/`（または `error/`）に必ず残す**。
  → 「レポートを見てから移す」だけでは `--dry-run → y` と完全に同じ強度にはならない（§7-4）。
- **⚠️ `approved/` でも安定化待ちが要る**：人がSharePoint上でmdを編集してから移動すると、保存・同期・移動の
  イベントが分かれて飛びうる。→ `size`・`cTag` が**連続2回のポーリングで変化しないこと**を確認してから register。
- **register の前提条件**（満たさなければ `error/` へ）：
  - 拡張子が `.md` であること（`.check.txt` や無関係なファイルが入ってもトリガしない）
  - **台帳に対応する下書き（`drafted`）が存在すること**（別の場所から持ち込まれたmdを登録しない）
- **⚠️ 二重登録の防止（多層）**：
  1. **移動しても item-id は不変**（§8.5）→ 台帳で同一ファイルと判別し、**`registered` の item-id は再実行しない**。
  2. **⚠️ 「コピー」されると item-id が変わり、1が効かない**。→ **タイトル＋第N回＋本文ハッシュ**でも突合する。
  3. **⚠️ 台帳更新前にPOSTが成功したケース**（POST成功直後にVPSが落ちた／`done/` 移動に失敗した／
     応答だけタイムアウトした）では1も効かない。→ **POST後の不明状態は `unknown_after_post` として隔離し、
     自動再POSTを禁止**。**Backlog側を再照会して存在確認してから**しか先に進めない（§10.7）。
  4. register 側の `find_duplicate` が最後の網。
- 登録後は `done/` へ移動し、結果（BacklogのドキュメントID・URL）を併記。
  **⚠️ Backlog登録は成功したが `done/` 移動に失敗した場合**、mdは `approved/` に残る。
  → 台帳優先で**再登録は禁止**しつつ、**後処理（`done/` 移動）だけを再試行**する状態を持つ。
- **⚠️ 台帳を唯一の根拠にしない**：ローカル台帳が壊れる/消えると `registered` の根拠が失われる。
  → **BacklogのドキュメントIDを `done/` の結果ファイルにも残す**（台帳喪失時はここから復旧できる）。

**普段使いの形（2026-07-16 実測で確定・★エクスプローラーだけで完結する）**

**★ライブラリ全体を「OneDriveへのショートカットの追加」で追加する**（公式が「同期」ボタンより推奨）。
→ エクスプローラーに `C:\Users\<user>\OneDrive - 株式会社ティー・シー・エス\議事録投入 - ドキュメント\` として現れ、
  **`inbox/` `out/` `approved/` `done/` `processed/` `error/` が全部見える**（2026-07-16 実測）。

```
1. Teamsから録画と字幕をDL                          … Teams
2. inbox\<会議フォルダ>\ を作って mp4/vtt/metadata を放り込む … エクスプローラー
3. 15分待つ                                          … （何もしない）
4. out\minutes_<会議>.md が現れる → 開いて直す        … エディタ
5. approved\ へドラッグ → Backlog登録                 … エクスプローラー
```

**→ SharePointのWeb画面もSSHもコマンドも出てこない。** 触るのは **Teams・エクスプローラー・エディタ**だけ。

**⚠️ 2026-07-15 の記述を訂正（2026-07-16）**
~~**`out/` だけを個別に追加する**。**`inbox/` は追加しない**（400MBのmp4をエクスプローラーに露出させないため）。~~
→ **これだと「置くときはブラウザ、チェックはエクスプローラー」になり、2つのやり方を覚えることになる**（中村さんの指摘）。
→ **ファイルオンデマンドが有効なら、開かない限り実体は落ちてこない**（0バイトのプレースホルダー）。
  **そして置く人はそのmp4を手元に持っている**（Teamsから落としたばかり）ので、**ドラッグするのが一番自然**。
→ **慎重に倒しすぎていた。ライブラリ全体を追加する。**

**実測で確認したこと（2026-07-16）**
| 項目 | 結果 |
|---|---|
| **ライブラリ全体のショートカット追加** | ✅ `inbox` `out` `processed` `error` が全部エクスプローラーに出る |
| **エクスプローラーでフォルダを新規作成**（`approved` `done`） | ✅ **SharePoint側にも反映された** |
| **★`.mp4` を `inbox\` へドラッグ** | ✅ **通った。同期クライアント経由の拡張子ブロックは無い** |
| **★200MB の mp4 の同期** | ✅ **通った** |

**→ 公式に「管理者が追加できるブロック設定は『同期クライアント経由のアップロード』のみが対象」とあるため、
  mp4 が同期経由だけ弾かれる可能性があった。→ 実測で否定された。**

**⚠️ 気づいた点**：ショートカットのフォルダ名が **`議事録投入`**（旧サイト名）のまま。**現在のサイト名は `議事録作成`。**
→ **サイト名を変えてもショートカット名は追随しない。** 実害はないが、他の人に配ると「議事録投入とは？」となる。

**サイズについて（2026-07-16 実測）**：**200MB の mp4 が同期クライアントで通った。**
1時間の録画は約400MB（Teams公式）なので倍の余地はあるが、**200MB が通って400MBで落ちる理由は考えにくい**。
→ **人が置く分には、サイズは論点にならない。**

**⚠️ ただし Graph 側は別**：**VPSが書き戻す経路の 250MB 超（`createUploadSession`）は未検証**。
`createUploadSession` の250MB制限は **Graph API の話であって同期クライアントには関係ない**。
下書きの `.md` は小さいので実害は薄いが、**「小は通るが大が落ちる」前例がある**ため確認事項として残す（§8.6）。

**⚠️ 後始末**：検証で作った **`inbox/test/`（テスト用フォルダとmp4）を削除する。**
**残したまま実装すると `ingest` が処理しようとする。**
- **`.md` をブラウザで直接編集できるという公式記述は無い**（プレビューは対応）。→ 同期が本命。
  同期が使えない環境では「ダウンロード → 編集 → アップロード」（手間は増えるがSSHは不要のまま）。
- **VPS側は同期クライアントを使わない**（**Linux向けの公式クライアントは存在しない**）。Graph API 直叩きで正しい。

**⚠️ ファイル名のサニタイズ（必須）**
- **使用不可文字**：`" * : < > ? / \ |` および**前後の空白**。
- **使用不可名**：`.lock` / `CON` / `PRN` / `AUX` / `NUL` / `COM0`-`COM9` / `LPT0`-`LPT9` / `_vti_` / `desktop.ini` / `~$` 始まり。
- **パス全体（ファイル名込み）は400文字まで**。→ `processed/` の階層と会議名の組合せに注意。
- → **会議名から機械的にファイル名を作ると踏む。** サニタイズを実装に入れる。
- ※`.tmp` は同期対象外（公式）＝エディタの一時ファイルはクラウドに上がらない。これは好都合。

**⚠️ 競合コピーの検出**
複数人が同じライブラリを同期していると、**`議事録_2026-07-15-DESKTOP-ABC123.md` のようなPC名付きの
競合コピー**が生まれる（OneDriveの競合解決の仕様）。**`approved/` にこれが出現したときに登録してしまわないよう、
検出して弾く（または警告する）処理を入れる。**

**⚠️ 同期経由の削除は全員に伝播する**
一人がエクスプローラーで `out/` を削除すると、**同期している全員の端末から消え、SharePoint側からも消える**（公式）。
→ 大量削除の確認ポリシー（`ForcedLocalMassDeleteDetection`）の有効化を検討。

**~~⚠️ SharePoint側の権限分離（承認を「移動」にするなら必須）~~ → ✗ 不採用（2026-07-16・§7-4）**

~~フォルダ移動を承認行為にする以上、誰がどのフォルダに書けるかまで設計しないと安全性を評価できない。
`inbox/`＝投入者／`out/`＝編集者／`approved/`＝**登録してよい人だけ**／`done/` `error/` `processed/`＝botのみ。~~

**→ 採用しない。** **「サイトのメンバー＝Backlogに登録してよい人だけ」を前提にすれば、分ける対象がいない。**
**権限分離は「権限のない人の登録」には効くが、「権限のある人のうっかり移動」には無力**であり、
心配していたのは後者だった（＝理由付けが噛み合っていなかった）。詳細と訂正の経緯は §7-4。

**⚠️ 代わりに固定される運用の前提**：
**サイトにメンバーを追加するときは、必ず「この人はBacklogに登録してよいか」を確認する。**
**「置くだけの人」を1人でも入れた時点で、この前提は崩れる。** そのときに権限分離を検討する（設計を先回りしない）。

### 10.6 定期実行
- `systemd --user` タイマーで **15分ごと**に ingest（**インバウンド公開不要**）。
  ログインしていない時間も動かすため **`loginctl enable-linger <user>`** が必要。
- service に `WorkingDirectory`・`EnvironmentFile`・`ExecStart=nix develop -c python -m src.ingest` 相当、
  timer に `Persistent=true`。多重起動は systemd ＋ アプリ側 flock で抑止。

### 10.7 状態管理・冪等性・秘密
- **段階状態**：`discovered → downloading → downloaded → drafting → drafted`
  `→ approved → registering → registered / failed`
  ＋ **`unknown_after_post`**（POSTしたが成功/失敗が確定できない）
  ＋ **`registered_pending_move`**（Backlog登録は成功したが `done/` への移動が未完）。
  会議ごとに `attempts`・`last_error_code`・`next_retry_at`・保存パス・`output_md`・`backlog_doc_id`・
  **`post_attempt_id`・本文ハッシュ**を持つ。
- **`registered` に到達した item-id は二度と register しない**（§10.5の二重登録防止）。
- **⚠️ `unknown_after_post` は自動再POST禁止**：**Backlog側を再照会して存在を確認**し、
  あれば `registered` へ、無ければ人の確認を経てから再試行する。**APIで取り消せない**ため、
  「とりあえずリトライ」は絶対にしない（§7-4）。
- **`registered_pending_move` は後処理（`done/` 移動）だけを再試行**する。**再登録はしない。**
- **⚠️ 状態は VPS ローカルの台帳に持つ。SharePoint 側に状態ファイルを置かない。**
  置くと**自分の書き込みが delta に出て `cTag` を汚し、自己反応ループになる**（§10.4）。
  ローカル台帳の書き込みは**一時ファイル→atomic rename**（これはローカルだから成立する）。
- **⚠️ SharePointへの書き戻しは atomic rename にできない。** `out/` へは `.tmp` 付きの名前でアップロードし、
  完了後に rename/move する。人は `.tmp` を無視する運用にする（書き込み途中の下書きをレビューされないように）。
- **台帳の主キーは `(drive-id, item-id)`、再処理判定は `cTag`**（§8.5）。成功記録だけの `processed.json` にしない
  （部分失敗を区別できるように）。
- **同時実行**：既存の flock。
- **秘密（重要）**：**バッチ専用OSユーザー `minutes-bot` を作り**、トークンキャッシュをそのユーザー所有 `0600` で置く。
  共有 `aiocr` からは触れない構成にする（`sudo` 経由なら監査証跡も残る）。NixOSの宣言的構成内で完結。
  ※これは**アクセス制御**であって暗号化ではない。公式要件（access control **and** encryption）を完全に満たすとは言えず、
  「共有 `aiocr` より改善する」という位置づけ。バックアップ対象からの除外も検討する。

### 10.8 失敗・遅延の扱い
- **⚠️ 失敗を4種に分ける**（全部 `error/` に放り込むと、再投入・再承認・手動復旧の判断を誤る）：
  1. **一時的**（429 / 5xx / DL失敗）：`Retry-After` を尊重してリトライ。`error/` には出さない
  2. **恒久的**（403 / スコープ不足 / トークン失効 / 投入不備）：`error/` に理由を出し、**アラート**
  3. **登録成功か不明**（`unknown_after_post`）：**`error/` に入れない。隔離して人の確認を待つ。**
     **自動再POST禁止**（§10.7）
  4. **登録成功後の後処理失敗**（`registered_pending_move`）：後処理だけ再試行。**再登録しない**
- **トークン失効**：`acquire_token_silent()` が `None` → **人の再サインインが必要**。手順書化しておく。
- **`out/` の運用事故**：担当者が出力を消す／再投入する／古い下書きを見て登録する、が起こりうる。
  出力名に会議フォルダ名を含め、`processed/` への退避で「どれが処理済みか」を可視化する。
- **ログに出さない**：Authorization ヘッダ・`@microsoft.graph.downloadUrl`（事前認証済み）・トークン応答。
  ログは request-id・HTTP status・Graph `innerError.code`・item-id短縮 のみ。

### 10.9 着手順（作りながら検証）
1. **条件付きアクセスの確認**（§7-2）→ 不可なら中止
2. アプリ登録 → **device code flow でサインインでき、トークンキャッシュが永続する**（最小疎通）
3. **Selected スコープで投入ライブラリの `/root/delta` が通るか**（§8.2の最重要確認点）→ ダメならスコープを見直し
4. mp4のダウンロード（`downloadUrl` ＋ 206/200判定）
5. `ingest.py`：1会議フォルダを検知 → draft起動 → `out/` へ md ＋ `.check.txt` を書き戻し（`--out` 一意化）
6. `approved/` 監視 → register 実行 → `done/` へ（**二重登録防止の台帳判定を先に通す**）
7. タイマー＋linger で自動化 → **手動運用（Phase 1）と並行**で安定確認 → 主運用へ
