# 議事録自動化 サーバー化（VPS）設計書  v0.8（ドラフト）

作成日：2026-07-13 ／ 改訂：2026-07-16（入口の全面改訂） ／ 状態：レビュー中（未決事項は「14. 要決定・依存」に集約）

> **⚠️ v0.8 で入口（§6）と取り込み（§7）を全面的に差し替えた。**
> **v0.7 までの「入口＝B2（PC→VPSのHTTPS/443・Caddy・独自トークン）」は不採用。**
> 現在の方針は **専用SharePointサイト ＋ Microsoft Graph（delegated・`Sites.Selected`）＋ VPSからのポーリング**。
> **決定の経緯・根拠・実機検証の結果は `議事録自動化_フロー整理.md` が一次情報**（本書はその結論を設計に落としたもの）。
> 両者が食い違う場合は**フロー整理が正**。

このドキュメントは、現在PC上で半自動運用している議事録自動化を、AI-OCR用VPS（NixOS）上の社内サービスへ発展させるための設計方針をまとめたものです。実装前の合意形成用ドラフトであり、未決の論点は選択肢と推奨を併記しています。

変更履歴
- v0.1 初版。
- v0.2 Codexレビュー反映：出口とチェックの整合（8章）、ジョブ・状態/冪等性（9章）、無料枠運用（10章）、トークン上限対策（11章）を新設。入口セキュリティ（6章）・秘密情報管理（12章）・常駐運用（5章）を強化。Step1の適用範囲を限定（13章）。
- v0.3 文字起こし方針を確定：勉強会は月最大5回のため、文字起こしは毎回さくらのWhisper（`whisper-large-v3-turbo`）を使用（無料枠50回/月に十分収まる）。ローカルwhisper.cppは不要化。話者名はTeams .vttを併用。
- v0.4 さくらWhisper API仕様を反映（公式ナレッジ確認）：エンドポイント/認証/レスポンス（textのみ）。**1リクエスト上限=30分/30MB**のため45分超の会議は事前分割（ffmpeg）→複数回リクエスト→text結合、を設計に明記。
- v0.5 Codexレビュー(v0.4)反映：文字起こしの分割・結合の実装要件を新設（3.1章：サイズ/長さ検証・無音検出条件・オーバーラップ重複除去・順序結合）。話者割当を「VTT主・Whisper補正」に明確化（3・4章）。チャンク単位の冪等性/リトライ（9章）、クォータ最悪ケース見積り・月次リセット基準（10章）、「外部送信なし」表現の是正（3・12章）。版番号・次アクションをv0.5へ整合。
- v0.6 入口を確定（6・14章）：他メンバーも使う前提＋全員エンジニアのため、本番入口=B2（PC→VPSのHTTPS/443・トークン+TLS認証・Web UIなし）、A(SSH)=管理者の検証用のみ。認証はトークン+TLS必須、IP制限は任意追加（オフィス限定確定時）。Backlog Webhook=出口通知の位置づけを明記。
- v0.6.1 Codexレビュー(v0.6)反映：取り込み(7章)をB2に一体化して確定、認証を利用者ごと発行トークン+ハッシュ保存+失効運用に、非同期ジョブの実行制御(同時実行/キュー/重複投入/クォータ)を追記(6章)、Caddy代替案の不採用理由を明記(6章)、証明書方式をStep3の前提条件化(6章)、グローバル非依存の443例外を12章にも明記、版ズレ(15章)修正。
- v0.6.2 Codexレビュー(v0.6.1)反映：段階番号を§13 Stepに統一(Caddy例外=Step3/CLI検証=Step2。6・12章)、§6の重複した旧入口表を削除、クォータ不足時の方針を「受付拒否+通知(月跨ぎ保留なし)」で6・10章一致、ジョブ状態に queued/rejected を追加(9章)、秘密情報を「システム秘密=環境変数／利用者トークン=ハッシュ台帳」に分離(12章)、server.py/常駐の記述からフォルダ監視(C不採用)を除去、版ズレ(15章)修正。
- v0.7 Step1(draft.ps1=③のAPI化)を実装・実データ検証済みに更新(13章)。草案LLMを`gpt-oss-120b`で確定(14章。llm-jpは長さ400/PLaMo・cotomi未開放/gemmaは日付誤生成)。PS5.1のInvoke-RestMethod応答文字化けの知見を追記。トークン疎通・日本語往復・実生成すべて確認済み。
- **v0.8 入口を全面改訂（6・7章）。上長助言「まずアプリ全体のフローを整理し、その上で認証認可を考える」に沿って
  `議事録自動化_フロー整理.md` で整理し直した結果、v0.7 の前提が崩れたため。**
  - **入口＝専用SharePointサイト**（`/sites/minutes`）に人がmp4/vttを置く → **VPSが15分ごとにポーリング**して下書き生成 → 同じ場所へ書き戻す。
  - **認証＝Microsoft Graph の delegated permissions のみ**（`Sites.Selected` ＋ `offline_access`）。
    **application permissions（app-only）は会社方針で不可**（範囲が個人でなく会社テナントになるため／上長判断）。
  - **B2（PC→VPSのHTTPS/443）は不採用。** 認証をM365アカウントに委ねられるため、自前の入口を持つ必要がなくなった。
  - **→ Caddy・443・ドメイン取得・証明書・利用者トークンの発行/失効運用が、まとめて不要になった**（6・12・13章）。
    **v0.7 が「グローバル非依存制約の唯一の例外」として管理者OKを取ろうとしていたシステム側変更は発生しない。**
  - Graph APIは v0.7 で「将来課題・別プロジェクト」としていたが、**本命に格上げ**（7章）。
  - 実機検証：device code flow・refresh_token の無人更新・Graphでの一覧/DL/書き戻しが VPS 上で成功（2026-07-15〜16）。
    **ただし `Sites.Selected` での検証は未了**（M365管理者の付与待ち）。詳細はフロー整理 §8.6。

---

## 1. 目的・ゴール

- 議事録作成の「AI草案生成（現状は人がChatGPTへコピペ）」をAPI化して自動化する（＝現行手順の③を自動化）。
- 将来的に、トリガ発火後の処理（文字起こし・草案生成・Backlog反映）をVPS上で完結させ、各自PCでスクリプトを回さなくてよい社内サービスにする。
- 公開前チェック（人名・数値・日付・機微情報の確認）は引き続き人が実施する。**VPSが自動で作るのは「ドラフト」まで**で、Backlogへの本登録は人の承認後に限定する（8章）。

### スコープ外（今回やらないこと）
- 公開前チェックの自動化（意図的に人手を残す）。
- Cybozuからの録画自動取得（公式APIが無いため、当面は人手ダウンロードを前提）。
- 未チェック草案の全社共有領域への自動公開。

---

## 2. 現状（As-Is）と課題

### できていること（PC版）
- 録画取り込み → 音声抽出（FFmpeg） → 文字起こし（whisper.cpp、PC内完結）
- Teamsトランスクリプト（.vtt）併用：話者名つきテキストとwhisperの2ソースをAIに併記、登壇者を自動セット
- 会議番号（Backlog自動採番）・開催日・録画リンクの自動決定
- AIプロンプト自動生成 → （人がAIへ貼付・.md取得） → Backlog登録

### 課題（今回対処したい）
- ③のAI草案生成が手作業（ブラウザでコピペ・.mdダウンロード）。
- 文字起こしがPCのCPU依存で重い／低スペックPCで遅い。
- 各自PCにツール一式を配布・維持する必要がある。

---

## 3. 利用コンポーネント

| コンポーネント | 役割 | 備考 |
|---|---|---|
| Cybozu Office | 録画（.mp4）・トランスクリプト（.vtt）の保管 | 公式ダウンロードAPIなし＝取得は人手 |
| VPS（NixOS, AI-OCR用） | サーバー側処理の実行基盤 | `~/projects/minutes/` 配下に自己完結で構築。グローバル依存は足さない |
| さくらのAI Engine | 文字起こし（Whisper）・草案生成（LLM） | OpenAI互換API・国内完結・無料枠。承認済み |
| Backlog | 議事録ドキュメントの登録先 | Document API。追加可・APIでの削除は不可（削除はブラウザで手作業） |
| **専用SharePointサイト**（v0.8） | **入口。担当者がmp4/vttを置き、下書きを受け取る場所** | `/sites/minutes`（新設済み）。1会議1フォルダ。ブラウザ or OneDrive同期で操作 |
| **Microsoft Graph API**（v0.8） | **専用サイトの検知・取得・書き戻し** | **delegated のみ**（app-only は会社方針で不可）。`Sites.Selected` ＋ `offline_access`。**付与はM365管理者待ち** |

### さくらAI Engine 仕様（設計の前提）
- API：OpenAI互換 Chat Completions／Responses、Anthropic互換 Messages。
- 認証：APIアカウントトークン（Bearer。環境変数 `SAKURA_AI_TOKEN` で管理、ファイルに置かない）。
- モデル：文字起こし `whisper-large-v3-turbo`／LLM `gpt-oss-120b`・`llm-jp-3.1-8x13b`・`PLaMo 2.0-31B`・`cotomi v3` 他。
- 無料枠：Chat **3,000回/月**、音声文字起こし **50回/月**、Embeddings 1万/月。
- データ取扱い：**国内クラウド完結・学習利用なし・承認済みAPI（さくらAI Engine）へのみ送信**（国外／未承認AIへは送信しない）。※音声・文字起こしはこの承認済みAPIへは送信される（社内VPS内で完結はしない）点に留意。

#### 文字起こしAPIの具体仕様（出典：さくら公式ナレッジ／マニュアル）
- エンドポイント：`POST https://api.ai.sakura.ad.jp/v1/audio/transcriptions`（OpenAI互換）
- パラメータ：`file`（multipart）＋`model=whisper-large-v3-turbo`
- 対応：mp3/wav/m4a/mp4 等。**1リクエストの上限は「最長30分 または 30MB」**（内部で30秒ごとに分割・結合）
- レスポンス：`{"text":"...","model":"whisper-large-v3-turbo"}` の**テキストのみ**（話者名・タイムスタンプは返らない）

> ★設計に効く制約：当会議は**45分超**があり30分上限を超える。
> - 送信するのは**動画(mp4)ではなく、ffmpegで抽出・mp3圧縮した音声**（例：元動画205MB → 音声mp3は約10〜20MB）。動画そのものは送らない。
> - それでも「30**分**上限」があるため、音声は**分割**して複数回リクエストし、`text`を順に結合する。実装要件は「3.1」に定義する。
> - 話者割当はTeams .vttを主とし、Whisperは聞き取り・固有名詞の**補正**に使う（次項）。消費回数は「1会議＝分割数（例：2）」で数える（10章）。

#### 話者割当の方針（VTT主・Whisper補正）（#1対応）
Whisperのレスポンスは`text`のみで話者・タイムスタンプが無いため、Whisper単独では「誰が」を機械的に割り当てられない。したがって：
- **Teams .vttの話者付き発話を骨格（＝誰が）**とし、**Whisper全文で聞き取り誤り・固有名詞・言い回しを補正（＝何を）**する。
- .vttが欠落、または.vttとWhisperの内容が大幅に食い違う場合は、話者推定を断定せず**人手確認（要確認）に回す**。
- 「Whisperを話者の正」とは扱わない。

### 3.1 文字起こしの分割・結合の実装要件（#2・#3・#5対応）
長音声対応は「分割できる」だけでなく、サイズ保証・分割位置・重複除去・順序結合まで定義する。

1. **サイズ/長さの保証（#2）**：`ffprobe`で音声のduration/sizeを検査 → 目標**25分程度**で分割 → 各チャンクをエンコード後に **duration < 30:00 かつ size < 30MB** を検証 → 超過時は再分割または低ビットレート再エンコード → **API送信前に満たさなければハードfail**（黙って送らない）。mp3ビットレートは音声認識に十分な範囲（例：モノラル 48〜64kbps）で固定。
2. **分割位置＝無音（#5）**：`ffmpeg -af silencedetect=noise=-35dB:d=0.5` で無音区間を検出し、目標時刻付近（探索範囲例：±3分）の**無音区間の中央**で切る。会議音声は閾値次第で過検出/未検出が起きるため、**候補が無い場合は固定時刻で分割し「要確認」フラグ**を立てる。境界に**数秒のオーバーラップ**を持たせる。
3. **オーバーラップ重複除去（#3）**：レスポンスはtextのみで時間情報が無いため、`chunk_i`末尾N文字と`chunk_{i+1}`先頭N文字の**文字列類似度で最長重複を検出し、一定閾値以上のときだけ削除**する。低信頼のときは**削除せず境界マーカー（例：`〔chunk境界〕`）を残す**（発言を誤って削らない安全側）。テストケース（重複あり/なし/部分一致）を用意する。
4. **順序結合**：`chunk_index` 昇順で結合。**全チャンク成功時のみ**結合・後続処理へ進む（9章のチャンク単位状態管理）。

> 枠の含意（確定）：勉強会は**月最大5回**のため、文字起こし50回/月には十分収まる（約10倍の余裕）。→ **文字起こしは毎回さくらのWhisper（`whisper-large-v3-turbo`）を使用**する。話者名は Teams .vtt を併用（Whisperは話者分離しないため）。消費は少ないが、失敗再実行・長時間分割での増加に備え、簡易な利用台帳だけ持つ（10章）。この方針により**ローカルのwhisper.cpp（約1.5GBモデル・重いCPU処理・未署名exeのAV警告）が不要**になり構成が単純化する。

---

## 4. 全体アーキテクチャ（To-Be）

```
録画(mp4)＝TeamsがOneDriveへ自動保存 ／ 字幕(vtt)＝Teamsのトランスクリプト
   │  ★人が手元にDLして、専用SharePointサイトへ置く（1会議1フォルダ）
   │    置く手段＝ブラウザ or OneDrive同期クライアント。認証＝M365アカウントそのもの
   ▼
[入口] 専用SharePointサイト  https://tcsbunkyou.sharepoint.com/sites/minutes    … 6章
   inbox/<会議フォルダ>/  ├─ *.mp4
                         └─ *.vtt
   ▲ │
   │ │  VPSが15分ごとにポーリング（Graph delegated・Sites.Selected・driveItem: delta）… 7章
   │ │  ジョブ登録（meeting_id・(drive-id,item-id)・cTag・状態=received）… 9章
   │ ▼
   │ VPS  ~/projects/minutes/     （Nix devenv/flake で自己完結・★外部からは接続を受けない）
   │  ├─ 1. 入力取得（Graphでmp4/vttをDL）
   │  ├─ 2. 文字起こし：（音声を30分/30MB未満に分割）→ さくらWhisper（`whisper-large-v3-turbo`）→ text結合。話者名は Teams .vtt を併用
   │  ├─ 3. プロンプト組立（Teams .vtt＋Whisperの2ソース。トークン上限対策…11章）
   │  ├─ 4. さくらAI Engine（Chat Completions）で議事録ドラフト生成
   │  └─ 5. ドラフト(.md)と事前確認レポートを out/ へ書き戻し（状態=draft_ready）… ★ここまでが自動
   │                                                                    … 処理済みの入力は processed/ へ移動
   └────────────────────────────────────────────────────┘
        （HTTPレスポンスが無いので、状態も結果も SharePoint 側に書き戻して人に見せる）
   │
   ▼
公開前チェック（人・必須）… OneDrive同期＋手元のエディタで out/ の .md を直接編集（実測で確認済み）
   │
   ▼
承認（approval）… 8章。approved/ へ移動＝承認 ／ ★approved/ は登録してよい人だけ書き込み可（権限分離が前提）
   │
   ▼
出口：Backlog Document API で本登録（状態=registered）
   └─（任意）登録後、Backlog Webhook が外部へ通知発火／VPSからチャット通知
```

- 「誰が＝Teams .vtt」「何を＝whisper（またはさくらWhisper）」の2ソース突き合わせは現行踏襲。
- ドラフト生成のプロンプトは、PC版で確定済みの `prompt_template.txt` を流用。
- **自動処理はドラフト出力（draft_ready）まで**。Backlog本登録は人の承認後（8章）。
- **★VPSは外部から接続を受けない（outbound のみ）。** 入口はSharePointであり、VPSに公開面は無い（§6.3）。
- **⚠️ `approved/` 方式は「フォルダ単位の権限分離ができること」が採用条件**（§8・フロー整理 §7-4・§10.5）。
  **誤登録はAPIで取り消せない**（`register.ps1` 冒頭に明記）ため、「うっかり移動＝登録」の緩さを権限分離で補う。
  **分離できないなら承認方式を再検討する。**

---

## 5. VPS上の構成（`~/projects/minutes/`）

制約：ツール導入は**このフォルダ内のNix devenv/flakeで自己完結**させ、グローバル（システム全体）への依存追加はしない。

```
~/projects/minutes/
  flake.nix / flake.lock   # 依存の定義と固定（python3, ffmpeg, curl 等をこの中だけで）
  devenv.nix / .envrc      # 開発シェル（direnv連携は任意）
  src/
    build_prompt.py        # 2ソースのプロンプト組立（PC版ロジックの移植）
    parse_vtt.py           # Teams .vtt 解析（話者正規化・speaker_aliases）
    transcribe.py          # 音声を30分/30MB未満に分割→さくらWhisper呼び出し→text結合（ffmpeg利用）
    draft.py               # さくらAI Engine Chat Completions 呼び出し（③）
    register.py            # Backlog Document API 登録（承認後のみ）＋通知
    jobstore.py            # ジョブ/状態・冪等性（9章）
    quota.py               # 無料枠の消費記録・しきい値（10章）
    graph.py               # ★v0.8 Graph認証（device code / refresh）＋ delta / DL / UL      … 6・7章
    ingest.py              # ★v0.8 入口（専用SharePointサイトのポーリング → ジョブ登録 → 書き戻し）… 6・7章
    ~~server.py~~          # ✗ v0.8で廃止（FastAPIのHTTP受口は不採用。§6.3）
  prompts/prompt_template.txt
  config/settings.toml     # 非機密設定（モデル名・親ドキュメントID・しきい値・SP_SITE_ID / SP_DRIVE_ID 等）
  state/                   # ジョブ台帳（SQLite等）・クォータ台帳・delta token
  inbox/  work/  out/  logs/
```

- **v0.8 の追加依存**：`msal`（または生のHTTPで device code / refresh を実装）を `flake.nix` に追加。**フォルダ内で完結**。
- **`config/settings.toml` に持つのは `SP_SITE_ID` / `SP_DRIVE_ID`**（**`SP_LIST_ID` ではない**）。
  → **lists 経路は `Sites.Selected` で403という未解決報告があるため、drive 経路を使う**（フロー整理 §8.6）。
- `inbox/` `out/` は**VPSローカルの作業領域**。**利用者から見える入口/出口は SharePoint 側**（§4）。

### 依存・常駐運用（Nixで自己完結）
- 依存は `flake.nix`＋`flake.lock` で固定。**グローバル `pip` 等でのインストールは禁止**（devenvシェル or `nix run` 経由でのみ実行）。
- 実行入口：バッチは `nix run .#draft -- <input>`、**定期実行は `nix run .#ingest`**（v0.8。旧 `nix run .#server` は廃止）。
- **定期実行は user-level systemd の timer（`systemctl --user`）で15分ごとに起動**（v0.8）。**常駐プロセスは持たない。**
  → **system全体の変更は一切行わない。v0.7 が唯一の例外としていた443終端のCaddyは、入口の変更により不要になった**（§6.3・§12）。
- ログは `~/projects/minutes/logs/` に出力（秘密情報はマスク。12章）。失敗時はジョブ状態=failed＋リトライ方針（9章）。
- 秘密情報（AI Engineトークン・Backlog APIキー）は**環境変数**で渡す（`SAKURA_AI_TOKEN` / `SAKURA_AI_BASE_URL` / `BACKLOG_API_KEY`）。ファイル・リポジトリ・ログに置かない（12章）。
- 実装言語は Python（OpenAI互換呼び出し・VTT解析が書きやすく、Nixで閉じ込めやすい）。PowerShell資産は移植。

---

## 6. 入口（トリガ）設計 ― 確定（v0.8 で全面改訂）

> **v0.7 の「入口＝B2（PC→VPSのHTTPS/443・Caddy・独自トークン）」は不採用。** 却下理由は §6.3。
> 決定の経緯・根拠は `議事録自動化_フロー整理.md` §3〜§5・§8 が一次情報。

前提（確定）：**この仕組みは他のメンバーも使える必要がある。** かつ**利用者は全員エンジニア**（Web UIは不要）。
→ SSHは**管理者IP限定**（`110.4.190.167/32`・2201）なので、**A（SSH/CLI）は配布する入口にできない**。ここは v0.7 と同じ。

**上長助言（決定の起点）**：
> Microsoft系のサービスに依存するなら**アカウントそのものが認証の役割**を、
> エンドポイントを叩く場合は何らかの allow / deny が必要です。

### 6.1 決定

- **本番入口＝専用SharePointサイト**（`https://tcsbunkyou.sharepoint.com/sites/minutes`・**新設済み**）。
  担当者が **1会議1フォルダ**で mp4 と vtt を置く。**置く手段はブラウザ or OneDrive同期クライアント**（どちらも検証済み）。
- **トリガ＝VPSからの15分ごとのポーリング**（Graph `driveItem: delta`）。**VPSは外部から接続を受けない。**
- **認証＝利用者のM365アカウントそのもの。** 議事録用の独自トークンは**発行しない**。
- **A（手動CLI/SSH）＝ 管理者の開発・検証用のみ**（配布しない）。**Phase 1 のつなぎ運用としては当面継続**。
- **D（Backlog Webhook）＝ 入口ではなく出口の通知**（8章）。v0.7 から変更なし。

| 方式 | 概要 | 位置づけ |
|---|---|---|
| **S. 専用SharePointサイト＋VPSからポーリング** | 担当者がライブラリに置く → VPSがGraphで検知・取得 | **★本番入口（v0.8で確定）** |
| A. 手動CLI（SSH） | VPSにSSHして `nix run .#draft -- input` | 管理者の開発/検証用のみ／Phase 1のつなぎ |
| ~~B. PC側ツールから送信（HTTP）~~ | ~~`run.ps1` がVPSのAPI(443)へPOST~~ | **不採用（v0.8）**。§6.3 |
| C. VPSのフォルダ監視 | VPS上の受信フォルダに置くと発火 | 不採用（そもそもVPSにファイルを置く手段がSSH＝配布不可） |
| D. Backlog Webhook受信 | Backlog操作で発火 | 入口ではなく出口通知（8章） |

**この方式が成立する理由**：**「置く」と「トリガ」と「認証」が1つに畳まれる。**
担当者がSharePointに置ける時点で認証は済んでおり（M365アカウント）、置いた事実がそのままトリガになる。
v0.7 の C（VPSのフォルダ監視）を不採用にした理由は「**アップロード手段とトリガが分離する**」ことだったが、
**SharePointならアップロード手段（ブラウザ/同期）とストレージが一体**なので、この欠点が消える。

### 6.2 セキュリティ要件（v0.8）

**v0.7 の要件の大半が「自前の入口を持たない」ことで不要になった。** 残るのはGraph側の設計。

- **公開ポートなし。** VPSは **outbound のみ**（`login.microsoftonline.com` / `graph.microsoft.com` / さくらAI Engine / Backlog）。
  → **443の待受・TLS終端・証明書・ドメインが不要**（§6.3・§12）。
- **認証：Microsoft Graph の delegated permissions のみ。** app-only は**会社方針で不可**（上長判断）。
  - スコープは **`Sites.Selected`（サイトコレクション単位）＋ `offline_access`**。**専用サイト1つに限定**する。
  - **delegated は「アプリの権限 ∩ 利用者の権限」の積集合**（公式明記）。
    → **アプリが利用者の権限を超えることは構造的にない。** これが会社的な安全性の本体。
  - **⚠️ `Sites.Selected` で各操作が通るか・意図した範囲に本当に絞れるかは未検証**（管理者の付与待ち）。
    **ネガティブテスト（範囲外が403になること）は省略不可**（フロー整理 §8.6）。
- **トークンの保持**：`refresh_token` をVPS上に保持し、`grant_type=refresh_token` で無人更新（§12）。
  - **⚠️ refresh_token は漏れれば「本人としてGraphを操作できる鍵」**。**バッチ専用OSユーザーへの分離が必須**（§12）。
  - **⚠️ ローテーションは漏洩対策にならない**（古いRTは自動失効しない・実測確認済み／フロー整理 §8.4）。
- **利用者識別・監査**：**Graph側（Entraのサインインログ・SharePointの監査ログ）に寄せる。**
  → v0.7 の「利用者ごとの発行トークン＋ハッシュ台帳＋失効・退職者無効化」は**運用ごと不要**（M365の入退社処理に従う）。
- **入力検証**：拡張子/MIME検証（mp4/vtt のみ許可）、サイズ上限、ファイル名サニタイズは**引き続き必要**
  （SharePointに置ける＝安全ではない。人は誤ったものも置ける）。
- **実行制御（非同期ジョブ）**：**同時実行数の上限＋待ちキュー**、**同一入力の二重投入は既存jobを返す**（冪等・§9）、
  **クォータ不足時は受付拒否＋通知**（月跨ぎの保留はしない・§10）。v0.7 から**変更なし**（HTTPの有無と無関係のため）。
  - ただし**返し方が変わる**：HTTPレスポンスが無いので、**状態は SharePoint 側に書き戻して人に見せる**（§7・§8）。

### 6.3 なぜ B2（PC→VPSのHTTPS/443）を採らないか（v0.8 で却下）

**v0.7 の B2 は技術的に不可能だったのではなく、SharePoint方式が上位互換になったため不要になった。**
制約の強い案件なので、却下理由を残す（合意形成のため）。

| v0.7 が B2 のために必要としていたもの | v0.8 でどうなったか |
|---|---|
| **Caddy で443を待受**（＝`configuration.nix` を触る＝**「グローバル依存を足さない」制約の唯一の例外**。管理者OKが必要） | **不要**。VPSは外部から接続を受けない → **例外そのものが消滅** |
| **ドメイン取得 or 社内CA/自己署名の配布方式の決定**（Step3の前提条件） | **不要** |
| **Let's Encrypt / ACMEチャレンジ方式の確定** | **不要** |
| **利用者ごとのトークン発行・ハッシュ台帳・失効・退職者無効化の運用** | **不要**（M365アカウントに委ねる） |
| **レート制限・濫用対策・接続元IP許可リスト** | **不要**（公開面が無い） |
| **PC側 `run.ps1` の配布・維持** | **不要**（ブラウザ or OneDrive同期で置くだけ） |

**→ 「外部からVPSへ穴を開ける」という話が丸ごと消える。** 上長が指摘した
「**外部からテナント内にアクセスできるようにとなると、会社のセキュリティポリシーの話が出てくる**」は
app-only に対する指摘だったが、**自前エンドポイントの公開にも同じ論点が当たる**。B2 はその論点を抱えたままだった。

**却下したのは B2 の実装スタック（Caddy/FastAPI受口/証明書/利用者トークン）であって、以下は生きている**：
- **非同期ジョブ・状態管理・冪等性**（§9）… HTTPの有無と無関係
- **クォータ運用**（§10）・**トークン上限対策**（§11）… 同上
- **FastAPI そのもの**は不要（受口が無いため）。`server.py` は **`ingest.py`（ポーリング）に置き換え**（§5）。

---

## 7. 録画/字幕のVPS取り込み ― 確定（v0.8 で全面改訂）

入口をSに確定したので、取り込みもそれに揃える。**Graph API は v0.7 の「将来課題・別プロジェクト」から本命に格上げ。**

| 手段 | 概要 | 位置づけ |
|---|---|---|
| **Microsoft Graph（delegated）で専用サイトから取得** | VPSが `driveItem: delta` で検知 → `@microsoft.graph.downloadUrl` で mp4/vtt をDL | **★本番（v0.8で確定）** |
| 手動アップロード（SCP） | 管理者がSSHでVPSへ置く | A（検証）用のみ／Phase 1のつなぎ |
| ~~PC側ツールがHTTPで送信~~ | ~~`run.ps1` が multipart で VPSへPOST~~ | **不採用（v0.8・§6.3）** |
| ~~Graph で Teams録画/字幕を自動取得~~ | ~~主催者のOneDrive/transcriptを直接監視~~ | **不採用（§7.2）。人が置く方式に一本化** |

### 7.1 取り込みの要点（フロー整理 §8.5 が根拠）

- **検知**：`driveItem: delta`。**children列挙は使わない**（公式が「列挙中に書き込みがあると全件を返す保証がない」と明記）。
  - `410 Gone` + resync のハンドリングが必須。`?token=latest` で初期全列挙を回避できる。
  - **⚠️ ODfB/SharePoint の delta は root 単位**。フォルダ単位の監視は公式REST仕様では確認できない
    → **専用サイトを1つ切ってサイトごと監視する**ことで、この制約を設計で回避している（これが専用サイト新設の理由）。
- **重複判定**：**`(drive-id, item-id)` をキーに `cTag`** で再処理要否を判定（公式のscan-guidanceが明示的に推奨）。
  - `id` は**移動・リネームで不変**（公式明記）。**同期クライアント経由の移動でも保持されることを実測で確認済み**
    （フロー整理 §8.6。ただし `UniqueId` での確認であり、**Graph の item-id でも同じかは `Sites.Selected` 付与後に再確認**）。
  - ハッシュは **`quickXorHash` のみ保証**（`sha256Hash` は公式に「Don't use」と明記）。
- **ダウンロード**：`$select` で **`@microsoft.graph.downloadUrl`** を取得して直接GET（**Authorizationヘッダ不要**）。
  - **`Range` は downloadUrl 側に付ける。** レンジが無視されて `200` で全体が返ることがあるため **`206`/`200` の判定が必須**
    （しないと再開処理でファイルが壊れる）。
  - downloadUrl の有効期限は公式内で矛盾（「1 hour」／「might expire within minutes」）→ **取得直後に使い、失効したら再取得**。
- **アップロード（書き戻し）**：**250MB超は `createUploadSession`**（各チャンク60MiB未満・320KiBの倍数・順次）。
  下書き `.md` は小さいので単純PUTで足りるが、**`createUploadSession` 経路自体は実録画で要検証**（フロー整理 §8.6）。
- **アップロード途中の検知**：`pendingOperations` の挙動は公式に記載なし → **`size`・`cTag` の安定化待ちを併用**。
- **スロットリング**：429は **`Retry-After` を最優先**。UA装飾 `NONISV|<会社名>|<アプリ名>/<版>` を付ける。
- 1時間の録画は**約400MB**（Teams公式）。

### 7.2 なぜ「Graphで録画/字幕を自動取得」まで行かないか

**「担当者が置く」を残し、全自動にはしない。** 理由：

- **フォルダ単位の監視ができない。** 主催者（渡邉さん）のOneDriveを起点にすると、
  **ドライブ全体がアプリの視界に入る**ことを意味する（§7.1）。個人ファイル全体を対象にするのは筋が悪い。
- **transcript は Graph で取れるが、Teams管理センターに既定OFFのトグルが2つある。**
  片方がOFFなら `403` で**回避策なし**。公式に「関連するテナント管理者の制御は**2026年7月末から有効**」と記載＝期限つき。
- **`getAllTranscripts` / `getAllRecordings` は delegated 非サポート**（公式に "Not supported."）。
- **会議は週1回**。上長談「**指定の場所にファイルを置いてAPI実行したら記事が生成されるといっただけでも十分**」。
  → **全自動の対価（他人のドライブ全体への可視性・管理者トグル依存・実装量）が、週1回の手間に見合わない。**

- 人がCybozuへ録画をアップロードするところは**当面残る**（Cybozuに取得APIなし・§1スコープ外）。
  録画リンクは**Cybozu側のURLを議事録に載せる**運用を継続（フロー整理 §6）。

---

## 8. 出口（Backlog登録）と公開前チェックの整合 ★重要（#1・#2対応）

未チェックの草案をBacklogの共有領域へ自動登録すると、公開前チェックの前提が崩れる。したがって出口は次のとおり分離する。

- **既定（自動範囲）**：VPSは草案を**専用SharePointサイトの `out/` へ書き戻す**だけ（状態=draft_ready）。**Backlogには自動登録しない。**
  （v0.7 は「VPSの `out/<meeting_id>.md` に出力」だったが、**それでは人が見られない**ため v0.8 で SharePoint 側に変更。§4）
- **公開前チェック**：人が **OneDrive同期＋手元のエディタ**で `out/` の `.md` を直接編集する（**実測で確認済み**・フロー整理 §8.6）。
  併せてVPSが**事前確認レポート**（重複・番号衝突・登録先）を `out/` に書き出す（`--dry-run` 相当）。
- **承認 ＝ `approved/` フォルダへ移動（v0.8で改訂）**
  - ~~承認メタデータ `state/<meeting_id>.approval.json`~~ → **✗ 廃止。**
    **VPSローカルの承認ファイルは、SSHが無いと作れない＝本件で解こうとしている課題そのものだった。**
  - **`approved/` へ移動された `.md` を `ingest` が検知し、`register.py` がBacklogへ登録する。**
    **同期クライアント経由の移動でも item-id が保持されることを実測で確認済み**（フロー整理 §8.6）。
  - **⚠️ 採用条件：`approved/` の権限分離**（登録してよい人だけ書き込み可）。**§14-1c。できないなら承認方式を再検討。**
  - **⚠️ 「うっかり移動＝登録」は `y` 押下より明確に緩い。かつ誤登録はAPIで取り消せない。**
    `register.ps1` 冒頭に「**The API cannot delete (DELETE is rejected; removal is only possible by hand in the
    Backlog web UI), so this script cannot undo a post - hence the confirm gate.**」とあるとおり、
    **`y` の確認ゲートは「APIで取り消せないから」存在している。** 復旧には**権限を持つ人がBacklogのWeb UIで手作業削除**が要る。
    → **緩さは権限分離で補う**。加えて `find_duplicate` / `number_conflict`（`vps/minutes/src/backlog.py`）は従来どおり走る。
  - **⚠️ 人が `.md` を編集するとレポートは陳腐化する** → `approved/` 到着時に `cTag` が変わっていれば **dry-run を再実行**し結果を残す。
- **重複防止**：登録前に同名・同ハッシュ確認（9章）。**削除できない前提で二重登録を厳に防ぐ。**
- **通知（≠出口）**：Backlog Webhook は「Backlog上のイベントを外部へ知らせる」仕組み。VPSからBacklogへ送るものではない。用途は「本登録後にBacklog Webhookが関係者へ通知を発火」または「VPSからチャット等へ通知」。この2つを混同しない。

> どうしてもBacklog上でレビューしたい場合は、「非公開のレビュー専用領域に限定」または「本登録後のみ」に分ける。全社共有に近い親ドキュメント直下へ未チェック草案を置かない。
> **なお専用SharePointサイトは全社共有領域ではない**（`Sites.Selected` の対象＝議事録担当者のみのサイト）ので、
> 未チェック草案を `out/` に置くこと自体はこの原則に反しない。

---

## 9. ジョブ・状態管理／冪等性・重複防止（#5対応）

複数会議・再送・タイムアウト後の再試行で、二重生成・二重登録が起きないようにする。

- **識別子**：`meeting_id`（録画名の `YYYYMMDD_HHMMSS` トークン由来）＋入力ファイルの SHA-256。同一入力の再投入は同一ジョブとして扱う（冪等）。
- **状態遷移**：`received → queued → running → draft_ready → approved → registered`（同時実行の空き待ちは `queued`／異常は `failed`／入力不正・クォータ不足での受付拒否は `rejected`）。ポーリングAPI・監査ログ・再投入時の返却はこの状態名で表す。台帳は `state/`（SQLite等）で管理。
- **排他**：`meeting_id` 単位のロック（ロックファイル or DBトランザクション）で同時二重処理を防止。
- **重複登録防止**：Backlog登録前に、既存の同名ドキュメント／同ハッシュ済み登録を確認（PC版の重複チェックを踏襲）。APIでは削除できず、取り消しはブラウザでの手作業になる前提で二重登録を厳に防ぐ。
- **チャンク単位の冪等性（#4対応）**：長音声は「chunk 1成功・chunk 2失敗」が普通に起きる。会議単位で丸ごと再実行すると成功済みチャンクを再送してクォータを浪費し、結合順序事故も起きる。そこで `meeting_id + audio_hash + chunk_index + chunk_hash` を保存し、各チャンクに状態 `pending/running/succeeded/failed`・API呼び出し回数・取得済みtextを持たせる。**再実行は未成功チャンクのみ**送る。結合は `chunk_index` 昇順で、**全チャンク succeeded のときだけ**実行する。
- **リトライ**：外部API失敗は指数バックオフで限定回数リトライ（429/5xx対応）。文字起こしリトライはクォータ消費に反映（10章）。

---

## 10. 無料枠（クォータ）の運用管理（#4対応）

さくらAI Engineの無料枠、特に**音声文字起こし50回/月**の消費を把握する。

前提（確定）：勉強会は**月最大5回**。1会議が30分超だと事前分割で複数回リクエストになる（例：45分→約2回）。それでも月5会議×約2回＝**約10回/月**で、50回に対し十分な余裕がある。そのため**厳格な事前ゲート（上限で自動停止）は必須ではなく、簡易な記録と警告で足りる**。

- **消費記録**：`state/quota.*` に月次・会議IDごとの呼び出し回数（Whisper／Chat別）を記録。「1会議＝1回」とは限らない（再実行・長時間分割）前提でカウント。
- **実行前の見積り（#6対応）**：処理開始前に **予定チャンク数＋最大リトライ分** を見積もり、当月残枠が不足する場合は**開始前に警告／停止を選べる**ようにする（90分会議・30MB超過による追加分割・リトライで消費が増える最悪ケースに備える）。
- **月次リセット基準**：カウントの**リセット基準日・タイムゾーン**（例：毎月1日 00:00 JST。実際はさくら側の基準に合わせる）を明記し、月替わりの数え間違いを防ぐ。
- **警告（保険）**：残回数がしきい値（例：残り10回）を切ったら警告。**上限到達時は新規受付を拒否＋利用者へ通知**（キューでの月跨ぎ保留はしない。§6と一致。通常は発生しない想定）。
- **可視化**：月次の利用台帳を出力（残枠の見える化）。
- 将来、会議数や1会議あたりの分割回数が増えて枠に近づくなら、その時点で有料枠を検討する。

---

## 11. LLM入力のトークン上限対策（#6対応）

長時間会議では、Teams .vtt ＋ Whisper の2ソース併記がモデルのコンテキスト上限を超え得る。

- **見積り**：投入前に入力トークン量を概算し、上限を超える場合は分割処理に切り替え。
- **分割・階層要約**：議題／時間帯単位で中間要約 → 最終統合。切り捨て（truncation）で内容を落とさない。
- **切り捨て禁止チェック**：入力が上限超過なのに単純切り詰めで送る事故を防ぐガード。
- **呼び出し回数**：長時間会議は Chat を複数回呼ぶ前提で見積る（3,000回/月に対しては十分だが、台帳に計上）。

---

## 12. セキュリティ・ガバナンス（#3・#8対応）

- 使用AIは承認済みのみ：さくらAI Engine（小見山さん承認）。外部AI/外部サーバー利用は上長承認が前提（本件は承認済み）。
- データ取扱い：さくらAI Engineは**国内クラウド完結・学習利用なし**。音声・文字起こし・草案は**承認済みAPI（さくらAI Engine）へは送信される**（社内VPS内で完結はしない）。**国外／未承認AIへは送信しない**。「外部送信なし」とは表現しない。
- 社内情報（録画・議事録）は外部同期される場所に置かない。VPSは社内管理下のNixOS。
- 公開前チェックは人手を維持（品質・コンプライアンスの最終担保）。未チェック草案はBacklog共有領域へ出さない（8章）。
- VPS作業は `~/projects/minutes/` 配下で自己完結。アプリのツール依存はグローバルに足さない（Nix devenv/flake、`flake.lock`固定、グローバルpip禁止）。
  - **✅ v0.8：例外は無くなった。** v0.7 は「HTTPS/443終端のCaddy＝唯一の例外・管理者の明示OKが必要」としていたが、
    **入口をSharePointに変えたことでVPSは外部から接続を受けなくなり、443の待受が不要になった**（§6.3）。
    → **`configuration.nix` への追加は発生しない。制約を例外なしで満たせる。**

### 秘密情報・権限（強化）
- 実行は**専用OSユーザー**（最小権限）で行う。他プロジェクトと権限を分離。
- Backlog APIキーは**最小権限**（登録に必要な範囲）。削除不可の運用キーと管理用キーを分離。
- 秘密の種類で管理を分ける：
  - **システム秘密（`SAKURA_AI_TOKEN` / `BACKLOG_API_KEY`）** … VPS側の**環境変数のみ**。**注入手順・ローテーション手順**を定める（誰がどこで設定するか）。
  - ~~**利用者トークン（B2認証）**~~ … **✗ v0.8で廃止。** 入口がSharePointになり**独自トークンを発行しない**ため、
    発行・ハッシュ台帳・失効・退職者無効化の運用が**まるごと不要**になった（**M365の入退社処理に従う**）。
  - **★Graph の `refresh_token`（v0.8で新設）** … **下記の専用項で扱う。最も慎重な扱いが要る秘密。**
- ログ・プロセス環境からの漏えい対策：ログの秘密情報**マスク**。エラー出力にトークンを含めない。
- `SAKURA_AI_BASE_URL` は**許可リスト検証**（想定ホスト以外への送信を防ぐ）。
- 監査ログ：**入口の受付はEntra/SharePointの監査ログに寄せる**（誰がいつ置いたか）。VPS側は登録・失敗を記録（秘密情報はマスク）。

### ★Graph の refresh_token の扱い（v0.8で新設・フロー整理 §8.4 が根拠）

**これは「中村本人としてGraphを操作できる鍵」であり、本設計で最も慎重な扱いを要する秘密。**

- **公式の性質**：「Refresh tokens are bound to a combination of **user and client**, but **aren't tied to a resource or tenant**.」
  → **読めた人は、中村本人として同意済みスコープ全部でトークンを取得できる。**
- **⚠️ ローテーションは漏洩対策にならない（実測確認済み）**：更新時に新しいRTが返っても、**古いRTは自動失効しない**。
  **同一RTの3回連続使用が成功することを実測で確認**（2026-07-16）。→ **コピーされた時点で終わり。**
- **⚠️ 現構成では公式の保護要件を構造的に満たせない**：公式要件は「access control **and encryption**, in relation to the
  **user-owner, or process identity**」。**VPSは全員が同じ `aiocr` でログインするため、同一 user-owner となり `0600` が無意味**。
  → **バッチ専用OSユーザーへの分離が必須**（上記「専用OSユーザー」と同じ話だが、**v0.8では必須要件に格上げ**）。
  → ただし**OSユーザー分離＋`0600` は「アクセス制御」のみで「暗号化」は別途**。
    **「公式要件を満たす」とは言わず「共有 `aiocr` より改善する」と表現する。**
- **失効事由（自動復旧手段は公式に存在しない＝人の再サインインが必要）**：
  条件付きアクセス、管理者による失効、**本人のパスワード変更/SSPR**、リスクベース制御、CAEのcritical event。
  - **⚠️ 実測で `amr: ["pwd","mfa"]` を確認＝今回発行されたRTはパスワードベース。**
    → **中村がパスワードを変更した時点で無人ポーリングが止まる。**
    → **パスワードレス方式でサインインし直せば塞げる可能性がある（要確認・フロー整理 §8.4）。**
  - → **いずれにせよ「止まったことに気づける仕組み」（§9 failed＋通知）が前提条件。**
- **検証で使ったトークンの後始末**：`/tmp/token*.json`・`~/.minutes-verify/` の消去、ユーザー同意の削除、
  **RTの明示的失効**、検証用アプリの削除まで**すべて実施する**（フロー整理 §8.6）。

---

## 13. 段階導入計画（ロードマップ）

- **Step 0（完了）**：AI Engineトークン発行・疎通確認済み（Chat/`gpt-oss-120b` と `whisper-large-v3-turbo` のエンドポイント確認）。
- **Step 1：③のAPI化（PC先行・VPS不要）… ✅実装・実データ検証済み（2026-07-13）**
  - PC版に `draft.ps1` を追加。`run.ps1 -To draft` で「プロンプト生成 → AI EngineにPOST → 返却Markdownを `work\minutes.md` に保存」。
  - モデルは `gpt-oss-120b`（§14-5）。実データで登壇者=Teams自動・テンプレ順守・正しい日本語を確認。
  - 実装時に判明した重要点：PS5.1の `Invoke-RestMethod` は応答を ISO-8859-1 で解釈して日本語が化けるため、`HttpWebRequest`＋UTF-8 StreamReader で明示デコードする（VPS実装のPython版では標準で問題にならないが、応答エンコーディングは要確認）。
  - 人はChatGPTへの貼付/DLが不要になり、公開前チェックと登録だけに。
  - ⚠ **適用は管理者PCまたは限定利用に留める**（AI Engineトークンを多数のPCに配るのはサーバー化の狙いと逆。全社展開はStep2/3のVPS集約後）。
- **Step 2：VPS化（CLI起点）… ✅ 実装・稼働中（第2回まで Backlog 登録済み）**
  - `~/projects/minutes/` をNixで構築し、Step1と同じ処理をVPS上でCLI実行（方式A）。
  - 文字起こしをさくらWhisper（`whisper-large-v3-turbo`）に統一し、ローカルwhisper.cpp/1.5GBモデルを不要化。
  - **⚠️ ただし現状は中村しか運用できない**（SSH鍵＋固定グローバルIPが必要）。**これがStep3で解く課題。**
- **Step 3：入口の自動化（v0.8で全面改訂）** ← **いまここ**
  - ~~VPSにHTTP受口（方式B）＋PCの `run.ps1` から送信~~ → **不採用（§6.3）**
  - **専用SharePointサイト ＋ Graph（delegated・`Sites.Selected`）＋ 15分ポーリング**（6・7章）。
  - **進捗**（詳細はフロー整理 §8.6・§9）：
    - ✅ 上長の暫定承認（2026-07-15）／✅ 専用サイト新設（`/sites/minutes`）／✅ 検証用アプリ登録
    - ✅ VPS上で device code flow・`refresh_token` の無人更新・Graphでの一覧/DL/書き戻しが成功
    - ✅ OneDrive同期の挙動を実測（編集・移動で item-id 保持 → `approved/` 方式が成立）
    - ✅ サインイン頻度の短い設定が無いことを実測（15時間11分後のrefreshが成功）
    - ⬜ **`Sites.Selected` の付与（M365管理者＝今井さん待ち）** ← **ブロッカー**
    - ⬜ **`Sites.Selected` での再検証**（一覧/DL/書き戻し・**ネガティブテスト**・実録画250MB超のUL）
    - ⬜ **条件付きアクセスの長いサインイン頻度設定の確認**（不可なら**方針ごと組み直し**）
    - ⬜ **`approved/` のフォルダ単位の権限分離**（**採用条件**。できないなら承認方式を再検討）
    - ⬜ 実装：`graph.py` / `ingest.py`（§5）
  - 出口：承認後のBacklog登録（8章）＋登録後の通知。
- **Step 4：運用整備**
  - ログ・監視・リトライ・クォータ台帳の運用、**user systemd timer での定期実行**（常駐ではない・§5）。
  - **★停止検知**：refresh_token の失効で無人ポーリングは止まりうる（§12）。**止まったことに気づける通知は必須。**

> Step 1 と Step 2 のコア（OpenAI互換呼び出し）は同一なので、PC版で作った `draft` ロジックをVPSへほぼそのまま移せる。
> **Step 3 でも処理コアは無改修。** `draft` の `--vtt`/`--out`/`--fid`/`--link`、`register` の `--md`/`--dry-run`/`--force` は
> **既に実装済み**なので、`ingest.py` から呼ぶだけで足りる（`vps/minutes/src/main.py` で確認済み）。

---

## 14. 要決定・依存（オープン事項）

| # | 論点 | 現時点の推奨 | 必要な情報/決定 |
|---|---|---|---|
| 1 | 入口（トリガ）方式 | 【v0.8で改訂・確定】本番=**S（専用SharePointサイト＋Graph delegated `Sites.Selected`＋15分ポーリング）**。A(SSH)=管理者の検証用/Phase 1のつなぎ。**~~B2（HTTPS/443・独自トークン）は不採用~~**（§6.3） | **上長の暫定承認済み**（2026-07-15）。**残：M365管理者による `Sites.Selected` の付与** |
| 1-a | **`Sites.Selected` で各操作が通るか・本当に範囲が絞れるか** | **未検証。公式の権限表にSelectedの明示が無く、delegated×Selected×deltaの実績報告はゼロ** | **付与後に実機確認。ネガティブテスト（範囲外が403）は省略不可**（フロー整理 §8.6） |
| 1-b | **条件付きアクセスのサインイン頻度（長い設定）** | 短い設定は無いことを実測済み（15時間11分）。**1日/7日/30日等は未確認** | **今井さんへ確認。設定されていればアプリ側で回避不可＝方針ごと組み直し** |
| 1-c | **`approved/` のフォルダ単位の権限分離** | **`approved/` 方式の採用条件。** 誤登録はAPIで取り消せないため、「移動＝登録」の緩さを権限で補う | **実機確認（設定できるだけでなく、権限の無い人が移動/コピー/上書きできないことを試す）。不可なら承認方式を再検討** |
| 2 | Backlog Webhookの位置づけ | 出口ではなく「登録後の通知」。出口はDocument API（8章） | この整理の確認 |
| 3 | Backlogドラフトの置き場 | 既定はout/のみ。本登録は承認後（8章） | 共有領域へ未チェック草案を出さない、の合意 |
| 4 | AI Engineトークン/エンドポイント | ― | 発行済みか、ベースURL、利用可否 |
| 5 | 草案生成に使うLLM | 【確定】`gpt-oss-120b`。実データ比較で最も網羅的・正確・安全側（要確認/機微退避）。`llm-jp-3.1-8x13b`は長プロンプトで400(context超過)、`PLaMo`/`cotomi`は当アカウント未開放。`preview/gemma`は作成日を誤生成 | 決定済み |
| 6 | 文字起こしの寄せ方 | 【確定】毎回さくらWhisper（月最大5回で50回枠に余裕）。話者名はTeams .vtt併用。ローカルwhisper不要化 | 決定済み |
| 7 | 実装言語 | Python（Nixで自己完結） | 合意（PowerShell資産は移植） |
| 8 | Step1の適用範囲 | 管理者/限定PCのみ | 全社展開はVPS集約後、の合意 |

---

## 15. 次アクション（v0.8 で更新）

> ~~1. 本設計書 v0.6.1 のレビュー~~ / ~~2. AI Engineの疎通テスト~~ / ~~3. §13 Step2着手~~ → **すべて完了**
> ~~4. §13 Step3公開の前提：証明書方式の決定、Caddy例外の管理者OK、利用者トークン発行運用~~
> → **✗ 消滅。** 入口をSharePointに変えたため、証明書もCaddy例外も利用者トークンも**発生しない**（§6.3）。

1. **本設計書 v0.8 のレビュー**（特に「6. 入口」「7. 取り込み」「12. refresh_tokenの扱い」「14. 要決定」）。
2. **アプリの本番化**：検証用アプリ（`minutes-automation-test`）を**リネーム**し、
   **`Files.ReadWrite` の同意を削除**して **`Sites.Selected` ＋ `offline_access`** に差し替える。
   → **⚠️ 同意を残したまま `Sites.Selected` を検証してはいけない**（「Selectedで通った」のか区別できず、
     **ネガティブテストが素通りする**。フロー整理 §8.6）。
3. **今井さん（M365管理者）へ依頼**（**2件**。サイト新設・アプリ登録・IP確認の一部は自力で完了済み）：
   - **① 専用サイトへの `Sites.Selected` 付与（roles: `write`）**
     ＋ **⚠️ 中村（および議事録担当者）をそのサイトの読み書きメンバーに追加**（忘れると動かない）。
     delegated は「アプリのSelected付与 ∩ 本人のSharePoint権限」のため、**アプリ側だけ整えても書けない。**
   - **② 条件付きアクセスの確認**（長いサインイン頻度設定 ／ IP制限。**送信元IPは `133.125.62.159` と確定済み**）。
4. **`Sites.Selected` での再検証**（§14-1a）→ 通れば **`graph.py` / `ingest.py` の実装**（§5・§13 Step3）。
5. 当面は **Phase 1（手動SSH＋CLI）で運用継続**。

**参照値（フロー整理 §0 と同期。実測で確定済み）**

| 項目 | 値 |
|---|---|
| テナントID | `85c9340e-a7a2-48cb-97cf-ca56a6e44875`（`tcsbunkyou.onmicrosoft.com`） |
| 専用サイト | `https://tcsbunkyou.sharepoint.com/sites/minutes` |
| siteId | `tcsbunkyou.sharepoint.com,92289f8d-ebbe-4ad3-b6a4-c041b99268e2,8046e19d-e6f7-49d5-ac57-d07e3b80b642` |
| VPSのグローバルIP | `133.125.62.159`（Entraから見えている実際のIP） |
| 検証用アプリ（clientId） | `81214b2e-d6e6-4afc-a371-06c033b56077`（**要リネーム**） |
