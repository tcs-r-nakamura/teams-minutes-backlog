# Backlog registration config - SHARED, non-secret values.
# setup.ps1 copies this to C:\minutes\backlog.config.txt if that file is absent.
# The live backlog.config.txt is git-ignored; this sample is committed.
#
# Each user only needs to set their OWN API key via the environment variable:
#     setx BACKLOG_API_KEY "<your personal Backlog API key>"
# Do NOT write the key into this file or the live config.

Space=tcs-s.backlog.com
ProjectId=520525
ParentId=019f3bd7c41e77b78709ccf2885a1175
AddLast=true

# Recording link written into every minutes draft. This is the Cybozu folder
# where recordings are stored (shared for the team). Update it here if the
# recording folder ever changes.
RecFolderUrl=https://tcs-s.cybozu.com/o/ag.cgi?page=FileIndex&fCID=45883
